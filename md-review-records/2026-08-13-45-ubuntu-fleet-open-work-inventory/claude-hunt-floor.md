<!-- provenance: runtime=claude model=claude-sonnet-5 effort=high cell=defect-hunt tier=floor target=/home/nedlern/Projects/nedschorus/.claude/worktrees/gatekeeper-walk-fork-continuation/docs/issues/queue/45-ubuntu-fleet-open-work-inventory.md -->

I read the target file in full, plus the CLAUDE.md files in scope, and verified several claims against files it references by explicit path (`docs/agents/*.md`, `scripts/launch-claude-mac`, `scripts/launch-claude-ubuntu`, `md-review-records/...`). Findings below, in document order.

1. **Lines 9** — "The gatekeeper seat has reported and is waiting on a ruling; the sanity-checker seat is triaging its four findings and writing queue documents to route them." Neither "a ruling" nor "its four findings" is pointed at a location. The doc does this explicitly elsewhere ("See § 6a for how they were launched"), so the omission here is inconsistent with its own practice. "Its four findings" is plausibly § 3's four bullets (the count matches) and "a ruling" is plausibly one of § 2's three items, but neither link is stated. Unsure whether this rises to a real defect, but a reader without the ability to count bullets across sections cannot resolve either reference confidently.

2. **Line 10** — "Every seat document has been md-reviewed, twelve in all, ... 23 findings on the gatekeeper brief, 28 on the seat model, 30 on the sanity-checker brief, 26 on the fleet brief, with "clean sections: none" on each of the first four." Only 4 of the claimed 12 documents are named, and "the first four" presupposes an ordering of twelve that is never given — the other 8 documents and their outcomes are unidentified. Separately, the informal names used here ("gatekeeper brief," "seat model," "sanity-checker brief," "fleet brief") are never mapped to actual filenames; line 13 later uses `seat-first-prompt.md`, `agent-seat-model.md`, `gatekeeper-instructions.md` — a reader has to guess that "seat model" = `agent-seat-model.md`, and `seat-first-prompt.md` is never named among "the first four" at all, so its review-finding count (if any) is unstated. Sure this gap exists.

3. **Line 10 vs. line 25** — "The corrections are in PR #58." (also relied on at lines 91, 98, 100 as a currently open, unmerged PR) directly conflicts with § 1's table and its closing line, "All five are open and awaiting the Mac-side review-and-merge seat." PR #58 is never listed in that table, yet the surrounding text repeatedly treats it as open and pending merge. Sure — this is a contradiction between what § 1 claims to enumerate and what § 0/§ 6a assert exists.

4. **Line 11, reused at lines 22, 31, 106** — "the briefs used *pile*, *walked approval*, *instruction-class*, *slice* and the C-numbers as if established, and none was defined anywhere. The seat model now defines them once." This file then itself uses "slice" and "walked-approval" as load-bearing terms without definition: "folded into the slice plan" (line 22), "the walked-approval evidence format, then build slice 6" (line 31), "evidence format → slice 6 → credential work" (line 106). The only pointer to a definition is the informal name "the seat model," not a resolvable path — so by the file's own diagnosis, it repeats the defect it describes. Sure. "The C-numbers" is additionally never explained anywhere in the file; unsure whether it matters since it's not reused elsewhere, but as written it is opaque.

5. **Line 12** — "the seat-first-prompt's repair command could not work — git worktree add refuses a non-empty path, and its "drop -b" variant was an invalid invocation, on the branch-already-exists path that any relaunched seat takes." The trailing clause "on the branch-already-exists path..." is ambiguous: it could modify only the "drop -b" variant, or both listed failure modes — the sentence supports both readings. Separately, "any relaunched seat takes" this path is an absolute claim that assumes the seat's branch was never deleted between launches; a seat cleaned up with its branch removed before relaunch would not hit either failure. Unsure how much this matters, but both are real readings.

6. **Line 22** — the PR #55 table row lists its owner stream as "a third stream," unlike every other row, which uses an established, findable name (`choirmaster`, `gatekeeper-walk-fork`). "A third stream" is not a name used anywhere else in the file and is not self-documenting or greppable. Sure this is inconsistent with the naming used elsewhere in the same table.

7. **Line 35** — "Surfaced beyond both ground-truth sets, never presented, each needing verification against current code before any walk — the cells read archived snapshots:" "both ground-truth sets" is never named or defined anywhere in the file — a reader cannot tell what the two sets are or where they come from. Sure this term is undefined in context.

8. **Line 44** — "Twenty-four open. By theme, for splitting:" Counting every issue number listed across the four bullet groups that follow (lines 46–49) gives 6 + 11 + 4 + 9 = 30 distinct issue numbers, not 24. Sure — this is a direct numeric contradiction between the stated total and the enumerated list immediately below it.

9. **Line 49** — "#32 (what NC preserves)" — "NC" is never expanded or defined anywhere in the file. Unsure whether this is meant to be self-evident from the issue title alone (acceptable terseness for a pointer) or is a genuine gap, but as written it is an unexplained abbreviation.

10. **Line 61, vs. lines 9 and 89** — The thread-map row for "sanity-checker" gives job `d9eda3ec` with state "retired; handoff written." But § 0 (line 9) and § 6a (lines 89, 96) describe a currently *live* seat also named `sanity-checker`, launched 2026-08-13, verified running, and actively "triaging its four findings." § 5 is introduced as "where each thread's context lives... A new session can be pointed at any of these," and its sweep subsection claims "Everything else is accounted for" — yet no row anywhere in § 5 gives the job/transcript for the live `sanity-checker` seat itself; only its retired predecessor is listed. Sure this gap exists in the table as written, though it may simply be very recent (too new to have a transcript yet) rather than an oversight — the file doesn't say which.

11. **Line 82** — "the launcher passes it through to the supervisor, which reads that file as the new session's first prompt and then reverts to ordinary ignition." "Ordinary ignition" is not defined in this file or in either CLAUDE.md in scope. Unsure whether this is meant to be self-evident ("normal startup"), but it reads as a specific term of art that is never introduced.

12. **Line 89** — "status line present (which is the tell that project settings loaded, and therefore that the recycle hook and the instruction-file guard loaded too)." Neither "the recycle hook" nor "the instruction-file guard" is defined in this file or in the provided CLAUDE.md files, and no path is given to where they are defined. This sentence is offered as a verification step ("verified to start correctly"), so a reader cannot independently check the reasoning behind it. Sure these terms are undefined in the given context.

13. **Line 96, with its justification in the surrounding prose** — "The launcher was then run with the prompt file taken from the seat's own checkout: `sh scripts/launch-claude-mac <seat> --no-attach --first-prompt-file /home/nedlern/agents/<seat>/docs/agents/seat-first-prompt.md`. The Mac twin runs locally on the box and is mechanically identical to the Ubuntu launcher minus the SSH hop, which is what makes it usable from a box-side session." I checked `scripts/launch-claude-mac`'s own header, which it references by explicit path: it states the script is to "reach the named agent ON THIS MAC" and "Use THIS script only for work that must run where the user sits — anything needing the Mac's browser session, its keychain, its GUI, or files that live only on it... the wrong pick is easy to make." The seats being created here live under `/home/nedlern/agents/<seat>` — the box's home directory, not the Mac's — which is exactly the kind of box-hosted work the script's own header warns the reader against using it for. (I confirmed mechanically that `launch-claude-mac` contains no `ssh` call while `launch-claude-ubuntu` does, so the "minus the SSH hop" claim is technically accurate.) The document presents this as the clever, correct workaround without acknowledging that it runs counter to the referenced script's own documented usage guidance. Sure about the factual conflict with the script's header; unsure whether the author considered it and judged it acceptable, since that judgment isn't stated.

14. **Line 106, vs. lines 57 and 90–100** — § 7's proposed seat is named `gatekeeper` and its context is given as "this stream's transcript and handoff" — i.e., the `gatekeeper + launchers (this stream)` thread from line 57 (job `ec9045a3`, owner of PR #57, running in the worktree this very document lives in). But § 7's `gatekeeper` bullet also claims "the gatekeeper's remaining road (evidence format → slice 6 → credential work)" — which §§ 0 and 6a assign to a *different*, already-running seat also named `gatekeeper`, living at `~/agents/gatekeeper`. The document never states whether § 7's proposed `gatekeeper` seat is the same entity as the already-launched `~/agents/gatekeeper` seat (in which case pointing its context at "this stream" rather than that seat's own context is confusing), a merge of the two, or a third, differently-located seat that simply reuses the name. This supports incompatible readings of what action § 7 is actually proposing. Sure the ambiguity is present in the text as written.

15. **Line 108** — "**`ghi`** — ghi-info (#46) and its neighbours (#41, #42, #39). Context: the ghi design documents already in the repo." Every other row in this list gives a concrete, followable context (a transcript path, a handoff path, "this stream's transcript and handoff"). This row alone gives no path at all, despite `docs/agents/ghi-instructions.md` and other ghi-related documents existing in the repo — a future agent has to guess which files "the ghi design documents" refers to. Sure this is inconsistent with the specificity used everywhere else in the same list.

clean sections: none

