<!-- provenance: runtime=claude model=claude-sonnet-5 effort=session-inherited (Agent tool has no per-cell effort control; model id read from subagent transcript) cell=defect-hunt tier=floor target=.claude/skills/d-review/SKILL.md session=4cf7d488 date=2026-08-04 -->

# Sentence-level defect review — `/Users/el/Projects/nedschorus/.claude/skills/d-review/SKILL.md`

Context used: `/Users/el/Projects/nedschorus/CLAUDE.md` and `/Users/el/Projects/nedschorus/AGENTS.md` do not exist (verified — neither file is present in the repo root). This file's explicit path references were read as part of context: `scripts/d-review-codex-cell.py`, `prompts/defect-hunt.md`, `prompts/restate.md`. `docs/issues/`, `docs/cross-project/`, and `d-review-records/` exist as directories (checked only for existence, not read as content, since the file cites them only as path *patterns*, not specific documents).

Findings, in document order:

**1. Frontmatter — "Use when a document is about to be built from or landed, or when the boss says "d-review this" — his ask wins over the exclusions."**
Defect class: (d) wrong/impossible when obeyed literally, in combination with the immediately preceding sentence: "Not for code correctness, and not for reviewing an implementation against its design." Taken literally, "his ask wins over the exclusions" lets the boss direct this skill at code-correctness review or implementation-vs-design review — precisely the two things just excluded. But every subsequent mechanism in the file (Mode 1's eleven lenses, Mode 2's two passes) is built exclusively to judge a written document/design/doctrine file; none defines any procedure for judging code correctness or an implementation against its design. Obeying the override literally routes the reviewer into a task the document supplies no method for.
Confidence: unsure — it's plausible the override was meant only for the archive-exclusion (line 61) rather than the domain-exclusion, but the sentence draws no such distinction and reads as "the exclusions" (plural, unqualified).

**2. Line 8 / Line 60 — "Designing the fix belongs to the author, and a reviewer who wants to propose an alternative design has left review — that is a create-design task, owned separately." / "a code-review skill's lane (the review-change candidate owns it when built), not this one's."**
Defect class: naming (identifiers without a locator). "create-design task" and "review-change candidate" both read as hyphenated, skill-name-shaped labels for sibling work, but unlike the document's other forward-references (e.g., line 33's `nedschorus#42` with a full URL), neither carries a path, issue number, or any pointer to where that work is tracked. A reader cannot tell whether these are real, findable skills-in-progress or just descriptive ad hoc categories invented on the spot.
Confidence: unsure — each phrase is glossed in the same clause it appears in, which may satisfy the "local labels defined where they are used" exemption the document itself states elsewhere (line 41).

**3. Line 14 — "Pick the mode by what the document IS, not by the state of the system around it: a design nothing has been built from yet gets the soundness checklist; a doctrine or instruction file gets the clarity review; a document that is both — doctrine carrying designed mechanisms — gets both, in separate passes."**
Defect class: (a)/(c) self-contradictory / two incompatible readings. The clause disclaims "the state of the system around it" as a factor, then immediately defines the "design" category by "nothing has been built from yet" — a fact about build state, i.e., about the system around the document, not about the document's own content. Reading A: "design" vs. "doctrine" is a genre distinction, and the build-state phrase is incidental description, harmless. Reading B: eligibility for the soundness checklist is conditioned on build state having NOT happened yet — so a design document whose implementation has since been built would seemingly lose its "design" classification, which is exactly the "state of the system around it" the sentence just said not to use. The sentence gives no way to tell which reading governs a design document whose implementation now exists.
Confidence: unsure.

**4. Line 21 — "A live specimen of the cost of skipping this: a review once passed the confidently false sentence "uncommitted work has no copy outside the conversation" — a written-out model of the boundary collides with that class of sentence, since on this project's runtime, files on disk survive session restarts."**
Defect class: (e) unexecutable/unverifiable by a zero-context reader, on two counts within the same sentence. First, "a review once" cites an unnamed, unlocated specific past review as evidentiary support, with no path, date, or record pointer — unlike other citations in this document that do carry one (e.g., `d-review-records/`, `nedschorus#42`, the script path). Second, "since on this project's runtime, files on disk survive session restarts" asserts a fact about "this project's runtime" that neither an absent CLAUDE.md/AGENTS.md nor this file defines, with no path to an authority that would confirm it.
Confidence: unsure — this functions as illustrative background rather than an operative rule the reader must execute, so the practical cost of not being able to verify it may be low; still, it is offered as the reason a whole doctrine (step 2) exists.

**5. Line 21 — "(This step is invoker-side preparation — in mode 2, the delegated restate cells do their paraphrasing separately and innocently.)"**
Defect class: (c) supports two incompatible readings, in tension with line 18 ("Steps 1–4 and 6 apply in both modes; step 5 is discharged by the clarity matrix in mode 2"). Reading A: step 2 (the invoker's private write-out-understanding exercise) still happens in full in mode 2, and the delegated restate cells are a separate, additional activity — consistent with line 18. Reading B: in mode 2 the invoker's step-2 obligation is satisfied by the delegated cells doing the paraphrasing instead, so the invoker need not personally do the write-up — which would contradict line 18's claim that only step 5 (not step 2) is discharged by the mode-2 matrix. The parenthetical's wording does not clearly rule out Reading B.
Confidence: unsure.

**6. Line 22 — "A cell states only its own confidence — sure, or unsure and why."**
Defect class: (e) unexecutable by a zero-context reader / naming inconsistency. This is the first use of "cell" in the document, inside step 3, which line 18 declares applies "in both modes." "Cell" is only ever defined later, and only for Mode 2 (the {pass}×{tier}×{runtime} matrix, line 52: "eight cells"). Mode 1's parallel unit is called a "lens," run by "one focused agent per lens" (line 29) — never "cell." A reader applying step 3 under Mode 1 cannot tell whether "a cell" means their lens-agent, or is Mode-2-only vocabulary that doesn't apply to them at all, since the two terms are never equated.
Confidence: sure the term is used before being defined and is never equated with "lens"; unsure whether they were intended as synonyms.

**7. Line 23 — "When checking would be hard, laborious, or impossible, do not attempt it — mark the claim unverified, and let that label feed lens 7's measured-or-believed judgment; the label is the deliverable."**
Defect class: conditional whose condition is a judgment call rather than an observable predicate. "Hard" and "laborious" (and, to a degree, "impossible") carry no objective threshold — they depend on the specific reviewer's tools, time, and skill. Two reviewers facing the identical claim can reach opposite verify/don't-verify decisions while each faithfully following the instruction as written.
Confidence: sure the condition is not objectively observable; unsure whether the vagueness is a genuine defect or deliberately left to reviewer discretion.

**8. Line 23 — same sentence as #7 — "...let that label feed lens 7's measured-or-believed judgment..."**
Defect class: (d) wrong/impossible when obeyed literally. Step 4 is declared (line 18) to apply "in both modes," but "lens 7" is Mode-1-only numbering (the eleven lenses at lines 31–41); Mode 2 has no lenses at all, only two named passes (restate, defect-hunt). A reader executing step 4 while running Mode 2 has no "lens 7" to feed the label into — the instruction cannot be carried out literally in that mode.
Confidence: sure.

**9. Line 23 — "designs' own existence labels are where they lie to themselves (the standing specimen: a backup script everyone believed ran, whose output directory had zero commits in the repository's history)."**
Defect class: (e) unexplained reference. "The standing specimen" and "everyone believed" cite a specific incident with no path, date, or record pointer, and "the repository" is unnamed — unlike other citations in this document that do carry a locator. A zero-context reader cannot verify or locate this claimed incident.
Confidence: unsure — illustrative rather than operative.

**10. Line 29 — "What scales with revision size is the reviewer count, never the text scope: a light revision may earn a single good-tier pass over the full document plus verification of each prior finding's fix; a heavy revision earns the full grid."**
Defect class: conditional whose condition is a judgment call rather than an observable predicate. "Light" vs. "heavy" revision is never given an objective threshold (lines changed, sections touched, percentage of document affected) — it is left entirely to the invoker's subjective sense of weight, so two invokers facing the identical diff could reasonably pick different review scales (one pass vs. the full eight-cell grid) while each claiming to follow the rule.
Confidence: sure the condition is undefined; unsure whether this is a genuine defect versus intentional discretion (the document elsewhere explicitly flags similar discretion, e.g. "grouping is the invoker's call," but does not do so here).

**11. Line 34 / Line 40 — "...require the document to name or explicitly discard the reachable, consequential cells." / "the best catches live in cells the design's own story never visits." / "A plan that maps the design's own cells is necessary..."**
Defect class: naming — overloaded term. Here "cell" denotes a state-space or test-matrix scenario (a combination of actor-state × dependency-failure × concurrency, or a test-plan case). This is a different sense from "cell" as used in step 3 (line 22), step 4 (line 23), and throughout Mode 2 (lines 48–56), where "cell" denotes one delegated review-agent invocation in the {pass}×{tier}×{runtime} matrix. The same word is used for two unrelated concepts (a scenario/case vs. an agent instance) without the overload ever being flagged.
Confidence: sure both senses are used; unsure whether a real reader would actually be misled given that surrounding context usually disambiguates.

**12. Line 41 (with Line 6 heading and frontmatter `name: d-review`) — "Every in-scope name must be self-documenting and greppable: full words a search matches verbatim, one shared token across a family of related names, no cryptic abbreviations, no bare sequence labels... Expect almost every truly self-documenting name to run two to five words... a one-word name is a finding candidate by default when the word is generic or collides in context."**
Defect class: (b) internal consistency — the document's own persistent identifier, "d-review" (frontmatter `name: d-review`; heading "# Design review (d-review)"), abbreviates "design review" to a single letter plus a word, arguably a "cryptic abbreviation" under the bar lens 11 itself sets, and shorter than the stated "two to five words" expectation, yet the document never states that its own name is exempt from this lens's scope.
Confidence: unsure — the greppability criterion arguably passes (it is the literal skill directory name), and the lens may implicitly apply only to names *within a reviewed design*, not the reviewing skill's own name — but this exemption is never stated.

**13. Line 45 — "Two pass types, and they run in SEPARATE agents — never one agent doing both: a single agent doing one task first is primed for the second..."**
Defect class: (f)/(d) absolute with no stated fallback. Mode 1 explicitly provides a degraded-mode fallback for the analogous constraint: "with no subagent facility, one reviewer working the lenses serially is the accepted degraded mode" (line 29). Mode 2 states its agent-separation rule as an unqualified "never," with no equivalent fallback anywhere in Mode 2's text for an environment lacking a subagent-spawning facility. Taken literally, the rule cannot be obeyed in such an environment, and no alternative path is given.
Confidence: sure no Mode-2 fallback text exists; unsure whether that gap matters (an environment with no subagent facility at all may be considered outside this skill's intended operating envelope).

**14. Line 48 — "A confusion flag is never dismissed as the reviewer's ignorance (boss-ruled 2026-08-04): the reviewer's guaranteed context is the instruction floor plus the document, so a concept that confused them was missing from both..."**
Defect class: (b) conflicts with line 21 (step 2): "A divergence between your written understanding and the document's words is a finding — after ruling out that you simply misread clear text." Step 2 explicitly carves out "you simply misread clear text" as a case that is NOT a finding (i.e., is the reviewer's fault) before a divergence counts. Line 48 asserts flatly, without that carve-out, that a confusion flag is "never" dismissed as the reviewer's ignorance. Reading A: these describe different pipeline stages (step 2's private preparatory paraphrase vs. line 48's defect-hunt-pass confusion) and so don't truly conflict. Reading B: both describe the same underlying judgment — whether an agent's misunderstanding is the document's fault or the agent's — and the two passages give opposite answers about whether "the reader simply misread it" is ever a valid dismissal.
Confidence: unsure — the document never states whether these are the same judgment or different ones, so the literal conflict stands unresolved.

**15. Line 48 — same sentence as #14 — "...the reviewer's guaranteed context is the instruction floor plus the document..."**
Defect class: (e)/naming — "the instruction floor" is a new term (this exact phrase occurs nowhere earlier), introduced right after the sentence describes the zero-context reader's tripartite context ("the checkout's instruction file (CLAUDE.md / AGENTS.md), the document itself, and the files the document explicitly references by path"). "Instruction floor" presumably means "the checkout's instruction file," but this collides with the document's separately, heavily-used term "floor" meaning the low-capability model tier (line 50: "floor = the pinned floor model"; line 52: "Floor is defined by capability — the lowest tier that actually reads the file"). Additionally, "the instruction floor plus the document" names only two components, silently dropping the third ("the files the document explicitly references by path") that the same sentence just listed one clause earlier as part of the reader's "entire context."
Confidence: unsure on both points — the dropped third component may be deliberate (referenced files aren't guaranteed to exist for every document), but the term collision with tier-"floor" is a plain textual fact.

**16. Line 52 — "Floor is defined by capability — the lowest tier that actually reads the file, not the lowest tier that exists (a below-floor model flags mostly its own capability gaps); the framework's subagent default is the current instance."**
Defect class: (e) unexecutable by a zero-context reader. "The framework" is never named or introduced anywhere in the document (no product name, no path). "The current instance" is likewise unresolved — instance of what: the invoking model, the invoking session, something else? Neither referent is resolvable from the sanctioned context.
Confidence: sure neither term is defined in-document; unsure how much this matters practically, since it's a parenthetical aside rather than an operative instruction.

**17. Line 52 — "(The clarity cells run on today's runtimes; a Codex wrapper of this skill — the runtime-parity question — is separate and arrives at companion admission.)"**
Defect class: (e) unexecutable by a zero-context reader. "Companion admission" is unexplained jargon with no antecedent, path, or definition anywhere in the document. A reader cannot determine what process or event this refers to (a review gate, an approval ritual, a scheduled milestone).
Confidence: sure this phrase is undefined in the sanctioned context.

**18. Line 54 — "Measured on a legacy doctrine-file review: seventeen findings under an adversarial-literal prompt against two under a charitably-worded one that silently repaired the defects being hunted."**
Defect class: (e) unexplained reference. "A legacy doctrine-file review" cites a specific, unnamed measurement (17 vs. 2 findings) as the evidentiary basis for the entire "prompt is the lever" doctrine, with no path, date, or record pointer — unlike line 56's parallel measured claim, which does name its source ("first NedsChorus run, 2026-08-03"). A zero-context reader cannot verify or locate this measurement.
Confidence: unsure — illustrative, but offered as load-bearing empirical justification for a firm prompting rule.

**19. Line 54 — "The templates are the most leveraged text in the whole process — criticize them freely, in reviews and out of them; land changes deliberately (the context-holder rules on each, ideally micro-tested), never as silent drift (boss-ruled 2026-08-04)."**
Defect class: naming/terminology consistency. "The context-holder" appears to be a third label for the role already named "the invoker" (step 6, line 25: "The invoker — the one agent holding full context — assigns each finding's severity") and possibly also "the author" (see finding 20). The document never states these terms are synonyms.
Confidence: unsure whether "context-holder" is a plain descriptive phrase or an alternate name for the same defined role as "invoker."

**20. Line 56 — "Then the author reads that one file with full context ... assigns all severity at step 6, and plans the second pass." / "The author compares restatements against intent — no one else holds the intended meaning..." / "the author, who holds the intent, takes the notes and rewrites — that is what review notes are for."**
Defect class: (b)/(c) conflicts with the document's own earlier, distinct use of "author." At line 8, "the author" is unambiguously the original document's owner/writer, a role explicitly walled off from the review process ("Designing the fix belongs to the author, and a reviewer who wants to propose an alternative design has left review — that is a create-design task, owned separately"). At line 24, the document's stated reason for delegating to independent fresh agents is precisely that "an author reviewing their own text has the worst of it — the weak parts are already rationalized." But at line 56, "the author" is the one who reads the full merged cell-and-restatement file, personally assigns all severities, and "rewrites" the document — the role elsewhere called "the invoker" (step 6, line 25). Read literally with "author" meaning what it means at line 8 (the document's original owner), line 56 has that author doing hands-on severity triage of findings about their own text and then rewriting it — directly contradicting line 24's rationale for using independent fresh agents in the first place, and contradicting step 6's assignment of severity-setting to "the invoker," not "the author." Read with "author" as simply a second name for "the invoker" in this section, the passage is internally consistent with steps 5/6, but the document never states this equivalence.
Confidence: sure the word "author" is used with apparently inconsistent referents across the document and the two usages are never reconciled; unsure whether every reader would resolve it the harmless way, since line 24's explicit warning against self-review makes the literal "document-owner" reading a real hazard, not a purely theoretical one.

clean sections: none — findings were located in the frontmatter, the introductory paragraph, "Input and mode choice," "Steps," "Mode 1," "Mode 2," and "When NOT to use."
