<!-- provenance: runtime=claude model=claude-sonnet-5 effort=high cell=defect-hunt tier=floor target=/Users/el/Projects/nedschorus/.claude/worktrees/linters-tool-usage-f8b0a0/.claude/skills/ghi-write/SKILL.md -->

I've read the file in full (36 lines) and cross-checked its claims against the checkout's actual state: `CLAUDE.md`, `.claude/settings.json`, `.claude/skills/`, `scripts/`, and the referenced directories.

## Findings

**1. Frontmatter trigger scope vs. "When Used" trigger scope (lines 3, 10) — supports incompatible readings**
Frontmatter: *"Creating or revising any project artifact that might belong in an issue also triggers it, because routing is part of the skill."*
"When Used": *"...or writing project material whose home (issue, queue, or MD) is not yet decided."*
The frontmatter trigger is broader ("any project artifact that might belong in an issue," with no requirement that the home be undecided) than the "When Used" trigger (only material whose home is "not yet decided"). Revising an artifact whose home is already settled but which someone judges "might belong in an issue" would trigger the skill under the frontmatter wording but not under "When Used." A future agent following only one of the two sections would reach different invocation decisions on the same input. Confidence: sure the two wordings diverge; unsure which is meant to be authoritative.

**2. "Put the subject to ghi-info" — undefined term (line 14)**
*"Put the subject to ghi-info by running scripts/ghi-info-ask.py with the question..."*
"ghi-info" is never defined or expanded anywhere in this file. It's only inferable — not stated — that it names some system/agent the script queries. A zero-context reader has no explicit statement of what "ghi-info" is (a person, a bot, a subsystem, a doc corpus). Confidence: sure this term is used without definition; unsure whether the intended reader is expected to already know it from outside this file.

**3. `scripts/ghi-info-ask.py` does not exist, and the fallback ladder is described inconsistently across two sections (lines 14, 29)**
Line 14: *"A failed ask never blocks the write: fall back to grepping the local mirror, then `gh` search, and proceed under these rules."* (two fallback rungs: mirror grep, then gh search)
Line 29: *"...fall back in order: grep `ghi-mirror/` in the checkout (stale unless freshly regenerated), then `gh issue list --repo nedschorus/nedschorus --state all --search "<terms>"`, and grep the repository for pair documents on the subject."* (three fallback rungs: mirror grep, gh search, AND grep repo for pair documents)
I confirmed `scripts/ghi-info-ask.py` does not exist in `scripts/` and `ghi-mirror/` does not exist anywhere in the repo, so the primary "Ask" instruction is currently un-executable and the agent must fall through to a fallback described two different ways: line 14 names two rungs, line 29 names three. It's not stated whether "grep the repository for pair documents" (line 29's third rung) is part of "proceed under these rules" (line 14) or an additional rung line 14 omits. Also unstated: what to do when the mirror directory doesn't exist at all (vs. exists but returns no matches) — a `grep` on a missing directory errors rather than "coming up empty," and the file never says to treat "directory absent" the same as "search came up empty." Confidence: sure of the rung-count mismatch and the missing-directory gap; sure the script and mirror are absent from the checkout as of this review.

**4. "the same way md-write defaults to REVISE" — reference to a nonexistent mechanism (line 14)**
I searched the entire repository (all worktrees) for any skill, script, or file named `md-write`; none exists — only `md-review` exists as an actual skill in `.claude/skills/`. The comparison point this sentence relies on to explain "default disposition" is not resolvable by a reader confined to this file, `CLAUDE.md`, and explicit references, and grepping for the name turns up nothing to find. Confidence: sure `md-write` does not exist anywhere in the repo; unsure whether this is a typo for `md-review` or a reference to a skill that was never built.

**5. "Every artifact is either final at its home or in a named queue with a drain" — contradicted by the very next bullets (line 15)**
This topic sentence asserts an exhaustive two-way split. But the bullets immediately under it describe three outcomes: queue (bullet 1), GHI/issue for pending state (bullet 2), and final bare MD (bullet 3) — and the summary line even says so explicitly: *"the GHI carries state, the MD carries substance, the queue holds the not-yet-decided"* (line 19), three categories, not two. An open issue carrying pending state is neither "final at its home" (it's explicitly not final — it carries pending state) nor "in a named queue with a drain" (queues are the separate, distinct disposition in bullet 1). Confidence: sure — the topic sentence's binary framing is directly contradicted by the three-way breakdown that follows it in the same step.

**6. "goes to its destination queue ... with no GHI" contradicted by "the draft label for queued issues" in the same bullet (line 16)**
*"Material whose disposition is not yet decided goes to its destination queue — `docs/wiki/queue/` for wiki-bound doctrine, `docs/issues/queue/` for pair-bound MDs, the `draft` label for queued issues — with no GHI."*
One of the three listed queue destinations is "the `draft` label for queued issues" — but a `draft`-labeled item is necessarily an issue (a GHI) that exists on GitHub with that label attached. This directly contradicts the clause "with no GHI" that governs all three destinations in the same sentence. Confidence: sure the two clauses conflict as written; unsure whether the intended meaning is "no *non-draft* GHI" or something else.

**7. "GHI" is never defined or expanded anywhere in the file (first use: line 16)**
The term recurs throughout ("gets a GHI," "no GHI," "MD-GHI pair," "the GHI carries state") and is central to the whole routing scheme, but this file never states what the acronym stands for. It's inferable from the `gh issue` commands later in the file that it likely means "GitHub Issue," but that's an inference, not a statement — and the skill's own name "ghi-write" gives no more than the same unexpanded acronym. Confidence: unsure whether the intended reader is assumed to already know this term from outside this file, but sure it is used here without ever being spelled out.

**8. "issue-only when the body stays under 500 words" vs. "an MD-GHI pair when substantial working material rides with it" — two non-aligned tests for the same decision (line 17)**
These are presented as if a single condition ("when...") governs the issue-only vs. MD-GHI-pair choice, but they're actually two different, independently-triggerable conditions: a word count threshold and a qualitative judgment about "substantial working material." They aren't stated to be equivalent, and the file gives no resolution for the cases where they disagree — e.g., a verbose-but-insubstantial body that exceeds 500 words, or a body under 500 words that nonetheless links out to substantial working material. Confidence: unsure how often this edge case actually arises in practice, but sure the two criteria as worded can diverge and the file doesn't say which one governs when they do.

**9. "an instance outcome" — undefined and seemingly in tension with the completion rule (line 20)**
*"A comment is only for a genuinely new event — an instance outcome, or a challenge to a ruling. Completion is not a comment: record the outcome in the body, then close the issue with its reason."*
"Instance" has no antecedent anywhere in this file — nothing establishes that an issue can represent something with multiple discrete "instances" whose individual outcomes would be comment-worthy. This matters because the sentence right after it draws a sharp line for a structurally similar-sounding case ("completion... outcome") and sends it the opposite direction (body edit, not comment). Without a definition of what an "instance" is, a reader can't reliably tell whether a given event is an "instance outcome" (comment) or effectively a "completion" (body + close), since both are described using the word "outcome." Confidence: unsure whether "instance" refers to a concept defined in some out-of-file convention; sure it is undefined within this file and that the two rules it sits beside apply "outcome" oppositely.

**10. Step 4's third test is impossible for the "final reference content" category defined in step 2, and narrows CLAUDE.md's own zero-context definition (lines 17-18, 21; cf. CLAUDE.md)**
Step 4: *"...the next action is executable by a reader who was not in this conversation."*
Step 2 (lines 17-18) defines a category of artifact explicitly as: *"Final reference content awaiting nothing is a bare MD at its home."* By definition, an artifact "awaiting nothing" has no next action, so step 4's third test cannot be satisfied for it if step 4 is read as applying to every write this skill governs. Separately, `CLAUDE.md` states its own zero-context bar as: *"the subject identifiable, the why stated, usable without the conversation that produced it"* — a general usability criterion. Step 4 restates the first two elements near-verbatim but replaces the third with the narrower, action-specific "next action is executable," which is a different (and here, for the "awaiting nothing" case, unsatisfiable) requirement than CLAUDE.md's own broader "usable without the conversation." Confidence: sure of the internal conflict with step 2's "awaiting nothing" category; sure the wording differs from CLAUDE.md's; unsure whether step 4 was meant to apply only to issues that do carry a next action (in which case the scope should say so).

**11. `scripts/ghi-issue-write.py` does not exist, and — unlike the Ask step — no fallback is given for Comment (line 33)**
*"Comment (the two catalog events only): resubmit through the write tool `scripts/ghi-issue-write.py`'s comment verb naming the event kind; plain `gh issue comment` is denied by the write path."*
I confirmed this script does not exist in `scripts/`. Step 1 (line 14) explicitly provides a fallback ladder for when the Ask tool (`ghi-info-ask.py`) fails or is unavailable, but no equivalent fallback is given here for when the write tool (`ghi-issue-write.py`) is unavailable — and this same sentence states that the obvious fallback, plain `gh issue comment`, is "denied." Taken literally and as of the current checkout, there is no executable path to file either of the two catalog-event comments this skill defines. Confidence: sure the script is absent from the checkout; sure no fallback is stated for this specific case.

**12. "denied by the write path" — unstated and unverified enforcement mechanism (line 33)**
The file asserts plain `gh issue comment` "is denied by the write path" as a fact about system behavior, but never explains what "the write path" is (a hook, a wrapper, a policy convention to be followed voluntarily) or how/where the denial is enforced. I checked `.claude/settings.json` for a hook that would intercept Bash/gh commands — the only relevant hooks configured are a `Stop` hook and a `PreToolUse` hook matching `Edit|Write|NotebookEdit`, neither of which would intercept a `gh issue comment` shell invocation. So as of this checkout, no enforcement mechanism matching "denied by the write path" is discoverable from the referenced context. Confidence: sure no such mechanism is visible in `.claude/settings.json`; unsure whether enforcement is intended to live elsewhere (e.g., inside the not-yet-built `ghi-issue-write.py` itself, making the denial self-referential and currently non-existent) or is meant as a human/agent-obeyed convention only.

clean sections: "What to do" step 5 (lines 22-25, "Make every reference openable and every claim checked"); "How to do it" bullets for File, Edit, Close, Compose bodies in a file, and Queue routing (lines 30-32, 34-35).

