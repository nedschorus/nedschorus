<!-- provenance: runtime=codex model=gpt-5.6-terra effort=xhigh cell=restate tier=floor target=/Users/el/Projects/nedschorus/docs/drafts/claude-md-admitted.md -->

nedschorus

1. This repository’s location is `~/Projects/nedschorus`, and its short identifier is “NC.”

2. `~/Projects/nedlern` is an older system that may be consulted only as a read-only reference: its contents may be read without restriction, but nothing there may be written, committed to version control, or executed.

3. Use the ordinary terminology associated with the software development life cycle, rather than inventing nonstandard terms for familiar development concepts.

4. Create persistent documents or other outputs that someone with no knowledge of this conversation can understand and use: they must make their topic identifiable, explain why the artifact exists or matters, and provide enough concrete direction to act on it independently.

5. Categorical commands using words such as “always” or “never” may produce harmful results in circumstances that were not anticipated, so use such absolute wording sparingly and carefully.

6. Whenever a new name is being created—for example, for a directory, filename, global variable, function, or similar item—make it descriptive, unambiguous, precise, and composed of multiple parts.

7. For each newly invented name, check for conflicts or confusion by using `glob` when it is a pathname and `grep` when it is a name appearing in files.

8. If those checks reveal an existing collision or any ambiguity, replace the proposed name with a more descriptive one containing three or four parts rather than only one or two.

9. If the item already has an established name somewhere in this project, use that established name rather than making up another one.
