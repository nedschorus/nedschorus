<!-- provenance: runtime=claude model=claude-sonnet-5 effort=high cell=defect-hunt tier=floor target=/Users/el/Projects/nedschorus/docs/drafts/claude-md-admitted.md -->

I read the full file (no YAML frontmatter present — it opens directly with `# nedschorus`) and checked for a CLAUDE.md/AGENTS.md in this checkout; none exists, so criterion (h) has no target to compare against. I also confirmed `~/Projects/nedlern`, the one explicit path this file references, exists on disk and is readable by the current user.

## Findings

1. **Line 3** — `This repository is \`~/Projects/nedschorus\` (NC).`
   The parenthetical introduces "NC" as an alias for the repository but the alias is never used again anywhere in this document. Nothing states what it's for (a shorthand for other docs to use? a tag for search?) or where else it applies. A future agent with only this file has no way to tell whether "NC" is meant to be load-bearing terminology it should adopt/recognize elsewhere, or just incidental prose. Separately, "NC" is a 2-character, single-part name, which sits in tension with line 7's rule that invented names be "explicit, clear and precise multi-part names" (see finding 8). I'm unsure whether this rises to a real defect, since the alias is arguably self-explanatory in context (`X (NC)` conventionally reads as "call X 'NC'"), but its total lack of reuse or scope makes its purpose unclear.

2. **Line 3** — `read anything there freely; NOT: write, commit, or run anything there.`
   This supports two incompatible readings of "run." Reading A: "run" means executing programs/scripts that live inside nedlern (e.g., don't invoke its build or its scripts) — compatible with "read anything there freely." Reading B: "run" means don't execute *any* command with nedlern as a target, including read-only shell operations like `ls` or `cat` used to do the reading. Under reading B, "read anything there freely" is impossible to satisfy without violating "NOT: ... run anything there," since listing or catting files is itself "running" something. The sentence doesn't disambiguate which sense of "run" is meant. I'm unsure how much this matters in practice (a reasonable agent will likely infer reading A), but the text as written literally supports both.

3. **Line 3** — `The legacy system at \`~/Projects/nedlern\` is read-only reference`
   This defines nedlern as a reference store but doesn't address the reachable case where that path doesn't exist or isn't readable for a given agent/environment (different machine, different user, relocated checkout). It exists and is readable in the current environment, but the file states the location as unconditional fact with no fallback or acknowledgment that this could fail. I'm unsure whether this needs handling here since it held true when I checked, but the case is reachable and undiscarded.

4. **Line 4** — `Use standard SDLC terms.`
   "Standard SDLC terms" is not defined anywhere in this file — no glossary, no examples, no reference to an external source that supplies the term list. "SDLC" itself is not spelled out. An agent with zero context beyond this file cannot execute this instruction: it has no way to determine which specific terms count as "standard" (e.g., is "backlog" standard but "todo list" not? Is "PR" standard, or must it be "pull request"?). I'm sure this term is undefined within the document itself; I'm unsure whether an author intended a specific external reference that just isn't cited here.

5. **Line 6** — `Absolute imperatives like 'always' or 'never' can backfire in unforeseen conditions. Use them cautiously.`
   It's unclear whether this rule is reflexive — applying to this very document's own rules — or is scoped only to content the agent authors elsewhere (code, docs, artifacts it writes). Read reflexively, it's in tension with line 3's own unhedged, categorical prohibition ("NOT: write, commit, or run anything there"), which uses an absolute construction without any cautionary hedge. Read non-reflexively, there's no tension. The sentence doesn't state its own scope. I'm unsure how significant this is, but the two readings genuinely diverge on whether line 3's phrasing is itself an example of the very thing line 6 warns against.

6. **Line 7** — `use explicit, clear and precise multi-part names` vs. `choose a more explicit name, with 3 or 4 parts, not 1 or 2`
   These two clauses give different numeric thresholds without stating whether they're the same requirement or a tiered escalation. The first sentence states an unconditional baseline: all invented names must be "multi-part" (which on its own would include 2-part names). The third sentence, triggered only "if these checks return collisions or ambiguity," requires 3-4 parts and explicitly excludes 2 ("not 1 or 2"). Taken together, it's ambiguous whether a 2-part name is ever acceptable (satisfying the baseline "multi-part" rule) or is always disallowed (per the "not 1 or 2" clause) — the text doesn't say the second clause only applies in the collision case, or that the two clauses set different thresholds. I'm unsure whether this is a genuine contradiction or a legitimate tiered rule (baseline ≥2, escalate to 3-4 on collision), because both readings fit the literal text.

7. **Line 7** — `choose a more explicit name, with 3 or 4 parts, not 1 or 2`
   "Parts" is not defined. It's not stated whether a "part" is a word segment separated by a hyphen/underscore, a camelCase token, or something else — e.g., is `nedschorus-open-threads` one part (a single compound identifier) or three? Without a definition, two agents applying this rule to the same candidate name could disagree on whether it already satisfies "3 or 4 parts." I'm sure the term is undefined here; unsure whether it's ambiguous enough in practice to cause disagreement, since common convention (hyphen/underscore/camelCase-delimited segments) is a plausible default reading.

8. **Line 7** — `If these checks return collisions or ambiguity, choose a more explicit name, with 3 or 4 parts, not 1 or 2.`
   This defines a name-checking mechanism (glob/grep, then escalate on collision) but doesn't state what to do if the escalated 3-4 part name *also* collides or is *also* ambiguous — a reachable case, since a more verbose name is not guaranteed to be unique. No further escalation, retry limit, or fallback is given, so the procedure has no stated stopping point for this case. I'm sure this case is reachable and unaddressed; unsure whether it needs one, since repeated collisions on a 3-4 part name are presumably rare in practice.

clean sections: Line 1 (title), Line 5 (durable-artifacts guidance)

