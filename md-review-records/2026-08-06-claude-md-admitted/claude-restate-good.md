<!-- provenance: runtime=claude model=claude-opus-5 effort=high cell=restate tier=good target=/Users/el/Projects/nedschorus/docs/drafts/claude-md-admitted.md -->

**Frontmatter:** none — the file has no YAML frontmatter; it begins with the H1.

## nedschorus (the whole document; single H1 section, no subsections)

1. The repository that this document governs — the one being worked in — lives on disk at the path `~/Projects/nedschorus`, i.e. a directory named `nedschorus` inside the `Projects` folder of the current user's home directory. The parenthetical `(NC)` I read as introducing a two-letter abbreviation for that repository, standing for "nedschorus" (or for a longer name it abbreviates), so that "NC" can be used elsewhere as shorthand for this repo; it is not, on my reading, a second path or a separate thing.

2. There is an older, predecessor codebase — "the legacy system" — located at the path `~/Projects/nedlern`, and its status is that of a reference you may consult but must not alter. The colon introduces the two halves of what "read-only reference" means here. The permissive half: you may open and read any file in that tree without asking or restricting yourself — "freely" meaning both "without needing permission" and "any file, not a limited subset." The prohibitive half, marked emphatically with "NOT:", forbids three things there: modifying or creating files (write), making git commits in that repository (commit), and executing anything (run). I read "run anything there" as: do not execute programs, scripts, tests, or build commands that live in or operate on that directory — an alternative narrower reading would be only "do not launch the legacy application," but I take the broader one given "anything." "NOT" in capitals I read as emphasis, marking these as hard prohibitions rather than preferences.

3. When you write or speak about software work in this project, use the ordinary, widely recognized vocabulary of the software development life cycle — words like requirement, specification, design, implementation, test, review, release, bug, refactor, deploy — rather than coining new terms or using idiosyncratic in-house jargon for concepts that already have standard names. "Standard" I read as industry-standard/conventional across the software profession, not "standardized within this project." I read the scope as all writing and communication in the project, not only the naming of code entities, though the sentence is terse enough that a narrower reading (use standard terms when labeling things) is possible.

4. When you produce an artifact meant to persist — a document, a design, a skill file, a commit message, a note that will outlive the current conversation — write it so that someone who arrives with no knowledge of the conversation, the session, or the surrounding history can use it. The colon then names three specific properties that "for a reader with zero context" cashes out to: (a) the subject is identifiable — from the artifact alone the reader can tell what thing it is about; (b) the why is stated — the reason the artifact exists, or the reason for the decision it records, is written down explicitly rather than assumed; (c) it is actionable without the conversation that produced it — the reader can take the next step, or do what the artifact asks, without needing access to the chat transcript in which it was written.

5. Instructions phrased in unconditional terms — rules containing words like "always" or "never," which admit no exceptions — carry a risk: in circumstances that whoever wrote the rule did not anticipate, following the rule can produce a bad result, and specifically ("backfire") a result contrary to what the rule was meant to achieve.

6. Therefore, do not stop using such absolute words altogether, but use them with care: reach for "always"/"never" only after considering whether there are situations where obeying them literally would be wrong, and prefer weaker or conditioned phrasing when such situations exist. "Them" refers back to the absolute imperatives of the previous sentence.

7. Whenever you are the one coining a new name for something — a directory, a file, a global variable, a function, and by "etc." any other nameable entity such as a class, constant, module, or skill — the name should be made of several parts (several words or components joined together), and those parts should be chosen so the name says plainly, unmistakably, and specifically what the thing is. "Explicit" I read as spelling things out rather than abbreviating or implying; "clear" as readily understood; "precise" as pinning down exactly this thing rather than a general category.

8. Having invented a candidate name, verify it by searching the project before adopting it: use glob (filename-pattern matching, e.g. the Glob tool or shell globbing) when the name is a path — a directory or file name — and use grep (text search through file contents) when the name is an identifier that would appear inside files, such as a function or variable name.

9. If that search comes back showing either a collision — the name is already in use for something else — or ambiguity — the results make it unclear which thing the name would refer to, or the name could plausibly denote more than one thing — then do not keep the candidate; replace it with a name that spells out more, specifically one built from three or four parts rather than one or two. I read "3 or 4 parts, not 1 or 2" as a concrete target for how many components the replacement name should have, implying the colliding candidate was probably a short one- or two-part name.

10. Before inventing at all, consider whether the thing you are about to name is already named somewhere in this project; if it is, adopt that existing name rather than creating a new one for the same thing — i.e. do not introduce a synonym for a concept, file, or entity that the project already has a word for.

