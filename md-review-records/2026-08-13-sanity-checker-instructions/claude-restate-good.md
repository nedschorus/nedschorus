<!-- provenance: runtime=claude model=claude-opus-5 effort=high cell=restate tier=good target=/home/nedlern/Projects/nedschorus/.claude/worktrees/gatekeeper-walk-fork-continuation/docs/agents/sanity-checker-instructions.md -->

**Frontmatter:** the file has none — it opens directly with the `#` heading, so there are no frontmatter fields to restate.

## Heading — `` `sanity-checker` — seat instructions ``

1. This document is the set of instructions for a role (a "seat") whose identifier is the literal string `sanity-checker`; the backticks around the name suggest it is a name used verbatim elsewhere in the system (a directory, an agent type, or a launcher argument), and "seat instructions" means the standing brief handed to whichever agent session occupies that role, rather than a description of a person or a one-off task.

## Opening paragraph (no section name, sits under the title)

1. "Your pile: **how this project reviews things, and how well that works.**" — The subject matter assigned to you (the reader occupying this seat) is the project's review practices: both the mechanics of how reviews are conducted here, and the question of whether those mechanics actually produce good results. "Pile" I take as a coined term of art in this project meaning the bounded body of work and responsibility belonging to one seat — roughly "your docket" or "your area of ownership" — not a physical stack or a backlog list; I read it as ownership of a subject area rather than of a specific queue of tasks, though the word alone leaves both readings open.
2. "The sanity-checker reviewer, the md-review grid it may join, and the skill rules that govern how reviews are delivered." — Spelling out three concrete things that fall inside that pile: (a) the reviewer entity itself, i.e. the sanity-checker as a review agent or review prompt that examines material; (b) something called the "md-review grid", which the sanity-checker is a candidate to become part of but is not yet part of — "grid" I read as a matrix of review cells, i.e. combinations of review-stance × model-runtime, each cell being one review run; (c) the rules encoded in a "skill" (in this harness, a packaged instruction set invoked by name) that determine the manner in which review output is handed to its recipient — for instance in what form, in what order, and at what pace.
3. "Read [the seat model](agent-seat-model.md) for how seats work; this file is your brief." — Instruction to consult a separate document, `agent-seat-model.md`, which explains the general concept and mechanics of seats in this project (what a seat is, how one is occupied, how they relate to each other); by contrast, the file you are reading now is the specific assignment for this one seat — "brief" in the sense of the instructions and background given to whoever takes the job, not in the sense of "short".

## The decision waiting on the user

1. "**Does the sanity-checker join the md-review grid as three stance attacks** — cut, mechanization, fresh-eyes — run on both Fable and gpt-5.6-sol at xhigh?" — The open question, phrased as a yes/no to be put to the user, is whether the sanity-checker should be added into the md-review grid in the specific shape of three separate review passes, each taking a different adversarial posture ("stance attack" = one review run conducted from one deliberately narrow critical angle): one whose posture is to find things to cut, one whose posture is to find things that could be made mechanical (automated/deterministic rather than left to an agent's judgment), and one whose posture is to read the material as though seeing it for the first time. Each of those three would be executed twice — once on the model named Fable and once on the model named gpt-5.6-sol — with the reasoning-effort setting "xhigh" in both cases, yielding six runs in total. I read "join the grid" as "become standing cells in the grid that run as part of every md-review", not "be run once as an experiment".
2. "This is the decision the whole attack-split experiment was built to answer, and it has been waiting since 2026-08-13." — The prior experiment referred to as the "attack-split experiment" (splitting one combined review into several single-stance attacks) existed for the purpose of producing evidence for exactly this yes/no question; the question has been outstanding, awaiting the user's answer, since 13 August 2026. Given the stated current date is also 2026-08-13, "waiting since 2026-08-13" means it has been pending since earlier the same day, i.e. it is fresh rather than stale — though the phrasing "has been waiting since" would more usually imply a longer interval, so I flag that I cannot tell whether the date is meant as "as of today" or was written expecting the reader to arrive later.
3. "The evidence is `md-review-records/2026-08-12-attack-split-experiment/scorecard.md` (also in PR #53): the split beat the unsplit baseline — gatekeeper 4 of 5 in-band accepted cuts against the baseline's best 3 of 6, fast-handoff 7 of 7 parity plus novel findings, zero unflagged false positives across eight judgment cells, and each runtime surfacing cuts the other missed." — The data supporting an answer lives in the named file, and the same content also appears in pull request number 53. The summary of that data: the split-into-stances approach outperformed the previous approach in which one review did everything at once. Broken out: on the document about the gatekeeper, the split approach produced 5 proposed cuts that were within the agreed scope ("in-band"), of which 4 were accepted, whereas the best single baseline run produced 6 in-band proposals of which only 3 were accepted; on the document about fast handoff, the split approach matched the baseline exactly (7 accepted out of 7, equal to the baseline's result) and additionally produced findings the baseline had not produced at all; across the eight cells in which a human or judge rendered a verdict, there were no false positives that had gone un-marked — i.e. every wrong proposal that appeared had been flagged as uncertain by the reviewer rather than asserted confidently, or, on an alternative reading, there were simply zero false positives that escaped flagging; and the two model runtimes each found cuts that the other runtime failed to find, meaning they are complementary rather than redundant. I am not fully certain of the "4 of 5 … against the baseline's best 3 of 6" comparison: it reads as a ratio comparison (80% versus 50% acceptance), and "the baseline's best" implies the baseline was run more than once and only its strongest run is being cited.
4. "The already-walked operating rulings — scope, trigger, order, models, piecemeal delivery, triage ownership — are in the header of `docs/drafts/sanity-checker-prompt-draft.md`." — A set of operational decisions has already been presented to the user item by item and settled with him ("walked" = taken through the walk-me-through process, one item at a time, with his ruling on each), and those settled decisions are recorded at the top of the named draft file. The six settled matters are: what the sanity-checker's remit covers (scope); what causes it to run (trigger); the sequence in which its passes or outputs occur (order); which model runtimes it uses (models); the rule that its output is handed over in installments rather than all at once (piecemeal delivery); and who is responsible for sorting the resulting findings into keep/discard/route buckets (triage ownership).
5. "**A skill change is walked with the user before it lands**, so this is a walk, not an application." — There is a standing rule in this project that any modification to a skill must be presented to the user item by item and approved before it is committed/merged ("lands"); therefore the action you take on this decision is to conduct that item-by-item presentation, and specifically not to go ahead and implement the change yourself.

## The un-triaged novel findings

1. "The experiment surfaced four findings beyond both ground-truth sets." — The attack-split experiment produced four observations that appear in neither of the two reference lists of known-correct findings that were prepared in advance to score the runs against; "ground-truth sets" = the pre-agreed answer keys, one per reviewed document, and "beyond" = these four were not in either key, so they were new discoveries rather than hits or misses against the key.
2. "Never presented, never triaged, and each read an *archived* snapshot — so **verify every quoted ground against current code before proposing anything**:" — These four findings have not been shown to the user at any point, and no one has yet decided which of them are real, which matter, and where they should go. Furthermore, each finding was generated by a reviewer reading a stored, point-in-time copy of the material rather than the live current files; consequently, before you put any of these findings forward as a proposal, you must go and check that the factual basis each finding cites ("every quoted ground" = each claim about what the code says or does that the finding rests on) is still true of the code as it exists now — because the archived snapshot may be out of date.
3. "The gatekeeper spec's 'when a test suite exists, the tests run here' never fired, though the suite now exists — so the gate runs no checks today." — Finding one: the design specification for the git-gatekeeper contains a conditional provision to the effect that once a test suite exists in the project, the gatekeeper will execute those tests as part of its checks; that provision has never actually taken effect in the running system, even though the precondition (the existence of a test suite) has since been satisfied; the consequence is that in its current state the gatekeeper performs no verification at all when a change passes through it.
4. "No gate-edits-the-gate guard." — Finding two: there is no protective mechanism preventing the gatekeeper from being used to approve and merge changes to the gatekeeper's own code or configuration — i.e. nothing stops a change that modifies the gate from being waved through by that same gate, which would let the gate be weakened or disabled by a change it itself admits.
5. "A writer-stamps-the-pin proposal, to stop agents hand-writing 40-character SHAs." — Finding three is not a defect report but a suggested change: that whatever component writes a record should itself insert ("stamp") the version pin — the exact commit identifier being pinned to — so that agents no longer have to type out full 40-hexadecimal-character git commit hashes by hand, which is an error-prone thing for an agent to do from memory.
6. "The wedged-but-light session: stalls below the recycle threshold with no watchdog." — Finding four: there is a failure mode in which an agent session becomes stuck ("wedged") while having consumed only a small amount of context or resources ("light"); because the automatic recycling/handoff mechanism only triggers once a session passes some usage threshold, a stuck session that sits under that threshold is never recycled, and there is no separate monitoring process ("watchdog") that notices a session has stopped making progress; so such a session hangs indefinitely and unnoticed.
7. "Findings 1 and 2 are gatekeeper territory and 4 is fleet territory — triage them here, then hand the survivors to those seats rather than implementing them yourself." — Findings one and two fall within the subject area owned by the seat responsible for the git-gatekeeper, and finding four falls within the subject area owned by the seat named `fleet` (which handles the running of agent sessions); the division of labour is that you do the triage — decide which of these findings are real, still true, and worth acting on — from this seat, and then pass whichever ones survive that filter over to the owning seats to act on, and you do not write the fixes yourself. Note that this sentence assigns owners for findings 1, 2, and 4 but says nothing about finding 3; I take that as simply unaddressed here rather than as an implicit assignment to you, but the text does not say.

## Background you will need

1. "`docs/drafts/sanity-checker-prompt-draft.md` — the settled prompt, walked from scratch after a first draft was rejected for trying to redefine the everyday meaning of 'simple'." — This file holds the version of the sanity-checker's prompt that has been agreed with the user and is no longer in dispute (despite living in a `drafts/` directory); it reached that state by being taken through the item-by-item walk process beginning from nothing, which was necessary because an earlier attempt at the prompt was turned down — the reason for the rejection being that that earlier draft attempted to assign the word "simple" a special, stipulated meaning different from how the word is ordinarily used, which the user would not accept.
2. "`md-review-records/2026-08-09-git-gatekeeper-design/subtract-cell-prompt-lessons.md` — the requirements record: what the flawed first subtraction review taught, including the user's verbatim axis statement." — This file is the document that captures requirements; specifically it records the lessons learned from the first attempt at a "subtraction" review (a review whose job is to propose removing material) — an attempt that was defective in some way — and among its contents is the user's own exact words, quoted rather than paraphrased, stating the "axis": the criterion or direction along which simplification is to be judged. "Subtract cell" I read as the particular grid cell whose stance is subtraction/cutting.
3. "Its calibration protocol is the live gate before any grid seat." — That same file contains a defined procedure for calibrating a reviewer (checking that it judges things the way the project wants before it is trusted with real work), and that procedure is currently in force as a mandatory precondition: no reviewer configuration may be installed as a cell in the review grid until it has passed that calibration. "Live" here means currently active/enforced rather than superseded; "gate" here means a checkpoint that must be passed, distinct from the git-gatekeeper program mentioned elsewhere.
4. "`md-review-records/2026-08-11-sanity-checker-prompt-draft/` — the md-review of that draft." — This directory holds the records of the md-review process as applied to the sanity-checker prompt draft mentioned in item 1 — i.e. the review of the review-prompt itself, conducted on 11 August 2026.
5. "The md-review machinery itself: `.claude/skills/md-review/`, `scripts/md-review-grid.py`, and the per-runtime cells." — A further item of background is the implementation of the md-review system as a working system: the skill directory containing its instructions, the Python script that drives the grid of review cells, and the individual cell definitions or cell outputs that are specific to each model runtime. I read "per-runtime cells" as the separate review cells that exist one per model runtime (e.g. one for Claude, one for Codex), though it could equally denote the record files those cells produce.
6. "Note the Codex floor tier moved terra → luna on 2026-08-13 (in PR #57)." — A change you should be aware of: for the Codex runtime there is a notion of a "floor tier" — the lowest-capability or minimum setting used in the grid, apparently in contrast to some higher "good" tier — and the value of that setting was changed from `terra` to `luna` on 13 August 2026; that change was made in pull request number 57. I take `terra` and `luna` to be named capability/effort tiers of the Codex runtime, but their meaning is not explained here.
7. "**Open PRs yours to shepherd:** [#51](...) (walk choice items are proposals; md-review delivers piecemeal under a Monitor) and [#53](...) (the attack-split experiment)." — Two pull requests are currently open and it is your responsibility to see them through to conclusion ("shepherd" = keep them moving, respond to review, get them merged or closed, rather than merely being aware of them). PR #51 contains two changes: a rule that the items presented during a walk are to be framed as proposals rather than as decisions already made, and a rule that md-review hands its output over in installments, with a Monitor (a background watching mechanism in this harness) supervising that delivery. PR #53 contains the attack-split experiment material.
8. "[#52](...) — fast-handoff findings applied — is adjacent; coordinate with `fleet` if it needs work." — Pull request 52, whose content is the application of the findings that came out of reviewing the fast-handoff document, is not yours but sits next to your work and touches it; if that PR turns out to require further effort, you should not simply act unilaterally but should work jointly with the seat named `fleet`, which is the seat with a claim on it.

## An unowned thread worth claiming

1. "Session `29d66917` (3.67 MB, last active 2026-08-13) drafted a **code-review prompt for reliability improvement** in `~/agents/choirmaster`." — A past agent session identified by the hexadecimal prefix `29d66917`, whose transcript is 3.67 megabytes in size and which last did anything on 13 August 2026, produced a draft of a prompt for conducting code review, aimed specifically at improving reliability; that work was done in the working directory `~/agents/choirmaster`. Ambiguity: "for reliability improvement" could mean the prompt's purpose is to find reliability problems in reviewed code, or that the prompt is an attempt to make the code-review process itself more reliable — I lean to the former but the phrase supports both.
2. "No live session, mentioned in no handoff, and plainly related to your pile." — That work is currently orphaned: the session that produced it is no longer running, and no handoff document passes it on to a successor, so nobody currently owns it; and its subject matter (a code-review prompt) obviously falls within your assigned area of how this project reviews things.
3. "Its transcript: `~/.claude/projects/-home-nedlern-agents-choirmaster/29d66917-9767-47cb-a221-d4876d8014cd.jsonl`." — The full conversation log of that session, from which the drafted work can be recovered, is at that path on this machine.
4. "Read it before starting any code-review-prompt work — that wheel is already partly built." — If you undertake work on a code-review prompt, read that transcript first, because a portion of the work already exists there; the idiom "that wheel is already partly built" is a variant of "don't reinvent the wheel" and means you would otherwise duplicate effort that has already been spent.

## The user's standing bar for reviewers

1. "Stated at S6 of the 2026-08-10 walk: *'asking to simplify is like asking to optimize without context.'*" — The user said the quoted words at step or item 6 (labelled S6) of a walk-through conducted on 10 August 2026; the quoted point is an analogy: a bare instruction to "simplify" is as meaningless as a bare instruction to "optimize", because in both cases you cannot act without being told what dimension you are optimizing or simplifying along — faster, smaller, cheaper, easier to read, and so on — so the instruction is underspecified until that dimension is named.
2. "State the axis." — The resulting requirement on you: whenever you ask for or perform simplification, explicitly name the dimension along which "simpler" is being measured.
3. "This project's axis is **simple-to-operate over simple-to-build; mechanical guarantees over trained habit; a detector with no consumer is cost without value; never trade a deterministic script for probabilistic agent behavior.**" — For this project, that dimension is fixed and consists of four stated preferences: (a) when the two conflict, prefer a design that is easier to run day to day over one that is easier to construct in the first place; (b) prefer enforcement by a mechanism that cannot be forgotten or skipped over reliance on people or agents having been taught to do the right thing; (c) a check or alarm that produces a signal nobody reads or acts on is pure overhead, since it costs something to build and maintain and returns nothing — the implication being that such detectors should not be built, or should be removed; (d) do not replace a script that behaves identically every time with an agent whose behaviour varies from run to run, i.e. determinism is not to be given up in exchange for whatever flexibility an agent offers. Note that this sentence uses "never", the strongest form of imperative, for the fourth item only.

## First action

1. "Read the scorecard and the prompt draft's header, verify the four novel findings against current code, then offer the user the grid-seat walk — the decision this pile exists to reach." — Your opening sequence of work, in this order: first read `scorecard.md` from the attack-split experiment and the header block of `sanity-checker-prompt-draft.md` that records the settled operating rulings; second, check each of the four un-triaged novel findings against the code as it stands today to see whether each is still accurate; third, propose to the user that you conduct the item-by-item walk on whether the sanity-checker should take up seats in the md-review grid, that being the question this whole area of responsibility was created in order to settle. I read "offer" as meaning you propose the walk and let him accept, not that you begin walking unilaterally.
2. "He has already been asked 'which first, the walk or the triage?' and has not answered; ask again plainly and let him choose." — The user has previously been put the question of whether he wants the grid-seat walk done before the triage of the four findings or the other way round, and he never gave an answer; you should therefore put the same question to him again, in straightforward language, and let his answer determine the order rather than deciding the order yourself. Note that this sits in tension with the ordering given in the preceding sentence, which puts verification before the walk offer; I take the standing instruction to be that the sequencing between walk and triage is his to set, but I cannot tell from the text alone whether the earlier sentence's order is meant as a default that his answer may override.

