<!-- provenance: runtime=claude model=claude-opus-5 effort=high cell=defect-hunt tier=good target=/home/nedlern/Projects/nedschorus/.claude/worktrees/gatekeeper-walk-fork-continuation/docs/agents/sanity-checker-instructions.md -->

Read the file in full (no YAML frontmatter present) plus the checkout's `CLAUDE.md`, and verified every path it names — all eight referenced paths exist. Analysis only; no edits made.

# Findings

**1. L3 — "The sanity-checker reviewer, the md-review grid it may join"** (b, i)
The file's own title is "`sanity-checker` — seat instructions", so `sanity-checker` names the seat. Here the same token names a *reviewer artifact* the seat is building. L7 then compounds it: "Does the sanity-checker join the md-review grid" — under the title's reading this asks whether the seat joins the grid, under L3's reading whether the reviewer prompt becomes grid cells. A future agent deciding what "the sanity-checker" refers to in any given sentence has to guess, and the two readings produce different work (staffing question vs. prompt-integration question). Harm lands at L7, the one decision the file exists to reach. Sure that the token is overloaded; the seat model's table (`agent-seat-model.md` line 23) uses the same overload, so this may be intentional project usage, but this file never states which is which.

**2. L3 — "the skill rules that govern how reviews are delivered"** (d)
No skill is named, no path given, and "delivered" is not explained (delivered to whom — the user, a PR, a records directory?). L29's parenthetical "md-review delivers piecemeal under a Monitor" appears to be an instance of these rules, but the connection is never made. An agent told this is part of its pile cannot enumerate the rules it owns. Sure.

**3. L7 — "run on both Fable and gpt-5.6-sol at xhigh" vs. L9 "The already-walked operating rulings — scope, trigger, order, **models**, piecemeal delivery, triage ownership"** (a)
L7 places the model choice inside the still-open question awaiting the user; L9 lists "models" among rulings already walked and settled. Both cannot be true. An agent preparing the walk does not know whether to present the model pair as decided background or as part of what the user is being asked to approve — and presenting a settled ruling as an open question wastes the user's turn, while presenting an open question as settled lands an unapproved decision. Sure.

**4. L7 — "as three stance attacks — cut, mechanization, fresh-eyes"** (d)
"Stance attack" is never defined, and the file gives no path to the three attack prompts. The Background section lists `docs/drafts/sanity-checker-prompt-draft.md` but not the per-stance prompt files that exist alongside it. An agent asked to propose that these three become grid cells cannot say what a cell would actually run. Sure — the terms appear nowhere in this file or in the one prompt-draft path it does supply as a definition.

**5. L7 — "it has been waiting since 2026-08-13"** (c)
This file is dated to the same day it describes as the start of the wait (L27 and L33 also stamp 2026-08-13 as current). Read literally on the day of writing, the sentence claims a wait of zero elapsed time while its rhetorical force ("has been waiting since") asserts a backlog. Read a month later, the reader cannot tell whether the date is the decision's origin or a stale copy of "today". Harm is mild but real: the phrase is being used to convey urgency it does not carry. Unsure — I cannot rule out that the decision genuinely arose earlier the same day and the date is exact.

**6. L9 — "gatekeeper 4 of 5 in-band accepted cuts against the baseline's best 3 of 6"** (d, b)
"In-band" is undefined here (it is project vocabulary used in `fast-handoff-design.md`, which this file does not reference). "The baseline's best" is ambiguous: best of several baseline runs, best-scoring baseline cell, or best of the two documents? An agent asked by the user "what does 4 of 5 mean?" during the walk — the predictable first question — cannot answer from this file, and the walk stalls on its own headline evidence. Sure.

**7. L9 — "zero unflagged false positives across eight judgment cells"** (b, d)
Two problems. "Unflagged false positives" is a double negative with no stated flagger: a false positive that the reviewer itself labelled uncertain, or one no human caught? The two readings give opposite meanings — the first is a virtue of the reviewer, the second only a statement that nobody checked. Separately, "eight judgment cells" does not reconcile with anything else in the file: L7 describes three stances on two runtimes (six), and the md-review grid separately runs eight reviewers, so a reader may wrongly conclude these were grid cells rather than experiment cells. Sure that the arithmetic is unreconstructable from this file.

**8. L9 — "**A skill change is walked with the user before it lands**, so this is a walk, not an application."** (b, g)
"Lands" is not defined, and the checkout's `CLAUDE.md` states the interim lane explicitly: "commit to the working branch, push it, and the user's Mac-side agent reviews and merges." Under that lane this seat does not control the landing event. So the rule supports two readings — walk before *committing* the change, or walk before it *reaches main* — and only the first is within the seat's power to obey. The reachable case where the Mac-side merger lands a skill change that this seat committed but never walked is neither covered nor discarded. Harm: an agent believing it satisfied the rule by walking before merge has no way to hold that guarantee. Sure the term is undefined; unsure whether the author intends the seat to be responsible past push.

**9. L13 — "each read an *archived* snapshot — so **verify every quoted ground against current code before proposing anything**" vs. the findings that follow** (a)
The instruction says every ground in the four findings is unverified, yet the findings are then written in confident present tense as established current fact: "though the suite now exists" (L16), "so the gate runs no checks today" (L16), "No gate-edits-the-gate guard" (L17), "stalls below the recycle threshold with no watchdog" (L18). The file simultaneously says "do not trust these claims" and states them as claims about today. An agent skimming for the findings list will carry the present-tense assertions forward into a triage or a walk without the verification the previous sentence demanded. Sure.

**10. L13 — "verify every quoted ground against current code"** (c, d, i)
"Quoted ground" is a coined term never defined in this file (it is used in the prompt drafts, which this file does not point at for a definition). Worse, taken literally the instruction is impossible for finding 4: "the wedged-but-light session: stalls below the recycle threshold with no watchdog" is a claim about live session behaviour and about the absence of a monitoring process, not a claim readable out of the repository's code. An agent obeying "verify **every** quoted ground against current code" has no defined way to discharge finding 4 and no stated alternative, so it either fabricates a code-level check or stalls. Sure.

**11. L13 — "four findings beyond both ground-truth sets"** (d)
"Both ground-truth sets" is a definite reference to two artifacts that are never named or pathed. From context they are probably the gatekeeper and fast-handoff ground truths mentioned at L9, but that is inference. An agent that wants to confirm a finding really is novel — the exact property the sentence asserts — cannot locate the sets to check against. Sure.

**12. L16 — "The gatekeeper spec's 'when a test suite exists, the tests run here' never fired, though the suite now exists — so the gate runs no checks today."** (c)
The conclusion does not follow from the premise given the state the checkout's `CLAUDE.md` records: "the gate is dormant" until credential work lands at build slice 6. A dormant gate runs no checks regardless of whether that spec clause fired, so "the gate runs no checks today" is true for a reason this finding does not name, and the finding as written reads as a live defect rather than as an expected consequence of dormancy. An agent triaging this will either propose a fix for a non-problem or mis-scope the real one (that the clause has no implementation ready for when the gate goes live). Sure that the causal chain as stated is wrong; unsure exactly what the original reviewer meant to claim.

**13. L17 — "No gate-edits-the-gate guard."** (d, i)
A five-word fragment. It does not say what the guard would prevent (the gatekeeper program approving a change to its own source? an agent editing `scripts/git-gatekeeper.py` and pushing through the gate it just edited?), where such a guard would live, or what evidence supports its absence. "Gate-edits-the-gate" is coined here and is not self-documenting: the phrase does not indicate which "gate" is subject and which is object. This is one of four items the file instructs the seat to triage; triage requires knowing what is being claimed, and this item does not supply it. Sure.

**14. L18 — "A writer-stamps-the-pin proposal, to stop agents hand-writing 40-character SHAs."** (d, i)
"The pin" has no antecedent anywhere in this file — no pin, pinning mechanism, or pinned artifact is introduced. "Writer" is likewise unbound (the writer of what — a record, a commit, a design doc?). The name "writer-stamps-the-pin" is a three-part coinage that a reader cannot expand without already knowing the mechanism, and it is not searchable to a definition: outside this file it appears only in a queue document and in review transcripts, none of which this file cites. An agent asked to triage this cannot determine what is being proposed. Sure.

**15. L18 — "stalls below the recycle threshold with no watchdog"** (d)
"Recycle threshold" is a harness/handoff concept defined in `.claude/skills/handoff/SKILL.md`, which this file never references; "watchdog" names a component that does not exist in the file's world. The finding also does not say what a stall *is* here (no tool calls for N minutes? no user-visible output?), so there is no criterion by which an agent could confirm or reject it. Sure.

**16. L20 — "Findings 1 and 2 are gatekeeper territory and 4 is fleet territory"** (g)
Finding 3 is silently omitted. The sentence enumerates ownership for three of four items and the next clause — "hand the survivors to those seats" — has no destination for 3. Combined with "rather than implementing them yourself", finding 3 is stranded: this seat may not implement it and no seat is named to receive it. This is a reachable case (finding 3 surviving triage) that the file neither routes nor discards. Sure.

**17. L20 — "triage them here, then hand the survivors to those seats"** (d, g)
No handing mechanism is specified — an issue via the ghi path, an append to a queue document, a message to a live session, an edit to that seat's instructions file? The seats are named in backticks as if addressable, but nothing in this file says how to address them. "Survivors" is also undefined: survivors of verification against current code, or of a merit judgement, or of the user's ruling? Harm: the triage completes and its output has nowhere to go, so the work evaporates — the exact failure the "never presented, never triaged" history at L13 records. Sure.

**18. L25 — "Its calibration protocol is the live gate before any grid seat."** (a, d, f, h)
Three distinct problems in one sentence.
- *Term collision.* This file uses "the gate" at L16 in the checkout `CLAUDE.md` sense — "How a change reaches main: the git-gatekeeper ... is the permanent path — one program, one credential, one door" — and here uses "gate" to mean a mandatory precondition check, further qualified as "live", which is precisely the word `CLAUDE.md` reserves for the git-gatekeeper's not-yet-reached activated state ("the gate is dormant"). A reader encountering "the live gate" in a project where "the gate" is dormant will read this as a statement about the git-gatekeeper. Sure.
- *Un-executable.* The sentence asserts the protocol's status but never says what it requires, who runs it, or what passing looks like; an agent cannot execute "be gated by" without those. Sure.
- *Over-broad absolute.* "before **any** grid seat" — the ordinary counterexample is a grid cell that performs no subtraction judgement at all (a restate or hunt cell of the kind the records directory is full of), for which a calibration protocol derived from a subtraction review has no evident applicability. Sure the claim is stated broader than the evidence sentence before it supports; unsure whether the author intends it to cover non-subtraction cells.

**19. L27 — "Note the Codex floor tier moved terra → luna on 2026-08-13 (in PR #57)."** (d)
"Floor tier", "terra", and "luna" are undefined; nothing in this file says what a tier is, what floor means, or what changes as a consequence. The sentence begins "Note", asserting relevance, but never states the relevance — an agent cannot tell whether this invalidates prior records, changes how it should read cell outputs, or is trivia. It also breaks the section's own convention: #51 and #53 are given as links at L29 while #57 is a bare number. Sure the terms are undefined here; unsure whether they matter enough to the seat's work to need explaining.

**20. L29 — "**Open PRs yours to shepherd:**"** (d, e)
"Shepherd" is undefined and, per the checkout `CLAUDE.md`, the seat cannot perform the terminal act: "the interim lane applies: commit to the working branch, push it, and the user's Mac-side agent reviews and merges." So the file assigns an open-ended obligation with no completion criterion — an agent has no way to know when a PR is done being shepherded, and no way to close it out itself. This is work-without-a-stopping-point: the seat is left polling two PRs indefinitely. Sure.

**21. L29 — "coordinate with `fleet` if it needs work"** (b, d)
"It" is ambiguous between PR #52 and the fast-handoff findings applied in it. More importantly, no coordination mechanism exists in this file — `fleet` is written as an addressable name with no address. The same gap as finding 17, at a second site. Sure.

**22. L9/L29 — "`md-review-records/2026-08-12-attack-split-experiment/scorecard.md` (also in PR #53)"** (g)
The parenthetical implies the scorecard lives on the #53 branch. The file then instructs, at L41, "Read the scorecard" using the bare repository path, with no statement of which branch that path is valid on. An agent starting from a fresh checkout of `main` while #53 is unmerged will not find the file and the file gives no fallback. (In this worktree the path does resolve, so the case is conditional, not certain.) Unsure — the parenthetical may simply be noting the PR that will land an already-committed file.

**23. L29 — "md-review delivers piecemeal under a Monitor"** (d)
"Monitor" is capitalized as a proper noun with no explanation and no path. It is a harness tool referenced in the md-review skill, but this file's stated context does not include the harness tool list, and the file offers no definition. An agent reading this cannot tell whether "under a Monitor" is a constraint it must reproduce or a description of existing behaviour. Sure.

**24. L33 — "No live session, mentioned in no handoff, and plainly related to your pile."** (f)
"Mentioned in no handoff" is an unbounded negative over every handoff document ever written, in a project where handoffs are produced per-session across at least two machines. It cannot have been established and cannot be re-established by a later reader. The ordinary counterexample is a handoff written on the Mac side, or one written after this file was, that does mention the session. Harm: an agent treats "unowned" as verified fact and claims a thread another seat is actively holding. Sure the claim is broader than checkable.

**25. L33 — "Its transcript: `~/.claude/projects/.../29d66917-....jsonl`. Read it before starting any code-review-prompt work"** (e, g)
The file states the transcript is 3.67 MB and then instructs the agent to read it, with no extraction target, no section to seek, and no stopping condition. A 3.67 MB JSONL transcript will not fit a context window; obeying literally either exhausts the session before any work begins or forces an undocumented improvisation about what to sample. Separately, the section heading promises a thread "worth claiming" but no claiming mechanism is given, and the work sits in `~/agents/choirmaster`, outside this project — whether this seat may write there, and under what lane changes there reach anywhere, is neither stated nor discarded. `CLAUDE.md` marks `~/Projects/nedlern` read-only and says nothing about `~/agents/`, so the agent has no rule to apply. Sure on the stopping-point defect; sure that the write permission for `~/agents/choirmaster` is unaddressed.

**26. L37 — "State the axis."** (b)
Two readings, and they call for different behaviour. Reading one: in each review, state the axis along which a given simplification is being proposed (the direct consequence of the quoted "asking to simplify is like asking to optimize without context"). Reading two: state the project axis given in the next sentence. Under reading one the axis varies per finding and must be derived; under reading two it is a fixed string to quote. The sentence's placement between the quote and the project axis makes both available. An agent building the reviewer prompt will encode one of them into a skill. Sure.

**27. L37 — "This project's axis is **simple-to-operate over simple-to-build; mechanical guarantees over trained habit; a detector with no consumer is cost without value; never trade a deterministic script for probabilistic agent behavior.**"** (b)
Singular "axis" is then given four clauses of three different grammatical kinds: two preference orderings ("X over Y"), one valuation claim, and one prohibition. If the reviewer is to "state the axis" (singular) it does not know which of the four to state, and if a proposed simplification is favoured by one clause and disfavoured by another the file gives no precedence. Sure the singular/plural mismatch is real; unsure how often the clauses actually conflict in practice.

**28. L37 — "a detector with no consumer is cost without value"** (f)
Stated as an unqualified identity. The ordinary counterexample is a detector whose consumer is future and human: an audit or log check with no automated consumer today, read by a person once when something goes wrong — the `audit` subcommand named in the checkout `CLAUDE.md` ("`audit` checks main's live protection against the design") is exactly this shape and is a shipped part of this project. An agent applying the clause literally would recommend cutting it. Sure.

**29. L37 — "never trade a deterministic script for probabilistic agent behavior"** (f)
An absolute with a standing counterexample inside this very file: the md-review grid, the three stance attacks proposed at L7, and the sanity-checker reviewer itself are all probabilistic agent behaviour deployed where no deterministic script can do the job (judging whether prose is clear). The clause as written forbids a trade the project makes routinely; what it presumably means is "do not replace a working deterministic script with an agent", which is narrower. The checkout `CLAUDE.md` also cautions against exactly this construction: "absolute imperatives like 'always' or 'never' can backfire in unforeseen conditions. Use them cautiously." Harm: an agent holding this as a bar rejects every mechanization proposal that mechanizes *into* an agent — including, potentially, the grid-seat decision at L7 the file exists to advance. Sure.

**30. L41 — "Read the scorecard and the prompt draft's header, verify the four novel findings against current code, then offer the user the grid-seat walk ... He has already been asked 'which first, the walk or the triage?' and has not answered; ask again plainly and let him choose."** (a)
The first clause prescribes a fixed order — verification of the findings first, walk second — and the last clause says the order is the user's open choice. Obeying the first sentence answers the question the second sentence says to ask, and does so in the opposite direction from the sentence's own emphasis ("offer the user the grid-seat walk — the decision this pile exists to reach"). An agent following the paragraph top to bottom does the triage work, then asks the user whether he'd like the triage or the walk first. Sure.

**31. L41 vs. L20 — "verify the four novel findings against current code" / "triage them here"** (b)
The user's question is framed as "the walk or the triage", but the first action names *verification*, and L20 names *triage*, without saying whether these are the same step, sequential steps, or alternatives. If they are distinct, the first action instructs the agent to do the first half of the thing it is then supposed to ask permission to start. Sure the relationship is unstated; unsure whether the author treats verify and triage as one activity.

**32. L3 vs. L41 — "Your pile: **how this project reviews things, and how well that works.**" / "the decision this pile exists to reach"** (a)
L3 defines the pile as a standing domain of ongoing responsibility (a reviewer, a grid, skill rules, two PRs, an unowned thread); L41 says the pile exists to reach one decision. If the second is true the pile terminates when the user rules on the grid seat, and the PRs, the standing bar, and the choirmaster thread have no owner afterward. An agent reading L41 as the pile's purpose will treat everything else in the file as incidental. Sure the two statements of purpose differ in scope; unsure whether L41 is meant as rhetorical emphasis rather than a definition.

clean sections: none — every section (opening/pile statement, "The decision waiting on the user", "The un-triaged novel findings", "Background you will need", "An unowned thread worth claiming", "The user's standing bar for reviewers", "First action") carries at least one finding.

