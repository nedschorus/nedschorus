<!-- provenance: runtime=claude model=claude-sonnet-5 effort=high cell=defect-hunt tier=floor target=/home/nedlern/Projects/nedschorus/.claude/worktrees/gatekeeper-walk-fork-continuation/docs/cross-project/repo-boundary-and-runtime-layers.md -->

Read the target file in full along with the two referenced CLAUDE.md files, and cross-checked several of its citations and manifest claims against the actual repository state (`docs/issues/queue/45-session-seat-and-isolation-riders.md`, `docs/agents/`, `docs/cross-project/`, `scripts/`, `.claude/hooks/`, `.claude/skills/`, `walk-ledgers/`). Findings below.

1. **Line 3**: `"Ruled by the user 2026-08-14, in the walk that also hardened the box's backups."` (recurs at line 102: `"...got stronger during the walk that produced this document"`)
   "The walk" is used as if it names a specific, resolvable thing, but it is never defined here, and neither CLAUDE.md defines it. I checked the repository and found the only artifact type that would explain it — `walk-ledgers/*.md` — is described in that ledger's own header as "Machine-local, gitignored, deleted at walk close." So even a reader on the same machine, at almost any later date, cannot resolve the term: it isn't defined in the allowed context, and the one thing that could define it is designed not to persist. Confidence: sure.

2. **Lines 8, 27, 58, 90**: `"its seat briefs"` / `"seat worktrees"` / `"seat briefs, drafts"` / `"seat model and briefs"`
   "Seat" is used four times as load-bearing vocabulary (contrasted with plain sessions/worktrees) but is never defined in this file or in CLAUDE.md. A definition does exist at `docs/agents/agent-seat-model.md`, but this file's only pointer to that location is the manifest row at line 90 — which arrives after three prior uses and functions as a classification entry, not a definition. A reader restricted to CLAUDE.md + this file hits "seat briefs" at line 8 with nothing to resolve it against. Confidence: sure the term is undefined in-file; unsure whether the author intends "seat" as assumed-known ambient vocabulary, but flagging per instructions regardless.

3. **Line 31**: `"**A cloud session can reach exactly what is inside the git repository it cloned, and nothing else.**"` — contradicted by **line 78**: `"scripts/md-review-grid.py and its two cell runners | 2 — tool | Yes, if the runtimes it shells out to exist there"`
   Line 31 is an absolute claim: a cloud session reaches only repository contents. But md-review-grid.py "shells out to" the box and the Mac, which by the runtimes table (lines 25-29) are physical machines, not repository contents — under line 31's own rule, a cloud session can never reach them. The manifest's hedged "Yes, if..." therefore describes a condition that line 31 says can never hold; the answer should be an unconditional "No" (or a note that the script runs but is functionally useless there), not a conditional "Yes." Confidence: sure — derived entirely from the document's own two statements.

4. **Line 33**: `"...recorded in \`docs/issues/queue/45-session-seat-and-isolation-riders.md\`"` (source for the `--teleport` and per-turn sync-budget facts in lines 35-36)
   I read that file in full (43 lines). It covers session/seat isolation guards, a `--directory` launcher flag, branch-per-session discipline, and a "facts worth keeping" list (job ids vs. session ids, the `claude agents` view, etc.), verified against Claude Code **2.1.231**. It contains no mention of `--teleport`, cloud sessions, or per-turn sync budgets, and cites a different binary version than this document's "2.1.232." The citation points to the wrong file for these two claims — there is no way to trace or verify them from the given path. Confidence: sure — verified by reading the cited file's entire contents.

5. **Line 40**: `"Every component belongs to exactly one of these..."` — contradicted by **line 120**: `"When a file's layer is genuinely unclear, record the ambiguity in the manifest row rather than picking silently."`
   Line 40 asserts every component has exactly one, decidable layer. Line 120 directly allows for files whose layer is "genuinely unclear," to be recorded as ambiguous rather than resolved. This is borne out in the manifest itself: four rows (lines 85-88) are tagged "2 — unverified" and one (line 89) "3 — misfiled" rather than a clean single-layer number, and the Layer 3 test at line 58 explicitly allows a third answer ("it depends") beyond a clean determination. Confidence: sure.

6. **Line 44** (Layer 1 definition: `"The status line, skills, hooks, and the global \`CLAUDE.md\`... read... from the working directory's \`.claude/\` at session start"`) and **lines 60-94** (manifest, claimed to cover `"Every component in the repository as of 2026-08-14"`)
   This repository's own root-level `CLAUDE.md` — at the repo root, not inside a `.claude/` subdirectory — is loaded at session start (as this very review's context bundle demonstrates) but is never classified: it's not named in Layer 1 (which only names the "global" one), not in Layer 3, and not in the manifest. Checking the manifest against the actual repository turned up further gaps: `scripts/sanity-checker-attack-experiment.py` exists on disk with no manifest row, and none of the seven `*-test.py` files under `scripts/` (e.g. `git-gatekeeper-test.py`, `handoff-supervisor-test.py`) appear either. Confidence: sure — verified by listing `scripts/`, `.claude/hooks/`, `.claude/skills/`, `docs/`, and the repo root against the manifest table.

7. **Line 52**: `"so they need to exist on disk somewhere findable — a checkout on \`PATH\`"`
   `PATH` is conventionally an executable-search variable that resolves individual files, not a directory ("a checkout"). It's unclear whether this means the checkout directory should be added to `$PATH`, individual scripts symlinked onto it, or something else — none of these is stated, and a future agent trying to satisfy "findable... on PATH" for a new Layer 2 tool has no concrete procedure to follow. Confidence: unsure — may be intentional shorthand understood by the intended reader, but as written the mechanism is unspecified.

8. **Line 58**: `"If the answer needs a 'well, it depends', it is project content."` vs. **Line 116** (Filing rule, step 1): `"Would a different project want this unchanged? No → layer 3..."`
   The Layer 3 test explicitly names a third possible answer — "it depends" — and resolves it to layer 3. The Filing rule, presented as the procedure to actually follow for a new file, frames the identical question as strictly binary and never mentions the "it depends" case. A reader following the Filing rule literally and arriving at "it depends" has no instruction for which branch to take, even though the earlier section already resolved that exact case. Confidence: unsure — the intended mapping ("it depends" → "No") is guessable, but isn't stated in the section meant to be followed as a procedure.

9. **Line 118**: `"Otherwise it is layer 2 — a tool, in \`scripts/\`, with its design document in \`docs/cross-project/\`."`
   This states, as a defining property of layer 2, that a tool has a companion design document in `docs/cross-project/`. I grepped every file in `docs/cross-project/` for `md-review-grid`, `md-drift-lint`, and `backup-health-check` — each is already classified layer 2 in this document's own manifest (lines 78-81) but has no matching hit in any file other than this document itself. The Filing rule gives no guidance for a layer-2 tool that doesn't (yet, or ever) have a design document: is one mandatory before commit, optional, or deferred? Confidence: sure that the three named tools currently lack a design doc under `docs/cross-project/`; unsure whether the rule is meant as a strict requirement or an aspirational default.

clean sections: "## What is not the problem"

