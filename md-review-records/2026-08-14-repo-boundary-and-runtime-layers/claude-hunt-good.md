<!-- provenance: runtime=claude model=claude-opus-5 effort=high cell=defect-hunt tier=good target=/home/nedlern/Projects/nedschorus/.claude/worktrees/gatekeeper-walk-fork-continuation/docs/cross-project/repo-boundary-and-runtime-layers.md -->

Read in full, plus the checkout's `CLAUDE.md`, and verified the referenced paths on disk (`~/nedsmessenger` does exist; `docs/issues/queue/45-session-seat-and-isolation-riders.md` and `docs/cross-project/nedschorus-founding-plan.md` exist). No edits made.

## Findings (document order)

**1. L3 — "Ruled by the user 2026-08-14, in the walk that also hardened the box's backups."**
"the walk" is an undefined term used as if it were a known unit of work (class d). A reader with only this file cannot tell whether a walk is a meeting, a session type, a review procedure, or a document, and therefore cannot tell what "ruled in the walk" confers — nor find the record. (The repo has a `walk-ledgers/` directory, but this file never says so.) The backup-hardening clause adds nothing checkable and points at work this document never touches. Harm: a future agent trying to establish the provenance or authority of these rulings has nowhere to go. Sure.

**2. L3 / L27 — "the box's backups" before "the box" is defined**
"the box" is first used at L3 but only defined at L27 (`ned-box`, Ubuntu). Anyone reading top-down hits an unexplained proper noun in the second sentence. Sure, minor but real (class d).

**3. L5 — "It answers one question — *which repository, and which machine* — and it answers it for three runtimes rather than one."**
Two defects. (i) "one question" is immediately followed by two questions joined by "and"; a reader cannot tell which one is the promised deliverable (class b/c). (ii) The machine half is never delivered: the Filing rule (L114–118) emits a *layer* and a *repository/directory*, and no step of it ever names a machine. The manifest's third column asks about the cloud only, and nothing in the document tells you which of `ned-box` / the Mac a new file should land on. Harm: an agent that reads the opening promise, follows the filing rule, and still has no machine answer will either invent one or stall. Sure (class a — the promise contradicts the procedure).

**4. L11 — "That single misfiling is the evidence"**
"single" is contradicted downstream: L94 records four further entries in the same `docs/cross-project/` drawer whose classification was guessed from filenames and is explicitly unconfirmed, and one of them (`seed-claude-md-draft.md`) is on its face a draft, which L58/L91 place in layer 3. So the count of known-or-suspected misfilings in that directory is not one. Harm: a reader takes "single" as an audited fact and does not re-examine the other rows. Unsure only about whether the author meant "single *confirmed*" — as written it reads as an audit result.

**5. L15 — "Moving files between directories is cheap and reversible, and a wrong directory costs a `git mv`."**
Directly contradicted by L96, where the one ruled move in this document was *not* executed precisely because it is not cheap: 14 files cite the path, one of them instruction-class and needing its own walked approval, and two more are mid-rewrite on an unmerged branch. Both statements are load-bearing — L15 is the argument for why the boundary is not about disk layout, and L96 is the reason the misfiling still exists. Harm: an agent that believes L15 will move a cited file and break references; an agent that believes L96 will treat every move as a project. Sure (class a).

**6. L17 — "A single repository holding both kinds of thing works fine for one person on two machines."**
The claim is scoped to two machines, but this document's whole premise (L21–36) is that there are *three* runtimes and that the third is the constraining one. It also sits against L36, which says repository size is a functional limit on the cloud runtime — i.e. holding both kinds of thing in one repository is not cost-free. Harm: the sentence is used to dismiss tidiness as a motive, but as written it also appears to dismiss the cloud consideration that the rest of the document rests on. Unsure — the "if that were the whole picture" hedge partly covers it, but the "two machines" framing still omits the runtime the document says decides everything.

**7. L23 — "There are three, and they differ in what they can reach."**
A closed-world count with no expiry or extension rule (class f). Ordinary counterexamples: a second Ubuntu box, a session run inside a devcontainer or CI runner, or a session on a machine belonging to someone else on the LAN. Because "every component belongs to exactly one" layer is derived from this enumeration, a fourth runtime silently invalidates the derivation and nothing in the file says what to do then. Sure that it is an unqualified closed set; unsure whether the author intends it as "three today" or "three by construction" — that ambiguity is itself the problem.

**8. L27 — "seat worktrees" (also L9 "seat briefs", L56 "seat briefs", L90 "seat model and briefs")**
"seat" is used four times as a defined concept and never defined (class d). A reader cannot tell whether a seat is a machine, a role, a checkout, a session, or a person, and therefore cannot apply the filing rule to anything seat-shaped — which matters because L90 classifies a whole directory on the strength of the word. Sure.

**9. L31 — "no path to either machine" vs L35 "`claude --teleport` moves a session between the cloud and a local CLI, in both directions."**
Contradiction two lines apart (class a). L31 says the cloud runtime has no path to either machine; L35 says a documented command moves a session from the cloud to a local CLI and back. Whatever the intended distinction (file access vs session migration), it is not stated. Harm: an agent reasoning about what a cloud session can hand back to the box gets opposite answers depending on which line it reads, and L105 then builds a decision ("a `--teleport` lands in the project checkout") on the L35 reading while the surrounding argument uses the L31 reading. Sure.

**10. L31 — "A cloud session can reach exactly what is inside the git repository it cloned, and nothing else."**
Absolute claim broader than can hold (class f). Ordinary counterexamples: the network (WebFetch/WebSearch, the GitHub API via `gh`, package registries), the container's own installed binaries and interpreters, and environment/secret material provided to the session. The file contradicts this itself at L78, where `md-review-grid.py` is judged cloud-reachable "if the runtimes it shells out to exist there" — those shelled-to runtimes are, by construction, not inside the cloned repository. Harm: this sentence is the stated foundation of everything below it, so an over-strong version of it distorts every downstream decision (notably L105's "bound to one repository"). Sure.

**11. L31 — "It has no `~/.claude` to install into"**
False when obeyed literally (class c): a cloud session runs on a machine with a home directory and a Claude Code configuration; what it lacks is a *persistent, pre-provisioned* one that survives the session and is not derived from the clone. As written, an agent could conclude that writing to `~/.claude` in a cloud session is impossible rather than merely futile, and could rule out install-on-start approaches that are in fact available. Unsure — I believe the intended claim is about persistence and pre-provisioning, but the sentence does not say that.

**12. L33 — "read from the Claude Code 2.1.232 binary rather than assumed"**
The two facts below are pinned to one binary version, and two of this document's three "case against splitting" items (L105 item 2, and the size argument at L36) rest on them, but nothing states what happens when the version moves — no re-verification trigger, no owner, no statement that the conclusions are contingent (class g, applied to the evidence store this file establishes). Harm: a year from now the citation reads as a durable fact rather than a snapshot, and no one knows it needs re-checking. Unsure whether the author considers this in scope, but the version number is stated precisely enough that its volatility should be too.

**13. L35 — "It requires a clean local working directory and requires running from a checkout of the same repository the cloud session used."** vs **L105 — "a `--teleport` lands in the project checkout"**
The two descriptions do not compose (class b). L35 describes teleport as something you *run from* a specific checkout that must already exist and be clean; L105 describes it as something that *lands* somewhere by itself, as if the destination were chosen for you. An agent reasoning about the split cannot tell whether teleport fails, prompts, or silently picks a directory when the requirement isn't met — and L105's argument depends on which. Also, no reachable-case statement for the dirty-working-directory path: refuse, stash, or error is unspecified. Sure that the two phrasings conflict.

**14. L36 — "Cloud sessions sync files under hard budgets ... more files than per-turn sync can track. Repository size is therefore a functional limit"**
Two defects. (i) "hard budgets" is un-executable: no number, no unit, no way to measure whether this repository is near the limit (class d). An agent told repository size is a functional limit cannot act on it. (ii) The sentence conflates *file count* ("more files than per-turn sync can track") with *size* ("repository size"), which are different quantities with different remedies — a repo of few huge files and a repo of many tiny ones behave oppositely under the stated mechanism (class b). Sure.

**15. L40 — "Every component belongs to exactly one of these, decided by *where it must physically be at the moment it is used*."**
The stated deciding criterion does not decide (class a/c). Layers 2 and 3 have identical physical answers — both are files on disk in a checkout, read when wanted — so "where it must physically be at the moment it is used" cannot separate them. The actual layer-3 test given at L58 is an ownership question ("would a different project want this file unchanged?"), not a location question, and the filing rule at L116 puts that ownership question *first*, before location is considered at all. Harm: an agent applying L40 as stated and an agent applying L116 as stated will classify the same file differently. Sure.

**16. L40 — "exactly one of these"** vs the manifest's Layer column
The manifest uses five distinct values in a three-valued column: `1 — runtime`, `2 — tool`, `2 — tool's design` (L82–84), `2 — unverified, see below` (L85–88), and `3 — misfiled, see below` (L89). "Tool's design" in particular is a category the three-layer model never defines — a design document is not "invoked by path when needed" (L52) — so the manifest silently extends the taxonomy the model claims is closed. Harm: a new design document has no defined home; the reader must guess whether "tool's design" is a layer, a sub-layer, or an annotation. Sure (class a).

**17. L42 — "### Layer 1 — Runtime: must be present in every session"** vs **L23 — "A 'runtime' here means a place a Claude Code session actually runs."**
The file explicitly defines "runtime" as a *place a session runs*, then names a *layer of components* "Runtime" (class b/i). The document title, "Repository boundary and runtime layers", inherits the collision and reads as though runtimes and layers were one concept when the file's own structure keeps them separate — three runtimes and three layers, both counted three, both called runtime. Harm: "runtime" is exactly the word a future agent will grep for, and the hits split between two incompatible meanings; sentences like L78's "the runtimes it shells out to" (see finding 24) become unreadable. Sure.

**18. L44 — "Nothing an agent does later can load them."**
Absolute and false as stated (class f/c). Ordinary counterexample: skills are precisely the thing an agent loads later — a skill's instructions enter the turn when it is invoked, mid-session, which is why four skill directories are listed in this very manifest as Layer 1. An agent can also read any of these files with Read at any time; what it cannot do is make the *harness* re-register a hook or a status line mid-session. Harm: the sentence is the justification for the whole Layer 1 install obligation, and its over-strong form will be quoted back to rule out approaches that actually work. Sure.

**19. L46 — "`~/.claude/CLAUDE.md` loads into every session on the box unconditionally, with no recall step to forget."**
"unconditionally" and "every" overreach (class f): an ordinary counterexample is a session started with a different `HOME`, in a container, or by a non-Claude-Code runner — none of which read that file. Separately, "no recall step to forget" leans on an undefined notion of a recall step (class d); a reader without the memory-system context cannot tell what is being contrasted. Unsure on how much the author intends "on the box" to fence the claim — but the sentence is doing evidentiary work ("the user already relies on it"), so the fence matters.

**20. L48 — "requiring the git-gatekeeper to version itself and upgrade automatically" ... "Any install step here carries the same obligation: a version stamp and a staleness check, from the first version."**
The obligation stated is weaker than the ruling it claims to inherit (class a/b): the cited ruling requires *automatic upgrade*; the derived obligation requires only a *stamp and a check*. "The same obligation" therefore misdescribes what follows it, and a builder cannot tell whether auto-upgrade is required or optional. Compounding this, the check's behaviour on detection is nowhere stated (class g): when the installed copy is found stale, does the tool warn, refuse to run, or self-update? The quoted rationale — fixes made but never deployed — is only answered by one of those. Harm: two implementers reading this ship different things, and the failure mode the rule exists to prevent survives in the warn-only version. Sure.

**21. L52 — "These are run by name at the moment they are wanted, so they need to exist on disk somewhere findable — a checkout on `PATH`"** vs **manifest rows L82–L88**
The Layer 2 definition is entirely about executables (run by name, on `PATH`), but eight of the manifest's Layer 2 rows are Markdown design documents, which are never run and cannot meaningfully be on `PATH`. The definition therefore excludes the majority of the rows filed under it (class a). Harm: an agent filing a new design document under the L118 rule ("layer 2 — a tool, in `scripts/`, with its design document in `docs/cross-project/`") cannot tell whether the document *is* a layer-2 component or merely an attachment to one. Sure.

**22. L56–58 — "design documents about this project ... Cheapest test: would a different project want this file unchanged?"** vs **CLAUDE.md and L82**
Layer 3 is defined to include "design documents about this project", and the checkout's `CLAUDE.md` states: "How a change reaches main: the git-gatekeeper (`scripts/git-gatekeeper.py check-in` — specification: `docs/cross-project/git-gatekeeper-design.md`) is the permanent path". That design document specifies *this project's* mandated change-control path — yet L82 files it as "2 — tool's design" (class a/h; both definitions quoted). The two readings are irreconcilable without a stated rule for "a document about a shared tool that is also this project's governing process". Harm: the same ambiguity applies to `fast-handoff-design.md` and `fleet-machine-paths-and-checkouts.md`, and to any future design doc; and if the split ever happens, this decides whether the project's own change-control spec leaves with the tools. Sure.

**23. L58 vs L116 — the layer-3 test as first gate reclassifies manifest rows**
L116 makes "Would a different project want this unchanged? No → layer 3" the *first* question for every new file. Run the manifest's own Layer 1 rows through it: `.claude/hooks/backup-and-snapshot-write-guard.py` "guards machine-local paths" (L68) and `scripts/handoff-context-threshold-hook.py` serves this project's handoff supervisor — a different project plainly would not want either unchanged, so the filing rule assigns them layer 3 while the manifest assigns them layer 1 (class a). Harm: the two halves of the document, which L100 insists must stay together, disagree on concrete files; a future agent using the rule to classify a new hook will produce rows inconsistent with the existing table and have no way to see why. Sure.

**24. L60 / throughout — "The manifest" as the name of this table**
Not self-documenting and collides with existing project naming (class i). The repository already contains a root-level `entry-manifest.md`, so "the manifest" is ambiguous on grep and in conversation, and the bare word appears eight times (L60, L62, L94 context, L100, L108, L120) with no qualifier. The checkout's `CLAUDE.md` states: "When creating or inventing names ... use explicit, clear and precise multi-part names. Check newly invented names with glob ... If these checks return collisions or ambiguity, choose a more explicit name, with 3 or 4 parts, not 1 or 2." Harm: L120 instructs a future agent to "record the ambiguity in the manifest row" — that agent must first work out which manifest. Sure.

**25. L62 — "Every component in the repository as of 2026-08-14, classified."**
False as stated (class f), and verifiable from the checkout. Unlisted: `README.md`; the root `CLAUDE.md` (which L44 explicitly names as a Layer 1 kind — "the global `CLAUDE.md`" — making its absence a live gap, and it is also the file whose loading behaviour the whole Layer 1 argument depends on); `.claude/settings.json` (the file that actually registers the hooks and status line, without which the Layer 1 rows above it do nothing); `entry-manifest.md`; `nc-queue/`; `walk-ledgers/`; `scripts/sanity-checker-attack-experiment.py`; every `*-test.py` beside its script (nine of them); and this document itself. Harm: L108 claims the manifest makes a future split "a mechanical operation against a list rather than an archaeology exercise" — a split driven off this list would leave those files unassigned, including the settings file that makes Layer 1 function. Sure.

**26. L62 — "Layer 3 entries are listed by directory rather than by file, since they are numerous and uniform."**
The stated rationale is not the practice: four Layer 1 entries (L70–73) are also listed by directory, and `scripts/md-review-grid.py and its two cell runners` (L78) collapses three files into one row. So directory-granularity is not a Layer 3 property, and a reader cannot infer the actual granularity rule (class b). Unsure whether this misleads in practice, but it will mislead anyone extending the table. Sure that the rationale does not describe the table.

**27. L64 — column heading "Cloud can reach it?" vs its answers**
The column asks a reachability question and is answered, in eight rows, with a usefulness judgement: "No: it manages machine-local sessions" (L74), "No: same" (L75), "No: reads machine-local transcripts" (L76), "No: they reach specific machines" (L77), "No: it reads machine-local backup state" (L81). Taken literally these are wrong (class c): every one of those files is in the repository and therefore *is* reachable by a cloud session — it just cannot do anything useful there. Worse, L68 answers the identical situation differently — "Irrelevant: it guards machine-local paths" — so the table uses two different answers for the same reasoning (class b). Harm: the column is the one the split will be read off, and "No" will be taken to mean "must not / cannot ship in the clone". Sure.

**28. L68 and L81 — "*(pending PR #58)*"**
Two rows describe files that are not in the repository, inside a table L62 declares to be "Every component in the repository as of 2026-08-14" (class a). Additionally, "PR #58" is an unexplained reference (class d): no host, no repository, no title, so a reader with only this file cannot look it up or tell whether it is the same numbering space as the `#45` queue file cited at L33. And there is no stated handling for the reachable case where PR #58 is closed unmerged or renumbered — the rows then describe files that will never exist, with nothing marking them provisional beyond a parenthesis (class g). Sure.

**29. L78 — "`scripts/md-review-grid.py` and its two cell runners"**
The two cell runners are never named (class i/d). They exist — `scripts/md-review-claude-cell.py` and `scripts/md-review-codex-cell.py` — but a reader of this file cannot find them by grepping for anything in this file, cannot tell whether "two" is still accurate if a third is added, and cannot verify the row. Harm: a split executed against this list would move the grid and leave, or wrongly take, files nobody can identify from the list. Sure.

**30. L78 — "Yes, if the runtimes it shells out to exist there"**
"Runtimes" here means language interpreters or model CLIs, not the concept this file defines at L23 as "a place a Claude Code session actually runs" (class b, compounding finding 17). Read with the file's own definition, the sentence says the grid works in the cloud if the box, the Mac, and the cloud exist there, which is nonsense. Separately, which runtimes, and how to check, is not stated, so the condition is un-executable (class d). Sure.

**31. L85–88 — "2 — unverified, see below" ... L94 "Whoever next touches them should confirm or correct the row rather than inherit the guess."**
Three problems. (i) The Layer column carries a value that is not a layer (see finding 16). (ii) The obligation has no trigger and no stopping point (class e): "whoever next touches them" is undefined — does citing the file count, or only editing it? — and no one owns the four rows, so the guess can persist indefinitely while looking like a classification. (iii) The instruction is to correct a row in a document that lives on main and reaches main through the gatekeeper/interim lane; nothing says how that update happens or that it may lag. Harm: the marking reads as diligence but has no mechanism to discharge it. Sure on (i) and (ii); unsure how much (iii) matters in practice.

**32. L88 — `docs/cross-project/seed-claude-md-draft.md` classified "2 — unverified"**
The filename declares it a draft, and L58/L91 place drafts squarely in Layer 3 project content (`docs/drafts/`). The row is marked unverified, so this is not strictly a contradiction, but the document had the information to resolve it and did not (class b). Harm: a reader scanning the "unverified" rows may assume all four are genuinely opaque, when one is decidable from the evidence already in the file. Unsure — deliberately deferring is defensible; the point is that the deferral hides a visible signal.

**33. L96 — "moves to `docs/nedschorus-plan.md` and is retitled — its heading already reads 'nedschorus Boot-Up Plan', so the file has disagreed with its own name for some time."**
Two defects. (i) "is retitled" gives no target title (class d/e): an agent executing the ruled move cannot complete it, and has no way to know whether it did. (ii) The ruled destination reproduces the exact fault it diagnoses — `nedschorus-plan.md` still does not match "nedschorus Boot-Up Plan", so unless the retitling drops "Boot-Up" (unstated), the file will again disagree with its own name. The name is also two parts and generic against the checkout's `CLAUDE.md`: "use explicit, clear and precise multi-part names ... with 3 or 4 parts, not 1 or 2" — and it sits alongside a `docs/founding/` directory whose relationship to a founding plan filed directly under `docs/` is unexplained (class i). Sure.

**34. L96 — "14 files cite it by path, one of them `.claude/skills/ghi-write/SKILL.md` ... and two more are being rewritten in an unmerged branch. It waits for that branch to land."**
No stopping point and no identifier (class e/d). The branch is unnamed, so a future agent cannot check whether it landed and cannot tell whether "that branch" still exists. The disposition of the other eleven citing files is never stated: are they updated as part of the move, left broken, or already fine? And the count "14" has no as-of date or recount instruction, so it will drift. Harm: the one concrete action this document defers is left in a state where nobody can determine that it is now safe to perform, or what performing it fully entails. Sure.

**35. L100 / L120 — "the manifest is its operative half" ... "record the ambiguity in the manifest row rather than picking silently."**
The manifest is defined as a snapshot ("as of 2026-08-14", L62), and L120 assumes new files have manifest rows — but nothing in this file says a new component *must* be added to the manifest, who adds it, or what the manifest means once it no longer matches the repository (class g, applied to the store this file defines). The reachable case is ordinary: a component is added tomorrow. At that moment L120's instruction refers to a row that does not exist, and L108's promise that a split is "mechanical ... against a list" quietly stops being true. Sure.

**36. L105 — "A cloud session is bound to one repository."**
Absolute beyond what holds (class f). Ordinary counterexample: a cloud session with network access can `git clone` or `gh` a second repository it has credentials for; the binding is on what it *starts from* and what teleport requires, not on what it can read. The very next section (L110) worries about whether a session authenticated as one identity can read a repository owned by another — a worry that only makes sense if cross-repository reads are possible in principle. Harm: this sentence is item 2 of the case against splitting; the over-strong version makes the split look more blocked than it is. Sure.

**37. L106 — "Two repositories mean two review-and-merge lanes ... Splitting doubles the credential work that is already the blocking item."**
Both halves are stated as necessity and neither is (class f). One gatekeeper program with one credential scoped at the organisation or account level can gate two repositories — which is precisely the shape the checkout's `CLAUDE.md` describes: "one program, one credential, one door; agents never push to main." "Doubles" is a specific quantitative claim with no derivation, and the ordinary counterexample (a single token or app installation with access to both repos) makes it more like "adds configuration" than "doubles". Harm: item 3 of the case against splitting rests on an unexamined multiplier. Sure.

**38. L110 — "He works under two identities ... `nedschorus` currently lives under the `nedschorus` GitHub organisation and merges run as `NedLern`."**
Three named identities appear (two email addresses, one GitHub login) plus an organisation, with no mapping between them (class d). The open question is "which account owns which repository", but the file does not say which email controls the `nedschorus` organisation, whether `NedLern` is one of the two identities or a third, or what "merges run as" means for ownership. Harm: the question is explicitly handed to the user to answer and marked as blocking the split — but as posed it cannot be answered without information the file does not carry, and a future agent cannot even restate it accurately. Sure.

**39. L116 — "No → layer 3, this repository, under `docs/` or wherever its kind lives."**
"or wherever its kind lives" is an escape hatch that voids the rule (class d): the step gives no way to determine a file's "kind" or where that kind lives, and the manifest contains no layer-3 entry outside `docs/` and `md-review-records/` to pattern-match against. Concretely, a project-specific *script* — plainly not a `docs/` file — has no destination under this step, and the file offers no worked case. Harm: the first and most-used step of the filing rule terminates in a guess for anything that is not a document. Sure.

**40. L117 — "It lives in the repository today; when the split happens it becomes an installed artifact, and it needs the version stamp described above."**
Contradicts L48, which states the obligation is "from the first version" — i.e. now, not at the split (class a). As written, an agent creating a Layer 1 component today can read L117 as deferring the version stamp until a split that L102 says will not happen yet, which is exactly the deployment-loop failure L48 quotes the user as ruling against. Harm: the one preventive obligation this document imposes can be read as not yet in force. Sure.

**41. L118 — "Otherwise it is layer 2 — a tool, in `scripts/`, with its design document in `docs/cross-project/`."**
Two defects. (i) It mandates a design document for every layer-2 tool, with no threshold and no exception; the manifest itself shows this is not the practice — `md-review-grid.py`, `md-drift-lint.py`, `backup-health-check.py` and the launchers appear with no design document row (class f/a). (ii) The rule provides no branch for classifying a design document *itself*: run a new tool design doc through step 1 ("would a different project want this unchanged?") and a project-specific tool's design lands in layer 3, contradicting both this step and the "2 — tool's design" rows at L82–84. Harm: the filing rule cannot file the very kind of file this document is. Sure.

**42. Structural — no section defines what happens when this document and the manifest disagree**
L100 asserts the model and the manifest "stay in one document" because each is useless without the other, but where they conflict — findings 15, 16, 21, 22, 23 above are all live cases — the file states no precedence (class g, applied to the two-part structure this file defines). Harm: an agent that discovers a rule/row conflict has no instruction beyond L120's "record the ambiguity", which addresses new files, not existing contradictions. Sure that no precedence is stated; unsure whether the author considers precedence necessary.

**43. Document-wide — no YAML frontmatter**
The file has none. Noting it because the review brief asked about it, not as a defect in itself — I have no evidence this project requires frontmatter on `docs/cross-project/` documents. Unsure.

clean sections: none — every section (title/intro, "The problem this exists to settle", "What is not the problem", "The three runtimes", "The three layers", "The manifest", "The boundary: what was decided, and what is still open", "Filing rule") carries at least one finding.

