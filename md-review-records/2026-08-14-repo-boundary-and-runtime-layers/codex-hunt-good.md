<!-- provenance: runtime=codex model=gpt-5.6-sol effort=xhigh cell=defect-hunt tier=good target=/home/nedlern/Projects/nedschorus/.claude/worktrees/gatekeeper-walk-fork-continuation/docs/cross-project/repo-boundary-and-runtime-layers.md -->

1. “Ruled by the user 2026-08-14, in the walk that also hardened the box's backups.” “The walk” has no identifier, explicit path, issue, transcript, or defined meaning. The backup work does not identify which boundary decisions were actually ruled. This prevents a future reader from verifying the claimed authority or scope. Confidence: sure.

2. “It answers one question — *which repository, and which machine*.” The document does not name the proposed shared repository, choose a machine for Layer 2, or provide a cloud destination for Layer 1. Its filing rule identifies the current repository only for Layer 3. A reader therefore cannot obtain both promised answers for a new Layer 1 or Layer 2 file. Confidence: sure.

3. “That single misfiling is the evidence: there was a name for the boundary but no rule, so nothing could be checked against it.” One misfiling cannot establish that no rule existed; it could be an error under an implicit or documented rule. “Nothing” is also too broad: project specificity and the meaning of `docs/cross-project/` already permit some checks. This overstates what the example proves. Confidence: sure.

4. “Moving files between directories is cheap and reversible, and a wrong directory costs a `git mv`.” Later, the document says one such move affects 14 references, needs special approval, and is blocked on unmerged work. `git mv` also does not update references or external consumers. The sentence directly understates the cost that the document later uses to defer a move. Confidence: sure.

5. “A ‘runtime’ here means a place a Claude Code session actually runs. There are three.” The definition encompasses every place a session runs, but the exhaustive count has no project, fleet, or date qualification. A session on another workstation, CI host, development container, or future machine is an ordinary counterexample. New locations would invalidate the classification model without any rule for extending it. Confidence: sure.

6. “The box's, including `~/.claude`, seat worktrees, and `/mnt/backup`.” “Seat worktrees” is not defined. The explicitly referenced fleet document distinguishes main checkouts, agent homes, and task worktrees but does not define this combined term. A reader cannot determine which directories or lifecycle “seat worktrees” denotes. Confidence: sure.

7. “A cloud session can reach exactly what is inside the git repository it cloned, and nothing else.” Taken literally, this excludes the cloud environment’s system files, tools, generated files, dependencies, and any network-retrieved material. Even creating a new file produces something that was not inside the cloned repository. The absolute is broader than the repository-access constraint the surrounding text appears to intend. Confidence: sure.

8. “Two further facts about cloud sessions, read from the Claude Code 2.1.232 binary rather than assumed, with the detail recorded in `docs/issues/queue/45-session-seat-and-isolation-riders.md`.” The referenced file contains no occurrence of “teleport,” “cloud,” “sync,” “budget,” or version 2.1.232; its verified-facts section names Claude Code 2.1.231. Consequently, neither following fact can be checked from the stated evidence. Confidence: sure.

9. “`claude --teleport` moves a session between the cloud and a local CLI, in both directions.” The sentence attributes both directions to one CLI flag but supplies no cloud-side command or direction-specific procedure, and the cited evidence contains no teleport material. A reader can interpret this as either one bidirectional command or a feature with separate commands. Confidence: unsure because the allowed context does not expose the 2.1.232 binary needed to establish the actual command behavior.

10. “Cloud sessions sync files under hard budgets and stop rather than degrade quietly when a repository has more files than per-turn sync can track.” “Hard budgets” and “per-turn sync” are undefined, and no threshold, unit, failure signal, affected file set, or recovery behavior is supplied. Because repository size is then used as an architectural constraint, a reader cannot determine whether a repository is within the limit or recognize the described stop. Confidence: sure.

11. “Every component belongs to exactly one of these, decided by *where it must physically be at the moment it is used*.” The layers are not mutually exclusive under the document’s own definitions. A project-specific hook or skill must be available at session start, satisfying Layer 1, while a different project would not want it unchanged, satisfying Layer 3. `.claude/skills/ghi-write/` is a concrete instance: the manifest calls it Layer 1, while the filing rule’s first question sends it to Layer 3. Confidence: sure.

12. “Layer 1 — Runtime: must be present in every session.” “Every session” includes the cloud runtime defined above, but the prescribed installation is machine-local `~/.claude/`, which the document says cloud sessions do not have. The manifest consequently weakens cloud availability to “Only if in the cloned repository.” The layer’s defining requirement and its delivery rule cannot both hold. Confidence: sure.

13. “Claude Code reads these from `~/.claude/` and from the working directory's `.claude/` **at session start**. Nothing an agent does later can load them.” This conflates discovery of configuration with execution of component files. The explicitly referenced status-line script says the harness invokes it “at every refresh,” so its contents can be loaded after session start. An agent can also explicitly read or invoke a skill or script later. The blanket claim makes runtime-loaded components look as though they require startup installation. Confidence: sure.

14. “These want to be **installed to `~/.claude/` on each machine**, not vendored into every project. That is not a workaround — it is the path the harness provides.” This excludes the only delivery route the document gives for cloud sessions: repository contents. “The harness” is also unnamed, leaving unclear whether it means Claude Code, the handoff supervisor, or another system. The instruction therefore conflicts with the cloud requirement and is not executable for all three runtimes. Confidence: sure.

15. “`~/.claude/CLAUDE.md` loads into every session on the box unconditionally.” A session on the same host under another Unix account has another home directory, and a missing, unreadable, or deliberately excluded user instruction source is an ordinary counterexample. The document does not establish control over all possible sessions on the host, so “every” and “unconditionally” are too broad. Confidence: sure.

16. “Any install step here carries the same obligation: a version stamp and a staleness check, from the first version.” Neither mechanism is defined: the source of truth, version comparison, check timing, response to staleness, behavior when the source is unreachable, and interaction with automatic upgrades are all absent. A reader cannot tell when an installer satisfies this obligation or when work is complete. Confidence: sure.

17. “They need to exist on disk somewhere findable — a checkout on `PATH`.” `PATH` contains executable directories, not a checkout in the abstract. The named tools live under `scripts/`, and explicitly referenced files such as `scripts/md-drift-lint.py` and `scripts/git-gatekeeper.py` are not executable in this checkout. Adding the checkout root to `PATH` would not make them runnable by bare name. Confidence: sure.

18. “Cheapest test: **would a different project want this file unchanged?** If the answer needs a ‘well, it depends’, it is project content.” “A different project” is unspecified. A generic Python tool may be wanted unchanged by another Python project but not by a non-Python project, so the same file yields both “yes” and “it depends” depending on the imagined project. This makes the primary filing discriminator nondeterministic. Confidence: sure.

19. “Every component in the repository as of 2026-08-14, classified.” The manifest omits at least the target document itself and the checkout’s root `CLAUDE.md`. It also includes two explicitly pending paths that do not exist in this checkout. The directory aggregation for Layer 3 does not cover those omissions. The claim is therefore false on its stated date and scope. Confidence: sure.

20. “Every component in the repository as of 2026-08-14, classified.” The manifest is later called operative and the basis for a future split, but the filing rule only instructs a manifest update when classification is unclear. A newly added, clearly classified Layer 1 or Layer 2 file, a deletion, a rename, or a pending PR landing has no stated manifest-update path. The store becomes stale through normal reachable changes. Confidence: sure.

21. “Cloud can reach it?” The column uses incompatible meanings. “Only if in the cloned repository” answers whether bytes are visible; “No: it manages machine-local sessions” answers whether a visible program is useful; and “Irrelevant” answers applicability. Since the listed scripts are repository contents, the cloud can reach their bytes under the document’s own line 31 even when it cannot operate their machine-local targets. The column cannot be used consistently. Confidence: sure.

22. “`\.claude/skills/ghi-write/` | 1 — runtime.” The referenced skill repeatedly says “this project” and hard-codes `nedschorus/nedschorus`, project queues, project scripts, and project issues. A different project would not want it unchanged, so the filing rule classifies it as Layer 3. The row demonstrates the overlap that contradicts the exactly-one-layer rule. Confidence: sure.

23. “`scripts/launch-claude-ubuntu`, `scripts/launch-claude-mac` | 2 — tool.” The referenced launchers use `NEDSCHORUS_*` variables, require or name a nedschorus checkout, and on Ubuntu invoke `$HOME/Projects/nedschorus/scripts/handoff-supervisor.py`. They are not usable unchanged by a different project, so the document’s own first filing question sends them to Layer 3 rather than Layer 2. Confidence: sure.

24. “`scripts/md-review-grid.py` and its two cell runners” and “if the runtimes it shells out to exist there.” Neither cell runner nor either runtime is named in the manifest. The referenced grid source reveals `md-review-claude-cell.py`, `md-review-codex-cell.py`, Claude, and Codex, but the manifest itself replaces searchable component names with pronouns and a count. That defeats its stated per-file operational role. Confidence: sure.

25. “`scripts/git-gatekeeper.py` | 2 — tool” and “`docs/cross-project/git-gatekeeper-design.md` | 2 — tool's design.” The script states that it is the path to main “in nedschorus” and hard-codes a nedschorus workspace name, commit identity, issue, and expected GitHub pusher. The design states “How a change reaches main in nedschorus” and records project-specific credentials and protection. Neither would be wanted unchanged by a different project, so both conflict with the filing rule and the Layer 3 example “design documents about this project.” Confidence: sure.

26. “`docs/cross-project/fleet-machine-paths-and-checkouts.md` | 2 — tool's design.” The referenced document explicitly describes “the nedschorus fleet,” its exact checkout paths, users, branches, credentials, and review seat. It is project deployment content, not a reusable tool design under the document’s own unchanged-file test. Confidence: sure.

27. “`docs/cross-project/nc-python-toolchain-plan.md`” and “`docs/cross-project/nc-python-toolchain-target-architecture.md`.” Neither the target nor the two referenced documents expands “NC.” The two-letter token is particularly hard to search and conflicts with the checkout instruction requiring explicit, precise multipart names. It also prevents the filenames alone from supporting the classification the manifest says was inferred from them. Confidence: sure.

28. The four rows marked “2 — unverified” conflict with their referenced contents:

   - `docs/cross-project/nc-python-toolchain-plan.md` ties its phases to the nedschorus gatekeeper, project issues, project walks, and project accounts.
   - `docs/cross-project/nc-python-toolchain-target-architecture.md` is the corresponding project quality architecture.
   - `docs/cross-project/comms-bridge-spec.md` binds exact participants to the canonical nedschorus checkout and the old system.
   - `docs/cross-project/seed-claude-md-draft.md` calls itself the successor project’s CLAUDE.md draft and contains a `<project-name>` placeholder that cannot ship unchanged.

   Under the document’s Layer 3 test, all four require project-specific interpretation or modification; presenting “2” as their layer conflicts with that test even though the rows admit the classifications were guesses. Confidence: sure.

29. “Whoever next touches them should confirm or correct the row rather than inherit the guess.” “Touches” can mean reads, cites, edits, moves, or otherwise depends on the file. The document gives no observable record of confirmation or meaning of “confirm,” so an unrelated reader cannot know whether this obligation has triggered or ended. Confidence: unsure because “touches” commonly means edits informally, but that narrower meaning is not stated.

30. “`.claude/skills/ghi-write/SKILL.md`, which is instruction-class and needs its own walked approval.” “Instruction-class” and “walked approval” are not defined in this document, and no approval procedure or completion evidence is linked from the sentence. These concepts determine whether a ruled move may proceed, so their absence blocks execution rather than merely omitting background. Confidence: sure.

31. “Two more are being rewritten in an unmerged branch. It waits for that branch to land.” The branch is unnamed and unsearchable, “two more” has no precise file list, and “it” can mean the file or the move. There is no terminal outcome if the branch is renamed, abandoned, superseded, or lands after changing the cited paths. A future agent cannot determine whether the wait has ended. Confidence: sure.

32. “Decided: the three-layer model above, and that the manifest is its operative half.” The manifest is simultaneously incomplete and contains four explicitly unverified classifications. “Operative” can therefore mean either that the rows govern actions despite being guesses or that uncertain rows must not govern until confirmed. Those readings lead to different file movements. Confidence: sure.

33. “A split needs an install mechanism with a version check, built first.” The document does not define the mechanism, what it installs, its source and destination, the version model, its behavior on mismatch, or evidence that it is “built.” It also conflicts with the earlier comparison to the gatekeeper’s automatic upgrade by reducing the stated prerequisite to a check. The split precondition has no executable stopping point. Confidence: sure.

34. “A cloud session is bound to one repository” and “Whichever repository a cloud session clones is the one it gets.” “Bound” can mean that the session’s product metadata and teleport destination are associated with one checkout, or that the environment cannot obtain another repository’s contents. Only the second meaning supports the no-split argument, but it is not established and has ordinary counterexamples wherever another public repository or dependency can be fetched. Confidence: sure.

35. “Two repositories mean two review-and-merge lanes” and “Splitting doubles the credential work.” Two repositories can use the same identity, reusable review workflow, GitHub App, or credential scoped to both repositories. Separate histories do not entail twice the credential work. The exact doubling claim is therefore broader than the ordinary configurations allow. Confidence: sure.

36. “The manifest is what makes the split cheap when it happens: the boundary is drawn and every file is classified, so the split becomes a mechanical operation against a list rather than an archaeology exercise.” The manifest omits files, includes missing pending files, and labels four classifications unverified. The document also says one ordinary move requires 14 reference updates, special approval, and coordination with a branch. Those facts directly contradict “every file,” “cheap,” and “mechanical operation against a list.” Confidence: sure.

37. “Open, and the user's to answer: which account owns which repository. He works under two identities — `ned@lerner1.com` … and `junk@lerner1.com` … `nedschorus` currently lives under the `nedschorus` GitHub organisation and merges run as `NedLern`.” This conflates email identities, GitHub accounts, an organization that owns the current repository, and the login used to merge. It never maps either email to a GitHub owner or names the prospective second repository. Consequently, the question cannot be answered in the terms supplied. Confidence: sure.

38. “Would a different project want this unchanged? No → layer 3” followed by “Must it be loaded at session start to work? Yes → layer 1.” Because the first “No” terminates classification, a project-specific hook or skill is assigned Layer 3 without ever evaluating its startup requirement. The existing `ghi-write` row proves that the manifest uses the opposite precedence. “Under `docs/` or wherever its kind lives” also supplies no destination rule for such a project-specific script, hook, or skill. Confidence: sure.

39. “It lives in the repository today; when the split happens it becomes an installed artifact.” This is a lasting rule for future new files but uses a time-relative state that stops being true after the split. It also conflicts with the earlier statement that Layer 1 artifacts are already installed on each machine. A source file does not cease needing a source repository merely because a copy is installed. Confidence: sure.

40. “Otherwise it is layer 2 — a tool, in `scripts/`, with its design document in `docs/cross-project/`.” The catch-all includes reusable libraries, tests, configuration, schemas, templates, licenses, and reference documents that are neither startup artifacts nor tools. It also mandates a design document for every tool, while the manifest lists tools such as `scripts/md-drift-lint.py` without a corresponding design entry. The filing mechanism leaves no valid outcome for ordinary shared non-tool files. Confidence: sure.

clean sections: none
