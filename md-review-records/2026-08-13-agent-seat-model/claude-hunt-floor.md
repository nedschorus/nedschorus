<!-- provenance: runtime=claude model=claude-sonnet-5 effort=high cell=defect-hunt tier=floor target=/home/nedlern/Projects/nedschorus/.claude/worktrees/gatekeeper-walk-fork-continuation/docs/agents/agent-seat-model.md -->

I read both `docs/agents/agent-seat-model.md` in full and `docs/agents/seat-first-prompt.md` (which it explicitly points to by path). Findings below, in document order.

**1. Line 3 — "Ruled by the user 2026-08-14 after a day in which..."**
The session's current date is given as 2026-08-13, so this describes a ruling dated one day in the future as an already-completed past event. The same forward date recurs at lines 32, 36, 38, and 40 ("archived on 2026-08-14"), and is internally consistent with itself, but every instance is one day ahead of "now." Unsure — this may simply be a mismatch between the session clock and the document's real authoring date rather than an error in the document itself, but taken literally it describes past events that haven't happened yet.

**2. Lines 11–12 — "Idle agents are free. There is no cost to a seat nobody is using..."**
Stated as an unqualified fact ("no cost"), but the document's own opening line undercuts it: the whole model was created "after a day in which parallel sessions accumulated faster than anyone tracked them" — i.e., the cost of an untracked/idle session was severe enough to trigger this redesign. An idle seat also still holds a live `~/agents/<seat>` worktree and branch (line 56/38), which is disk and bookkeeping cost, not nothing. Unsure whether this is meant as literal fact or as the user's stated belief being reported neutrally (the surrounding text frames it as "the user's reasoning"), but as written it's asserted as a plain fact, not a quoted opinion.

**3. Line 18 — "two or three run at a time, five is the user's maximum"**
Supports two different readings: (a) "two or three" is the working rule and "five" is a rarely/never-hit ceiling, or (b) up to five is a normal, sanctioned operating point and "two or three" is just typical. The file gives no way to tell whether an agent advising on "should we launch a 4th or 5th seat right now" would be within norms or over them. Sure this ambiguity exists; unsure which reading is intended.

**4. Line 22 — `` `gatekeeper` | the credential road to activating the git-gatekeeper ``**
The seat is named `gatekeeper`, but the project already has a distinct thing called `git-gatekeeper` (per this checkout's CLAUDE.md: `scripts/git-gatekeeper.py check-in`, `docs/cross-project/git-gatekeeper-design.md`). Grepping "gatekeeper" across the repo will return both the seat's name and every reference to the actual git-gatekeeper program/spec, with nothing here to disambiguate them by search. Sure the collision exists; unsure whether it causes real confusion in practice.

**5. Line 32 vs. Line 40 — "Considered and declined 2026-08-14... Revisit if seats ever need to hand work to each other without the user in the loop." vs. "...the user intends to reuse the name later for a coordinating agent."**
Line 32 declines a coordinating/master seat and sets a specific condition for revisiting it (seats needing to hand off work without the user in the loop). Line 40 states the user already intends to bring back a coordinating agent under the `choirmaster` name, without saying that condition has been or will be met. Read together, it's unclear whether the "declined" decision still stands or has already been superseded by a standing intent to recreate the same thing. Unsure — this may be intentional (a future plan contingent on the stated condition) rather than a real contradiction, but the file doesn't connect the two statements.

**6. Line 36 — "A seat is retired by exiting it"**
"Exiting" a seat is used as the base operation for both ordinary pausing (line 18: "simply exited") and retirement (line 36), but no part of this file states what "exiting" actually is — no command, tool, or procedure. `ExitWorktree` exists as a tool name elsewhere in this environment but is never mentioned here. An agent told to "retire a seat by exiting it" has no executable instruction for the "exiting" half of that compound action. Sure this procedure is never defined anywhere in the file.

**7. Line 36 — "A launch that finds no handoff starts clean on the ordinary ignition prompt"**
"Ignition prompt" appears nowhere else in the file. Everywhere else this same concept is called the "first-prompt file" (lines 48, 55, 57) or `--first-prompt-file`. Nothing states these are the same thing, and "ignition prompt" is not grep-able against the term actually used in code/flags elsewhere in the document. Sure these are two different names for what appears to be one concept, with no cross-reference tying them together.

**8. Line 36 — `<seat>-handoff-retired-<date>.md`**
`<date>` format is unspecified (e.g., `2026-08-14` vs. `08-14-2026` vs. something else). Given the rest of the document uses ISO dates consistently, this is likely inferable, but the file never says so explicitly, and an incorrectly-formatted filename here isn't obviously wrong at a glance the way a malformed path or command would be. Unsure how much this matters in practice, since ISO-date convention is used everywhere else in the doc.

**9. Line 40 — "the founding seat's work is being redistributed"**
"Founding seat" is asserted about `choirmaster` without ever having been established earlier in the file — no other seat or passage says `choirmaster` was first, original, or foundational, or explains what "founding" means in this seat model (there's no stated history of seats before this document's ruling). A reader arriving at line 40 has to infer this from context rather than being told it. Unsure whether this is meant to be self-evident from the surrounding sentence, but the term itself is not defined or previously used.

**10. Line 44 — "it is the seed of a seventh pile"**
This directly conflicts with line 18 ("Seven seats are defined") and the seven-row table (lines 20–28), which already includes `sidebar` as the seventh. If seven seats already exist, a new pile that fits none of them would be an eighth, not "a seventh." Sure this is a numeric inconsistency: literally read, either the earlier "seven seats are defined" claim is wrong, or this later "seventh pile" reference is off by one.

**11. Line 55 — "after the first recycle the seat's own handoff takes over"**
"Recycle" is used as a load-bearing term (it's the event that ends first-prompt-file seeding and switches to handoff-based continuity) but is never defined in this file. `seat-first-prompt.md` mentions "the recycle hook that hands off when context runs low" in passing, but neither file states what actually triggers a recycle or what "hands off" means operationally. Unsure how much practical harm this causes, since the referenced file gives a rough gloss, but neither file fully specifies the mechanism this document depends on.

**12. Line 56 — "The launcher creates that home as an **empty directory, not a checkout**; the first-prompt file has the agent make it one on its first run"**
This directly conflicts with `seat-first-prompt.md` Step 2, which this very document's own mechanics (line 48: "the first-prompt file tells a zero-context agent everything") sends every seat to read: *"confirm your home is a checkout. The launcher makes your home a checkout of the project on your own branch before your session starts."* One file says the launcher leaves an empty directory and the agent must run `git worktree add` itself; the other says the launcher has already done the checkout and the agent should merely verify it. An agent following this document literally would expect to have to create the checkout, then find `seat-first-prompt.md` (which it's directed to read) telling it the opposite is already true. This also matches recent commit history visible in this repo's log ("launchers make a seat's home a checkout before the session starts..."), suggesting this line is stale documentation that wasn't updated after the launcher's behavior changed. Sure this is a real contradiction between the two files that make up an agent's only context; the staleness explanation is a plausible but unconfirmed cause.

**13. Line 57 — "pass `--first-prompt-file <path>` to the launcher"**
Bullet 1 (line 48, 55) establishes that "the first-prompt file tells a zero-context agent everything, including how to find out which seat it is," and that this works because "one generic file serves every seat." This later bullet reuses the same flag to seed a seat from an arbitrary different file — "another thread's context" — without saying whether that substitute file still needs to carry the bootstrapping content (find your seat name, verify checkout, read-order instructions, standing rules) that the generic file provides, or whether that content is assumed to come from elsewhere. If a custom seed file replaces the generic one entirely, the new agent may never receive the Step 1–4 onboarding this document relies on elsewhere. Sure this gap exists in what the file itself defines; unsure whether it's intended to be an obvious combination (seed file plus generic bootstrapping) or a real hole.

**14. Line 58 — "point the new seat at the `-dialog-` extract or the raw transcript instead"**
Neither "`-dialog-` extract" nor "the raw transcript" is defined anywhere in this file or in `seat-first-prompt.md`. There's no stated file path, naming convention, or location for either artifact, so an agent told to act on this sentence has no way to find what it refers to. "Forked session" is also used here without being defined or referenced earlier in the document. Sure this is unexecutable as written — there is no path, filename pattern, or procedure given for locating either artifact.

**15. Line 59 — "A seat can be exited at any time without loss once its work is pushed."**
This sits in tension with lines 36–38, which treat losing a handoff file as a real problem worth active countermeasures: handoffs must be archived rather than deleted, and a specific ordering ("retire the stream first and archive second") exists precisely to avoid losing continuity to an overwrite race. If exiting a seat truly caused "no loss" once code is pushed, these precautions about preserving handoff files would be unnecessary — handoffs are not "work" in the git sense, but the document clearly treats their loss as consequential. Unsure whether "without loss" is meant narrowly (no code loss) versus how a reader would naturally take the phrase in context (no loss of any kind), but the two passages pull in different directions on how much handoff/context loss matters.

clean sections: none

