<!-- provenance: runtime=claude model=claude-sonnet-5 effort=high cell=defect-hunt tier=floor target=/home/nedlern/Projects/nedschorus/.claude/worktrees/gatekeeper-walk-fork-continuation/docs/agents/ghi-instructions.md -->

Findings, in document order.

**1. Line 3 — "it defines the words used here — pile, seat, walked approval, instruction-class, handoff."**
Two of the five listed terms, *walked approval* and *instruction-class*, never appear anywhere else in this document — I grepped the full file and both are absent outside this sentence. *Pile*, *seat*, and *handoff* do each recur (lines 5, 7, 20, 38). The claim that all five are "used here" is therefore false for two of them. This matters because a reader is told to go read the seat-model file specifically to understand vocabulary this brief needs, but two of the five terms turn out not to be needed at all — wasted lookup, and it leaves a reader wondering whether a use was silently dropped in editing versus never existing. Sure — verified by exhaustive grep of the file.

**2. Line 5 ("what goes in a pair document") and line 20 ("promoting queue material") — used before they're defined.**
"Pair document" and the queue-material concept are only defined later, in the "doctrine" section: "Issues carry state; pair documents (`docs/issues/<n>-<slug>.md`) carry substance; queue documents (`docs/issues/queue/`) hold material whose fate is undecided" (line 30). A reader proceeding top-to-bottom hits both terms with no definition available yet. This is mitigated by the definition eventually arriving in the same file, so it's not a hard "unexecutable" defect, but it is a real forward-reference gap at first read. Unsure — I'm confident the ordering is as described, but less confident this rises to the bar the task wants flagged versus being acceptable in-document forward reference.

**3. Line 7 — "its two companion tools are designed or ruled out"** contradicts **lines 22–26, "## The companions,"** which lists three items: run-agent (#41), the reference-integrity checker (#42), and memory instrumentation (#39).
The completion criterion says "two companion tools," but the section that enumerates the companions lists three, with no marker distinguishing which two are meant or why the third is excluded from the count. A future agent cannot tell whether finishing the pile requires resolving #39 or not. This is worsened by (not required to flag, but relevant context) that `agent-seat-model.md`'s ownership table for the `ghi` row lists only `ghi-info (#46), run-agent (#41), the reference-integrity checker (#42)` — omitting #39 entirely — which is consistent with "two" but then leaves #39 unexplained as to why it's in this brief's companions list at all. Sure on the internal 2-vs-3 mismatch; unsure whether the intended fix is to drop #39 from "The companions" or to update "two" to "three."

**4. Line 13 — "It lives on the box at `~/agents/ghi-info`"**
This is exactly the home-directory convention `agent-seat-model.md` reserves for seats: "a home directory (`~/agents/<seat>`)" (agent-seat-model.md line 9), and the referenced design doc (`docs/issues/46-ghi-info-agent-design.md`, line 36) goes further and literally labels ghi-info "**Seat:** the Ubuntu box, `~/agents/ghi-info`" and has it launched via the same `scripts/launch-claude-ubuntu` seat-launcher described in agent-seat-model.md's "Launching a seat" section. But `agent-seat-model.md` states "Seven are defined" and enumerates them by name (gatekeeper, sanity-checker, skill-builder, ghi, fleet, doctrine, sidebar) — ghi-info is not among them. `ghi-instructions.md` never says whether ghi-info is an (unlisted) eighth seat, or a different kind of entity that happens to reuse the seat directory convention and launcher. Either reading conflicts with something: if it's a seat, "Seven are defined" is wrong or stale; if it isn't, the file borrows seat machinery without saying why that's safe (e.g., does it collide with any real seat's directory, does it get a supervisor and handoff files, does the "five seats running at once" ceiling count it?). Unsure — the contradiction is real and traceable through documents this file explicitly points to, but I can't rule out that the authors consider this an intentional, well-understood exception that just isn't spelled out here.

**5. Line 17 — "That plan survives marked SUPERSEDED as the record of the rejected direction — read it before proposing anything gate-shaped, so you do not re-derive a decision already made."**
"That plan" is never named — no file path, title, or issue number appears anywhere in this document. I located it by search (`docs/drafts/ghi-gatekeeper-plan-draft.md`), and its path is only given inside a dense decision-trail sentence in the separately-referenced design doc (`docs/issues/46-ghi-info-agent-design.md` line 8), not repeated here. A reader confined to this file's own text has no way to find "it" to read it, which is exactly the instruction this sentence gives ("read it before proposing anything gate-shaped"). This is an unexecutable instruction as written. Sure that no path is given in this file; unsure whether the authors consider the one extra hop through the design doc's decision trail sufficient, since that doc is cited for a different purpose (line 11) and its decision-trail sentence isn't flagged as where to find superseded plans.

**6. Line 25 — "It serves the project's axis directly: deterministic checks beat asking a model to look."**
"The project's axis" is not defined in this file, in `agent-seat-model.md`, or in `docs/cross-project/nedschorus-founding-plan.md` (the other document this file names) — I grepped all three for "axis" and found no other occurrence anywhere in the accessible context. A reader has no way to know what "the project's axis" refers to (a named principle? a section heading? a informal turn of phrase for "the project's guiding rule"?). The same sentence also uses "cited revision-paths" (line 25) without definition, though that term is more inferable from context (paths to specific revisions of a file) than "axis" is. Sure that "axis" is undefined anywhere in the material this file points a reader to; unsure whether it's explained in some other project document not referenced here.

clean sections: "## Boundaries", "## First action", "## The doctrine you work inside" (aside from being the delayed definition site noted in finding 2)

