<!-- provenance: runtime=codex model=gpt-5.6-sol effort=xhigh cell=defect-hunt tier=good target=/home/nedlern/Projects/nedschorus/.claude/worktrees/gatekeeper-walk-fork-continuation/docs/cross-project/fleet-machine-paths-and-checkouts.md -->

1. Line 3 — **“`verified-as-of: 2026-08-13`”** conflicts with line 45’s statement that several Mac paths **“have not been inspected from the box.”** “Verified” can mean either that every factual row was checked or merely that the document was reviewed while retaining acknowledged assumptions. An agent cannot tell which guarantee the frontmatter provides and may treat inferred Mac paths as observed facts. Confidence: unsure — the author may intend the weaker, document-level meaning.

2. Lines 8 and 10 — **“Where every working copy, agent home, and handoff file lives on each machine”** and **“this document is the full map behind it.”** These are broader than the document’s actual coverage. A standalone clone is an ordinary working copy that `git worktree list` and the tables would omit; later rows also omit generated handoff archives and checkout-specific memory locations. The absolute scope makes omissions look impossible, which can cause an agent to overlook real state outside the listed patterns. Confidence: sure.

3. Lines 14 and 16 — **“Agent sessions run on `ned-box`”** conflicts with **“The Mac runs agents only when the work needs to be where the user is.”** The first reads as an exhaustive location rule; the second expressly recognizes Mac agent sessions. This affects where a future agent looks for sessions, state, and unpushed work. Confidence: sure.

4. Line 14 — **“its own agent state.”** “Agent state” is undefined and materially broader than the examples that follow. It could include transcripts, task records, auto-memory, handoffs, tmux state, worktrees, or all of them. An agent deciding what must be copied or inspected cannot determine the boundary of this claim. Confidence: unsure — it may be intended only as an informal umbrella term, but it carries operational consequences here.

5. Line 14 — **“Nothing but git branches crosses between them, and only when pushed — worktrees, handoff files, locks, and credentials are per-machine and never travel.”** Taken literally, this is false. Ordinary counterexamples include SSH command and terminal traffic, pushed tags, copied patches, `scp`/`rsync`, and the Mac’s mounted view of the box home described by `/home/nedlern/.claude/CLAUDE.md`. “Per-machine” describes where state normally resides; it does not imply that its contents can never travel. The claim can cause an agent to assume that copying or remotely accessing such state is impossible. Confidence: sure.

6. Line 16 — **“The Mac runs agents only when the work needs to be where the user is.”** The absolute excludes ordinary cases such as the box being unavailable, testing Mac-specific behavior, or intentionally doing review work on the review machine. It also leaves “needs to be where the user is” without a test, so two agents can classify the same work differently. Confidence: sure.

7. Line 16 — Target definition: **“The Mac is also the review-and-merge seat: branch protection admits only `NedLern`, so merges to `main` happen from the Mac.”** Checkout `CLAUDE.md` definition: **“How a change reaches main: the git-gatekeeper (`scripts/git-gatekeeper.py check-in` — specification: `docs/cross-project/git-gatekeeper-design.md`) is the permanent path — one program, one credential, one door; agents never push to main. Until its credential work lands (activation waits on build slice 6), the gate is dormant and the interim lane applies: commit to the working branch, push it, and the user's Mac-side agent reviews and merges.”** The target presents Mac merging as the standing architecture, while the checkout instruction defines it as an interim lane that ends when the gate activates. In addition, branch protection names an account, not a physical machine, so the stated causal inference does not hold by itself. This can leave an agent following the obsolete Mac path after activation. Confidence: sure.

8. Line 20 — **“All working copies on a machine are the same repository viewed at different branches, sharing one object store through git worktrees.”** Ordinary standalone clones do not share an object store, and Git worktrees may use detached HEADs rather than different branches. The sentence never limits “all working copies” to the sanctioned linked worktrees in the table. An inventory based on this assertion will miss independent clones. Confidence: sure.

9. Line 20 — **“Git allows a branch to be checked out in only one worktree at a time, which is the mechanism that keeps parallel agents from editing one branch's files: git refuses before they get the chance.”** The restriction applies only among linked worktrees sharing one Git common directory, can be overridden with force, and is invisible across separate clones. It also does not prevent two processes from editing the same existing worktree. The file later recognizes separate clones using the same branch name, so the bold rule lacks its necessary scope. Treating it as an isolation guarantee can permit concurrent edits in one directory. Confidence: sure.

10. Line 22 — **“This is what `git pull` updates.”** `git pull` updates the checkout in which it is run, not intrinsically the main checkout. Running it in an agent home updates that agent branch; using `-C` selects still another checkout. The wording can reproduce the wrong-checkout failure this document exists to prevent. Confidence: sure.

11. Line 22 — **“what a session reads for the live `.claude/` machinery (hooks, settings). A stale main checkout means stale hooks.”** The explicitly referenced launchers start sessions in the agent home and state that project settings are read from `.claude/` in that working directory. A task-worktree session likewise reads its working tree, not the separate main checkout. Consequently, a stale main checkout does not by itself prove that an already-existing agent home or task worktree has stale hooks. This directs diagnosis and updates at the wrong checkout. Confidence: sure.

12. Line 22 — **“the instruction-file guard's worktree bug.”** The component name is searchable, but this particular bug has no path, issue, behavior, date, or durable identifier; searching “worktree bug” finds only this assertion. A future agent cannot determine which defect supposedly outlived its fix or verify the causal history. Confidence: sure.

13. Line 23 — **“one permanent directory per named long-lived agent, each on that agent's own branch. Created by the launchers (as a plain directory) and made a checkout deliberately with `git worktree add` when the agent works on the repository.”** “Each on a branch” is incompatible with the reachable plain-directory state described immediately afterward. It also disagrees with both referenced launchers, which attempt checkout creation before every new session regardless of the task and continue launching if checkout creation fails. This leaves agents unable to know whether an agent home is guaranteed to be a checkout. Confidence: sure.

14. Line 24 — **“one per background job that edits files, each on its own branch, created by the harness when a session isolates itself.”** “Harness” and “isolates itself” are undefined, no executable procedure or trigger is identified, and “background job” and “session” are used as though interchangeable. The conditional creation wording also does not establish that every editing background job actually isolates. An agent cannot create, audit, or count the promised one-per-job worktrees from this description. Confidence: sure.

15. Line 24 — **“removed when the job ends. … the directory afterwards is litter.”** If removal occurs when the job ends, there is no directory afterward. If “afterwards” instead means after commits are pushed but before cleanup, the temporal boundary is unstated. These readings lead to opposite cleanup expectations. Confidence: sure.

16. Line 24 — **“Their value leaves as pushed commits.”** Jobs can crash, leave dirty or ignored files, fail to commit, or fail to push. The described cleanup mechanism neither states nor discards those reachable cases, and removal can then discard the only copy of the work. “Value” is also undefined: it could include generated artifacts or investigation notes that are not commit material. Confidence: sure.

17. Line 33 — **“Agent home — the founding seat.”** “Founding seat” is neither defined nor a durable, searchable project name; the actual identity is `choirmaster`. A future agent cannot tell whether this row has different lifecycle rules or is merely historical labeling. Confidence: unsure — the phrase may be decorative, but the table presents it as a distinct checkout kind.

18. Lines 34–36 — **“`/home/nedlern/agents/<name>`,” “`.../worktrees/<name>`,”** and **“`.../handoffs/<agent>-handoff.md`.”** The document does not define whether `<name>` and `<agent>` are identical, nor what `<name>` means for a task worktree. It also gives no permitted character set or relation between a task directory name and its branch. The current checkout itself demonstrates why inference is unsafe: its directory name and checked-out branch name differ. This prevents deterministic path construction and search. Confidence: sure.

19. Line 36 — **“Handoff files | `/home/nedlern/.claude/handoffs/<agent>-handoff.md`.”** The referenced launcher/supervisor machinery also creates numbered handoff archives such as `<agent>-handoff-0004.md`; the live directory contains such files. Because the row is plural and the introduction promises every handoff file, it supports the false reading that the unsuffixed file is the only handoff artifact. Cleanup or recovery based on the row can miss archived generations. Confidence: sure.

20. Line 38 — **“Auto-memory | `/home/nedlern/.claude/projects/-home-nedlern-Projects-nedschorus/memory/`.”** “Auto-memory” is undefined, and this encoded path corresponds only to the main checkout’s working-directory key. Agent homes and task worktrees receive different project keys; the box already contains memory under other encoded locations, including an agent-home key. A session in an agent home can therefore read or write a different memory store while the table appears to identify the fleet-wide one. Confidence: sure.

21. Line 39 target definition: **“Legacy reference system | `/home/nedlern/Projects/nedlern` | absent on this box.”** Checkout `CLAUDE.md` definition: **“The legacy system at `~/Projects/nedlern` is read-only reference: read anything there freely; NOT: modify anything there or execute its code.”** In this checkout `~` resolves to `/home/nedlern`, so one definition says the repository is available for reading while the other says it is absent. An agent following the checkout instruction will attempt an impossible read; an agent following this document may disregard a mandatory reference. Confidence: sure.

22. Line 41 — **“the git-gatekeeper's `--import` machinery refuses `import-invalid` (‘not a readable git repository’) until one exists, which is a named refusal rather than a failure.”** The file does not give a complete command, program path, or explain that `--import` is part of a `check-in` invocation. A named `import-invalid` refusal is still a non-success outcome, so “rather than a failure” supports incompatible operational readings: expected safe rejection versus successful completion. An agent cannot tell whether to stop, retry, or treat the import as completed. Confidence: sure.

23. Line 41 — **“Clone it only when an import is actually needed.”** This directs work but supplies neither the legacy repository’s source URL nor any other source location from which the absent checkout can be cloned. The nedschorus remote does not identify the legacy repository. With the deliberately limited context, the instruction is not executable. Confidence: sure.

24. Line 45 — **“The checkout path is pinned by the repository's own tests (`scripts/handoff-extract-conversation-test.py`).”** The named test merely passes `/Users/el/Projects/nedschorus` as a literal input and asserts how that string is encoded into a Claude project-directory name. It neither checks that a checkout exists there nor requires the repository itself to run from that path. Calling the path “pinned” gives an unverified convention the authority of an enforced invariant. Confidence: sure.

25. Line 60 — **“List agents running on a machine | either | run the matching launcher with no name.”** The preceding rows say both launchers are typed on the Mac, including the launcher that lists box sessions over SSH. “Either” can instead be read as running a launcher locally on either machine, and “matching launcher” does not identify which command to use in either case. On the box, running the Mac launcher would inspect the box’s tmux server under a misleading name. Confidence: sure.

26. Line 61 — **“See every checkout and its branch | either | `git -C ~/Projects/nedschorus worktree list`.”** The command lists only worktrees registered to that machine’s selected clone. It cannot show the other machine’s checkout or an independent local clone. “Every checkout” therefore exceeds the command’s scope and can make unlisted working copies appear not to exist. Confidence: sure.

27. Line 65 — **“Both launchers are attach-or-create: running a name that is already up attaches to that agent rather than starting a second copy.”** Both referenced launchers support `--no-attach`, which explicitly does not attach, and a supervised agent adopted outside tmux can hold the name’s lock without providing a tmux session to attach to. The sentence neither scopes itself to the default invocation nor states these reachable cases. Confidence: sure.

28. Line 65 — **“Within one machine, duplicate agents are impossible three ways.”** Tmux prevents duplicate session names only inside one tmux server; it can be bypassed. The supervisor lock prevents duplicate supervisors, not arbitrary unsupervised or background agent processes. Deriving a directory from a name prevents neither two processes from using that directory nor forks from sharing it. The absolute guarantee is therefore false and can cause agents to skip collision checks. Confidence: sure.

29. Line 65 — **“the supervisor holds an exclusive per-agent lock (reclaimed if its holder died).”** The referenced supervisor identifies the holder only by a stored PID. PID reuse can make a dead holder appear alive, while a malformed or unreadable lock is removed even if a supervisor is actually alive. Those reachable identity and corruption cases are neither stated nor discarded, despite the sentence presenting reclamation as categorical. Confidence: sure.

30. Lines 65 and 69 — **“the same name on both is simply two unrelated agents: no conflict”** and **“two clones pushing the same branch name fight over one remote ref.”** The referenced launchers derive each agent’s branch from its agent name. Thus, using the same name on both machines normally produces exactly the same branch name in separate clones, creating the remote-ref collision described four lines later. **“nothing requires”** the suggested `-mac`/`-ubuntu` suffixes is consequently false whenever both same-named agents may push. Confidence: sure.

31. Line 69 — **“Crosses: git branches, once pushed, and through them every committed file. That is also the one real cross-machine collision surface.”** Git can also push tags and other refs, and files or patches can cross through ordinary copy or the mounted box home. The remote branch ref is not the only collision surface: different branches can modify the same lines and conflict at merge, and agents can mutate shared GitHub issues or other remote state. The absolute inventory hides real coordination hazards. Confidence: sure.

32. Line 69 — **“two clones pushing the same branch name fight over one remote ref.”** Two pushes do not necessarily “fight”: sequential fast-forward pushes can both succeed. Divergent histories normally produce a non-fast-forward rejection, while force-push changes the outcome again. The undefined verb and omitted conditions prevent an agent from knowing which behavior or failure is being asserted. Confidence: sure.

33. Line 71 — **“Does not cross: worktrees and their directories, agent homes, handoff files, supervisor locks, tmux sessions, Claude credentials … and anything uncommitted.”** This is only defensible as “does not automatically cross through Git,” but that qualification is absent. Files and uncommitted patches can be copied, and the checkout’s user-level instruction explicitly says the Mac mounts the box’s home. The absolute can cause an agent to dismiss cross-machine access or accidental copying that actually occurred. Confidence: sure.

34. Line 73 — **“pulling the main checkout does not update agent homes or task worktrees.”** Pulling changes only the current checked-out files, but its fetch updates objects and remote-tracking refs in the shared Git repository, which are immediately visible from linked agent homes and task worktrees. “Update” therefore has two incompatible readings. An agent may wrongly believe `origin/main` is still stale everywhere else. Confidence: sure.

35. Line 73 — **“After a merge to `main`, the main checkout needs its own pull.”** This is not universally true on the machine where a local main checkout performed and pushed the merge, nor when it was updated by another Git operation. The missing remote-versus-local scope turns a common workflow case into a counterexample. Confidence: sure.

36. Line 73 — **“any long-lived agent branch needs `main` merged into it before that agent sees the change.”** An agent can see the change by inspecting `main`/`origin/main`, rebasing, cherry-picking, resetting, or receiving a fast-forward before launch. The referenced supervisor itself fetches and fast-forwards eligible branches. “Any,” “needs,” and “before … sees” overstate one integration method as the only possible visibility mechanism. Confidence: sure.

clean sections: none.
