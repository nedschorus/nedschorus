<!-- provenance: runtime=claude model=claude-sonnet-5 effort=high cell=defect-hunt tier=floor target=/home/nedlern/Projects/nedschorus/.claude/worktrees/gatekeeper-walk-fork-continuation/docs/cross-project/fleet-machine-paths-and-checkouts.md -->

Read the target file in full (with frontmatter) plus the project `CLAUDE.md`, and traced several claims against the files this document itself references by explicit path (`scripts/handoff-extract-conversation-test.py`, `scripts/launch-claude-ubuntu`, `scripts/launch-claude-mac`, `scripts/git-gatekeeper.py`) and against the legacy checkout's actual presence on this box.

1. **"Agent sessions run on `ned-box`, an Ubuntu machine on the same LAN, reached as `ssh nedlern@ned-box`."** (line 14) — Stated as an unqualified fact, but two sentences later the same paragraph says "The Mac runs agents only when the work needs to be where the user is," and the Commands table (line 59) gives a command to "Run a named agent on the Mac." Taken literally, "agent sessions run on ned-box" is false whenever a Mac agent is running — of which the document itself gives ordinary examples (Mac browser/keychain/GUI work, and the review-and-merge seat). Sure.

2. **"reached as `ssh nedlern@ned-box`"** (line 14) — This is presented as the literal command to reach the box. But `scripts/launch-claude-ubuntu`, which this same document's Commands table (line 58) points to by explicit path, defaults its SSH target to the host alias `ned`, not `ned-box`: `AGENT_BOX="${NEDSCHORUS_AGENT_BOX:-ned}"`, and all its `ssh` invocations use `"$AGENT_BOX"`. If `ned` and `ned-box` are meant to be the same SSH-config alias, the document uses a different string than the tool it documents; if they're different aliases, the document never says so, and a reader who types the literal `ssh nedlern@ned-box` command this file gives may reach a different host (or no host, if only `ned` is configured) than the launcher script actually connects to. Sure of the mismatch as written; unsure whether it's a real operational bug or two intentionally-distinct aliases, since I can't inspect the box's `~/.ssh/config`.

3. **"The Mac is also the review-and-merge seat: branch protection admits only `NedLern`, so merges to `main` happen from the Mac."** (line 16) — "NedLern" is introduced with no definition and no stated relationship to any other identity named in this document or in `CLAUDE.md` (the Ubuntu OS/SSH user is `nedlern`, lowercase, used throughout; the Mac OS user is `el`). A reader can't tell whether "NedLern" is a GitHub account distinct from both, or a re-casing of `nedlern`. This also creates a reading ambiguity against `CLAUDE.md`'s "the user's Mac-side agent reviews and merges": is the merge performed by the human user under the identity "NedLern," or by a Claude agent running on the Mac using that same credential? The sentence doesn't say which, and the two readings have different implications for who/what is actually authorized to merge to main. Sure the term is undefined in the available context; unsure which reading is intended.

4. **"A stale main checkout means stale hooks, which is how the instruction-file guard's worktree bug outlived its fix by a day."** (line 22) — "the instruction-file guard's worktree bug" is referenced with no explanation of what the guard is, what the bug was, or a pointer to where either is documented. A reader with only this file and `CLAUDE.md` has no way to resolve what happened or why it's a cautionary example. Sure this reference is unresolvable from the given context.

5. **"Their value leaves as pushed commits; the directory afterwards is litter."** (line 24) — This sentence only makes sense as a caution if the directory can, in practice, outlive the job (the preceding clause already claims it is "removed when the job ends," which would make it not-yet-litter by definition). The document defines the task-worktree lifecycle and asserts automatic removal, but never states what happens if that removal doesn't happen (a crashed job, a killed session) or what, if anything, cleans up an orphaned task-worktree directory. The later "Clear stale worktree registrations… `git worktree prune`" command doesn't obviously cover this case, since `git worktree prune` removes administrative metadata for worktrees whose directory is already gone — it doesn't delete a directory that's still present. Unsure whether this is a real gap or whether removal is in practice unconditional (I have no path in this file to the harness's cleanup code to check), but the sentence itself implies a state ("litter") the file never tells the reader how to detect or clear.

6. **"Agent home — the founding seat | `/home/nedlern/agents/choirmaster` | `choirmaster`"** (line 33) — "the founding seat" is used as if it were an established term, but nothing in this file defines it or explains why choirmaster's agent home is categorically different from "Agent homes — general" on the very next row. Sure this phrase is unexplained within the given context.

7. **"Legacy reference system | `/home/nedlern/Projects/nedlern` | **absent on this box**"** (line 39) — This conflicts with `CLAUDE.md`'s "The legacy system at `~/Projects/nedlern` is read-only reference: read anything there freely," which presupposes the path is there to be read. A future agent following `CLAUDE.md`'s instruction to "read anything there freely" will find nothing at that path on this box, and only this separate document (not `CLAUDE.md` itself) explains why. I confirmed the path is in fact currently absent on this box, so the fleet doc's factual claim is accurate — the defect is the unreconciled conflict between the two files' assumptions, not the fact itself. Sure.

8. **"Clone it only when an import is actually needed."** (line 41) — No source is given anywhere in this file for what to clone from (no remote URL, no backup location). An agent trying to follow this instruction has a destination path (`/home/nedlern/Projects/nedlern`) but no origin. Sure this file supplies no clone source.

9. **"The checkout path is pinned by the repository's own tests (`scripts/handoff-extract-conversation-test.py`)"** (line 45) — I read the referenced test file. The only occurrence of `/Users/el/Projects/nedschorus` there (line 234) is used as an arbitrary example input to a generic path-mangling helper (`project_directory_for_working_directory`), verifying that function's general behavior — it is not a check that the real Mac checkout must live at that path, and the test's own docstring states "the tests never read a real session and never touch `~/.claude`." Changing the actual Mac checkout location would not make this test fail. The claim that the path is "pinned" by this test is incorrect as written. Sure, based on reading the referenced file.

10. **Frontmatter `verified-as-of: 2026-08-13`** (line 3) versus **"the remaining rows follow the same conventions and have not been inspected from the box"** (line 45) — The frontmatter attaches a single document-wide verification date, and the Ubuntu section reinforces this with its own "Verified 2026-08-13." heading (line 28), but the Mac section explicitly disclaims verification for most of its own rows. If `verified-as-of` is meant to certify the document's content as checked, the Mac section's own text contradicts that for everything but the checkout-path row. Unsure whether `verified-as-of` is meant as a whole-document claim or merely a last-touched date — the file doesn't say which, and the two readings lead to different conclusions about whether the Mac rows are trustworthy.

11. **Mac table (lines 47–52)** — The Ubuntu table includes rows for "User-level instructions" and "Auto-memory" location; the Mac table has neither, and the file gives no note that these don't exist on the Mac, aren't tracked, or were simply omitted. A reader working from the Mac side has no way to tell, from this file, whether that information is absent by design or just missing from the table. Unsure whether this is an intentional scope narrowing or an oversight, but the asymmetry itself is unexplained.

12. **"Within one machine, duplicate agents are impossible three ways: tmux attaches by session name, the supervisor holds an exclusive per-agent lock (reclaimed if its holder died), and an agent's home directory is derived from its name rather than chosen."** (line 65) — Two issues here. First, "(reclaimed if its holder died)" asserts a specific reclaim-on-death mechanism that is not explained anywhere in this file, and is not corroborated by the two launcher scripts this document's own Commands table points to by path (`launch-claude-ubuntu`, `launch-claude-mac`), whose comments only say the lock "refuses a duplicate… even if tmux is bypassed" — neither mentions death detection or reclaim. Second, "impossible" is an absolute claim resting on that unexplained mechanism; an ordinary edge case (a lock held by a process that is merely slow or hung, not dead, or a race between two near-simultaneous launches before the lock is taken) is not clearly excluded by anything stated here. Sure the parenthetical is unsourced within the given context; unsure whether "impossible" actually holds, since verifying it requires the supervisor's own code, which this file doesn't path to.

clean sections: What crosses between machines, and what does not

