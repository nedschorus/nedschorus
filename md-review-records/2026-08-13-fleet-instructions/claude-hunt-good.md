<!-- provenance: runtime=claude model=claude-opus-5 effort=high cell=defect-hunt tier=good target=/home/nedlern/Projects/nedschorus/.claude/worktrees/gatekeeper-walk-fork-continuation/docs/agents/fleet-instructions.md -->

I read `docs/agents/fleet-instructions.md` in full (40 lines, no YAML frontmatter), the checkout's `CLAUDE.md`, and the one document it references by explicit path, `docs/agents/agent-seat-model.md`.

---

**1. Line 3 — "Your pile: **the machinery that runs agents**"**
"Pile" is a project-specific term with a precise meaning (`agent-seat-model.md` line 14: "**Pile** — the body of related work a seat owns. A subject area with shared context, not an ordered queue"). This file uses it in its first three words without definition; the pointer to where it is defined comes in the next clause. A reader who takes "pile" in its ordinary sense reads it as a stack of pending items to work through, which is the exact reading the model explicitly rules out — and that misreading shapes how the whole brief is used, because it turns "Your queue" and the issue list into a to-do list. Confidence: unsure, because the definition is one link away in the same sentence; the defect is ordering and reliance on the reader following the link before parsing the sentence they are already in.

**2. Line 3 — "you own that model's implementation"**
This is broader than anything else in the file or the model supports, and it conflicts with the enumeration in the same sentence ("launchers, handoffs, session isolation, and the tooling around keeping several agents straight"). `agent-seat-model.md` contains much that is not this seat's: the grouping rule, the filing rule, the retirement procedure, and a seat table whose rows assign the walked-approval evidence format to `gatekeeper` and the md-review grid to `sanity-checker`. The model's own row for this seat reads `fleet` | "session and agent machinery: the launchers, the handoff supervisor, seat isolation", and the model states "Each row's brief is the authority on what that seat owns" — so a brief claiming ownership of the whole model's implementation asserts authority over other seats' work. Harm: an agent obeying the broad reading will pick up the instruction-file guard, retirement tooling, or seat-naming work that belongs elsewhere, and two seats then edit the same files. Confidence: sure.

**3. Line 7 — "awaiting the Mac-side review seat"**
"Seat" is a defined term, and this usage contradicts the definition. `agent-seat-model.md` line 9: "One place the word is used loosely and deliberately: the **Mac-side agent**, which reviews branches and merges them to `main`, is the user's own agent on his Mac and is not one of the seats defined here." `CLAUDE.md` line 8 names the same thing "the user's Mac-side agent". This file invents a third label that asserts the opposite of the model's explicit carve-out. Harm: a reader who believes the reviewer is a seat will expect it to have a home at `~/agents/<name>`, a branch, a brief, and a supervisor, and may try to launch it or hand it work through seat machinery — none of which exist for it. Confidence: sure.

**4. Line 7 — "(both teach on the no-name case and list running agents)"**
"Teach on the no-name case" is not explained anywhere in this file. Neither "teach" (print usage? print an explanation of the seat model? refuse with guidance?) nor "the no-name case" (invoking the launcher with no argument? a seat whose `CLAUDE.local.md` has no name?) can be resolved from the text. Harm: this is the PR's stated behavioural contract; an agent asked to verify the launchers (line 39 asks exactly that) has no way to know what behaviour to check for. Confidence: sure.

**5. Lines 7, 18, 39 — "the session riders" / "rider 1's obvious detection method" / "ask which rider he wants next"**
"Rider" is never defined in this file, and it is not in `agent-seat-model.md`'s vocabulary section, which exists precisely to define "the words this model uses". Line 18 calls the same things "five ideas discussed and deliberately not built"; nothing tells the reader that an idea in that file *is* a rider. Two separate problems follow. First (undefined term): an agent cannot tell whether "the session riders" on line 7 is a set of files, a section inside `45-session-seat-and-isolation-riders.md`, or something else, so it cannot tell what the PR froze. Second (name quality): "rider" is a single common English word that will match large amounts of unrelated text on grep, and `rider 1` is a positional identifier with no textual content — if the queue file is reordered or an item is inserted, every reference in this brief silently points at the wrong item, and there is no way to search for the concept "the `/proc` detection idea" by name. `CLAUDE.md` line 7 asks for "explicit, clear and precise multi-part names ... likely to be grepped". Confidence: sure.

**6. Line 7 — "the fleet work inventory"**
Given as a contents item of the PR with no path, while the neighbouring items in the same list do carry paths (`scripts/launch-claude`, `docs/cross-project/fleet-machine-paths-and-checkouts.md`). The phrase is not the artifact's name — the file in this checkout is `docs/issues/queue/45-ubuntu-fleet-open-work-inventory.md` — so searching for "fleet work inventory" as written does not reliably lead to it. Harm: the same sentence ends with "Nothing else should touch those files", so an agent must resolve every item in this list to a concrete path in order to obey the freeze, and this item cannot be resolved from the file. Confidence: sure.

**7. Line 7 — "**Nothing else should touch those files until it merges.**"**
Three defects in one sentence.
- Un-executable: "those files" is only partly enumerable. "the session riders", "the fleet work inventory", and "these seat briefs" name no paths (see findings 5 and 6), so the forbidden set cannot be computed.
- Self-freezing: "these seat briefs" includes this file. The brief therefore forbids its own correction for as long as PR #57 is open, including correction of the defects listed here.
- No exit other than the happy path: the condition is "until it merges". If the PR is rejected, closed, superseded, or simply sits unreviewed for weeks, the freeze never lifts and the file says nothing about what to do. Since the Mac-side reviewer is outside this seat's control, this is a reachable and likely case.
It also conflicts with the rest of the brief: line 39 tells the agent to "ask which rider he wants next" when the PR has *not* merged, and the riders live in the session-rider material this same sentence freezes — so the instructed next action is the forbidden one. Confidence: sure.

**8. Lines 11–12 — "**Launchers** (`scripts/launch-claude-{ubuntu,mac}`)" and "Since 2026-08-13 it **syncs the agent's branch with main before each launch**"**
"The state of the machinery" describes both as present fact, but line 7 says the rename to `launch-claude-ubuntu`/`launch-claude-mac` and "the supervisor's branch sync" are contents of PR #57, which has not merged. On `main` — and therefore in any fresh seat home created from `main` — those paths do not exist and that behaviour is absent; `scripts/launch-claude` does. Harm: an agent that trusts the state section will run a launcher path that is not there, or will assume its branch was fast-forwarded when it was not and start work on a stale tree. The file gives no signal about which of the listed machinery is live and which is pending review. Confidence: sure.

**9. Line 11 — "identity from a `CLAUDE.local.md` there" vs "The name typed is the whole configuration — no roster"**
These support incompatible readings of where a seat's identity comes from. If identity is read from a per-home `CLAUDE.local.md`, then the name typed is not the whole configuration — a second file must exist and must be correct. `agent-seat-model.md` line 91 gives a third account: "the agent learns its own name from its working directory, so nothing needs substituting." The file also leaves the mechanism incomplete: nothing states who writes that `CLAUDE.local.md`, when, or what a session does if it is missing, empty, or names a different seat than the directory does. Harm: on the first launch of a new name, or after a seat home is recreated, the agent cannot tell whether it must author its own identity file, and cannot tell which source wins on conflict. Confidence: sure about the internal conflict; unsure only about which of the three accounts is actually implemented.

**10. Line 12 — "otherwise report and change nothing"**
The reporting channel is unspecified: stdout of a supervisor process that runs before the session starts is not visibly delivered to anyone by anything this file describes, and no log file, marker, or in-session message is named. Harm: the failure case here is exactly the important one — the branch is dirty or diverged and was *not* synced — and an agent has no stated way to learn that it happened, so it proceeds believing the sync guarantee holds. Confidence: sure that the channel is unstated; unsure whether an obvious channel exists in the script.

**11. Line 12 — "never runs on the adoption path"**
"The adoption path" appears once, undefined, with no cross-reference. Nothing in this file or `agent-seat-model.md` explains what adopting is (attaching to an already-running session? a supervisor taking over a session it did not launch?). Harm: this clause is the stated boundary of the sync behaviour, so an agent debugging a branch that failed to sync cannot determine whether it was on the excluded path or hit a real bug. Confidence: sure.

**12. Line 12 — "changing files under a live agent is the one forbidden act"**
An absolute uniqueness claim the file itself contradicts at least three times. Line 14 describes edits to `CLAUDE.md` and `.claude/` without walked approval as blocked — a second forbidden act. Line 7 forbids touching PR #57's files. Line 35 states an invariant with "must never". `CLAUDE.md` line 8 adds "agents never push to main". Harm: an agent that internalises "there is exactly one forbidden act" treats every other prohibition in the brief as advisory. `CLAUDE.md` line 6 warns specifically about this: "absolute imperatives like 'always' or 'never' can backfire in unforeseen conditions. Use them cautiously." Confidence: sure.

**13. Line 13 — "a Stop hook firing at 50% context, wired into project settings 2026-08-12"**
"50% context" is ambiguous between 50% consumed and 50% remaining; these are the same number only by coincidence of this particular threshold and read differently as soon as anyone tunes it. "Project settings" names no file, so an agent asked to check or change the wiring has no path to open. The mechanism is also left open at its failure point: the file does not say what the hook does when it fires (blocks the stop? injects a message?) or what happens if the agent declines to hand off — `agent-seat-model.md` line 12 says only that it "asked it to", which implies the agent can refuse, and the consequence of refusal is unstated. Harm: the recycle trigger is the mechanism that keeps seat threads from being lost, and this is the only description of it. Confidence: sure on the ambiguity and the missing path; unsure how much of the fire-time behaviour the author intended to describe here.

**14. Line 14 — "blocks edits to CLAUDE.md and `.claude/` machinery without the user's walked approval, consumed through a `.walk-approved` marker"**
Two gaps. First, the protected set stated here is narrower than the defined class it is enforcing: `agent-seat-model.md` line 17 defines "**Instruction-class** — files that tell agents how to behave: `CLAUDE.md`, `CLAUDE.local.md`, anything under `.claude/`." A reader of this brief alone concludes `CLAUDE.local.md` is unprotected — which matters directly, because line 11 of this same file makes `CLAUDE.local.md` the carrier of a seat's identity. Second, the marker mechanism is left with reachable cases unstated: the file does not say where `.walk-approved` lives, what "consumed" does to it (deleted after use? cleared?), whether it authorises one write or many, or what happens when a marker is present but was written for a different file than the one being edited. Harm: an agent that needs an approved edit cannot produce the marker from this description, and cannot tell whether a stale marker will silently approve the wrong write. Confidence: sure.

**15. Line 18 — "Solve detection before building that guard."**
This assigns open-ended work with no completion criterion. The paragraph establishes that the obvious method failed and gives the reason, but nothing states what "solved" means — a method that works for attached sessions only, for forks and background jobs too, with what false-positive tolerance, verified how. Harm: line 39 routes the user straight back to rider selection, and a user who picks rider 1 hands the agent a task it cannot know it has finished; per `agent-seat-model.md` line 56, "an agent with no completion criterion either invents adjacent work or stalls". Confidence: sure.

**16. Line 20 — "[#33](…) (fast-handoff pickup superseded)"**
The parenthetical parses two ways: the issue's subject is "fast-handoff pickup" and the issue has been superseded, or the issue's subject is the fact that fast-handoff pickup was superseded by something. The list's other parentheticals are all subject labels ("named agents", "worktree file hygiene"), which pulls toward the second reading, while the word order pulls toward the first. Harm: the two readings imply opposite actions — one says the issue is dead and should be closed or ignored, the other says it is live work in the pile. Nothing else in the file disambiguates, and the adjacent mention of PR #52 ("fast-handoff findings applied") touches the same topic without saying how the two relate. Confidence: sure.

**17. Line 20 — "[#37](…) (turn/steer equivalents)"**
"Turn/steer" is unexplained and not a standard term; "equivalents" gives no clue what they would be equivalent to. Nothing in this file, or in `agent-seat-model.md`, uses either word elsewhere. Harm: the label is the only summary the brief offers, and it does not let an agent judge whether the issue is relevant to a task in hand without opening GitHub — which the file elsewhere assumes may not be reachable. It is also not searchable as a concept. Confidence: sure.

**18. Line 20 — "coordinate with `sanity-checker`, which shepherds it"**
No channel exists for this. `agent-seat-model.md` line 76 states it directly: "a seat's only channel to another seat is through him" — the user — and line 32 of that file confirms seats do not hand work to each other. This brief names no mechanism (no shared file, no message tool, no procedure for asking the user to relay). Harm: an agent instructed to coordinate will either attempt to reach a seat it cannot reach, or — worse — attach to `sanity-checker`'s session or edit its files, which collides with the standing invariant on line 35 and with the branch-separation reasoning in the model. Confidence: sure.

**19. Line 27 — "`Ctrl+T` pins against the ~1-hour idle reap"**
"The idle reap" is introduced as established fact but is never described: what does the reaping (the Claude Code harness? tmux? a cron job?), what does it reap (the session, its transcript, its job directory?), and what is lost when it happens. Harm: this is offered as an expensive-to-learn operational fact, and its value is entirely in knowing when pinning is worth doing; without knowing what is destroyed, an agent cannot judge that, and cannot tell whether an unreachable session was reaped or crashed. Confidence: sure.

**20. Line 28 — "It lists **only the machine it runs on**; the box needs `ssh nedlern@ned-box -t 'claude agents'`."**
Wrong when obeyed literally by this file's stated reader. This is a seat brief, addressed throughout in the second person to an agent occupying the `fleet` seat, whose home is `~/agents/fleet` on the box (line 11) — that agent running `ssh nedlern@ned-box` connects to its own machine, adding a hop and a nested tmux/TUI for no gain. The sentence is written from the Mac's point of view without saying so, in a file that otherwise never states which machine the reader is on. Secondarily, `-t` requests an interactive terminal for a full-screen TUI, which an agent capturing command output cannot drive. Harm: the reader cannot tell whether this line is an instruction for itself or a line to hand to the user, and the two have different correct forms. Confidence: sure about the ambiguity; unsure only whether the author intended the whole "Session-management facts" section as user-facing notes.

**21. Line 29 — "**No side-by-side view exists.** Simultaneous views mean one terminal per session."**
The absolute claim is broader than holds, and the ordinary counterexample is the tooling this very file is built on: sessions run inside tmux (line 11, "attach-or-create by tmux session name"; `agent-seat-model.md` line 95 documents `Ctrl-b d` to detach), and tmux split panes put two attached sessions side by side in one terminal. The intended claim is presumably narrower — that `claude agents`, the subject of the preceding two bullets, has no built-in split view. As written it also contradicts its own second sentence, which concedes that simultaneous views *are* possible, just at one terminal each. Harm: an agent takes a solved display problem as impossible and does not use the mechanism already in the stack. Confidence: sure.

**22. Line 30 — "check its transcript's modification time under `~/.claude/projects/`"**
The procedure is incomplete at the step that matters: nothing says how to get from a session (or a job id, or a seat name) to its transcript file inside that directory, and the preceding bullet establishes that the two identifiers in play — job id and session UUID — are different things. Harm: an agent trying to tell a genuinely idle session from a busy one, which is the stated purpose of the fact, is left listing a directory of transcripts it cannot attribute. Confidence: unsure — the mapping may be obvious once the directory is opened, but this file, which is explicitly a store of facts "expensive to learn, easy to lose", does not record it.

**23. Line 31 — "Non-interactive SSH shells did not see `~/.local/bin` until `~/.bashrc` was fixed on 2026-08-13 to export PATH above its non-interactive early-return."**
The machine is not named, in a file that elsewhere insists two machines are in play (line 28, and `agent-seat-model.md`'s Mac/box split). `~/.bashrc` on the Mac and on the box are different files with different contents, and the fix applies to one of them. The fact is also left open at a reachable limit: this edit lives in a machine-local dotfile that is not in git, so a rebuilt box, a new user account, or a dotfile restore silently reverts it and the symptom returns — the file records the fix but not that it is unprotected, which is the part a future agent would need in order to recognise the recurrence. Confidence: sure about the unnamed machine; unsure whether the author considered the durability point in scope for a "facts" list.

**24. Line 35 — "**Two live sessions must never share a working directory.** … `EnterWorktree` relocates a running session correctly — the gap is the trigger, not the capability."**
Several problems in one paragraph.
- The invariant is stated as absolute and is, by this file's own account, unenforced: line 18 records that the detection method was tried and "proved unreliable", and that the guard is unbuilt. An invariant nothing checks is a hope; the file presents it as a guarantee, in a section headed "Your standing invariant".
- `EnterWorktree` is introduced with no explanation of what it is — a tool available to a session, a script, a git command — and no path or invocation. It is the only remedy named, and it cannot be acted on.
- "the gap is the trigger, not the capability" is compressed past the point of recovery: "the trigger" has no antecedent. The intended meaning is presumably that nothing detects the violation and so nothing invokes the relocation, but a reader cannot derive that from the words.
- No remedy procedure is given for the case the paragraph exists to describe: having noticed (by whatever means) that a fork or background job now shares a directory, what should the agent do, and which of the two sessions moves.
Confidence: sure.

**25. Line 39 — "If it has, verify the renamed launchers work from the Mac and tell the user."**
Not performable by the reader, and unbounded. The seat runs on the box (line 11 places seat homes at `~/agents/<name>`; line 28 treats the box as "here" or "there" ambiguously but places the agent on one machine, not both). Running the Mac launcher requires a shell on the Mac, which this file gives the agent no way to obtain — and `agent-seat-model.md` line 84 states the launch command is issued "From the **Mac**, using the Mac's own checkout of this repository", i.e. by the user. Separately, "verify … work" has no criteria and no stopping point: which of attach-or-create, the no-name teaching, the running-agent listing, the box-side path handling, and the first-prompt seeding must pass, and on which machine, is unstated, so the agent cannot know when verification is complete. Confidence: sure on the criteria; sure on the machine problem unless the seat has a Mac shell this file never mentions.

**26. Line 39 — "Check whether PR #57 has merged."**
No means is given. The file names no command, tool, or credential for querying GitHub, and `CLAUDE.md` establishes that this project's agents do not own the path to `main` — merging is done by the user's Mac-side agent — so network access to GitHub from this seat is an assumption, not a stated fact. Harm: this is the brief's very first instruction, and a fresh agent that cannot reach GitHub has no stated fallback (checking whether the branch's commits are present in the local `main` after a fetch, asking the user) and stalls at step one. Confidence: unsure — `gh` may well be installed and authenticated on the box; the defect is that the file's own first action depends on an unstated precondition.

**27. Whole file — no completion criterion for the seat**
`agent-seat-model.md` line 58 states the shared expectation: "Each brief states its own criterion" for when a seat's pile is done. This brief states none. Its only forward path is line 39's two branches, and both terminate in an exchange with the user rather than in a described end state; the issue list on line 20 is nine open issues with no indication of which belong to this pile, in what order, or what remains when they are exhausted. The same model paragraph explains the harm precisely: "an agent with no completion criterion either invents adjacent work or stalls waiting for an event it cannot observe" — and the events this brief leaves it waiting on (a Mac-side merge of PR #57) are exactly ones it cannot observe. Confidence: sure that the criterion is absent; unsure only whether the author considers line 39 to serve as one.

---

clean sections: none — every section (the untitled opening, "Your open PR", "The state of the machinery", "Your queue", "Session-management facts, verified 2026-08-13 on Claude Code 2.1.231", "Your standing invariant", "First action") has at least one finding.

