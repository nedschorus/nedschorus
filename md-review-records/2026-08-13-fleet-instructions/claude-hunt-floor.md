<!-- provenance: runtime=claude model=claude-sonnet-5 effort=high cell=defect-hunt tier=floor target=/home/nedlern/Projects/nedschorus/.claude/worktrees/gatekeeper-walk-fork-continuation/docs/agents/fleet-instructions.md -->

I reviewed `fleet-instructions.md` in full, plus everything it references by explicit path (`agent-seat-model.md`, `docs/issues/queue/45-session-seat-and-isolation-riders.md`) and the checkout's `CLAUDE.md`. Findings below, in document order.

1. **"awaiting the Mac-side review seat."** (line 7) — This calls the reviewer a "seat," but the glossary this file tells the reader to consult first, `agent-seat-model.md`, defines the term explicitly: "the **Mac-side agent**, which reviews branches and merges them to `main`, is the user's own agent on his Mac and **is not one of the seats defined here**." The two texts use "seat" for the same referent while one asserts it is not a seat. A reader following this file's own instruction to "Read the seat model first" hits a direct terminology conflict. Sure.

2. **"the fleet work inventory"** (line 7) — Listed among the things PR #57 carries, but no path or exact filename is ever given for it anywhere in this document (unlike the other items in the same list, several of which get explicit paths, e.g. `docs/cross-project/fleet-machine-paths-and-checkouts.md`). A reader cannot determine which file is covered by the following sentence, "Nothing else should touch those files until it merges," without guessing. (There is a plausibly-matching file, `docs/issues/queue/45-ubuntu-fleet-open-work-inventory.md`, but this document never names it, so the match is a guess, not something the file establishes.) Sure this reference is unresolved within the file; unsure whether the author intended a different file.

3. **"It carries: ... the supervisor's branch sync ..."** (line 7) versus **"Since 2026-08-13 it syncs the agent's branch with main before each launch"** (line 12) — Line 7 lists the branch-sync feature as part of PR #57's contents, which is "awaiting the Mac-side review" and therefore not yet merged to main. Line 12, in "The state of the machinery," asserts the same feature as unqualified present-tense fact about how the supervisor currently behaves. A reader can't tell whether "the state of the machinery" describes what's live on main today or what's live only on the unmerged PR #57 branch — if the former, it's false until #57 merges; if the latter, the section header ("the state of the machinery," not "the state of PR #57") doesn't say so. Sure this is at minimum ambiguous; unsure which reading the author intended.

4. **"identity from a `CLAUDE.local.md` there, the handoff supervisor inside."** (line 11) — The final clause is a sentence fragment with no verb, and "inside" has no clear referent: it could mean the supervisor process runs inside the tmux session, or that it's located inside the agent home directory (paralleling "there" two words earlier, which does mean the agent home). The two readings describe different architectures (a process launched in-session vs. a script rooted in a directory). Unsure which the author intended; sure the sentence supports both readings as written.

5. **"never runs on the adoption path"** (line 12) — "The adoption path" is used as an established term but is never defined in this file, nor in `agent-seat-model.md` (the only file this document points the reader to). It is defined only in code comments inside `scripts/handoff-supervisor.py` (`syncs only on the launch path and never on adoption`), which this file never references by path. A reader confined to the stated context (CLAUDE.md, this file, and explicit references) has no way to know what "the adoption path" is or how it differs from "the launch path." Sure.

6. **"changing files under a live agent is the one forbidden act"** (line 12) — Read literally, "the one forbidden act" claims this is the single prohibited action, full stop. But the very next bullet, Guards, describes a distinct forbidden action — editing CLAUDE.md/`.claude/` machinery without the user's walked approval — enforced by a different mechanism (`instruction-file-guard.py`). The parenthetical is presumably meant to be scoped to the supervisor's own behavior only, but nothing in the sentence marks that scope, so as written it conflicts with the guard described immediately after it. Sure the wording is broader than the file's own content supports.

7. **"fetch, then fast-forward only when the tree is clean and strictly behind; otherwise report and change nothing."** (line 12) — This describes only two outcomes: fast-forward, or "otherwise" (not clean / not strictly behind) report-and-do-nothing. It doesn't say what happens if the fetch itself fails (network error, auth failure, no remote) — a reachable case for a script run "before each launch" on a machine already noted elsewhere in this file to have had PATH/SSH quirks. The stated behavior doesn't cover it, and it isn't explicitly folded into "otherwise." Unsure whether "otherwise" is meant to catch this too, or whether it's simply unhandled — the wording doesn't say.

8. **"blocks edits to CLAUDE.md and `.claude/` machinery without the user's walked approval"** (line 14) — `agent-seat-model.md`'s definition of "Instruction-class" (the class of files this guard protects, per that file's own description of what `instruction-file-guard.py` consumes) is "`CLAUDE.md`, `CLAUDE.local.md`, anything under `.claude/`." This file's phrasing, "CLAUDE.md and `.claude/` machinery," omits `CLAUDE.local.md` — a file this very document says lives outside `.claude/`, in each seat's home directory, and carries seat identity (line 11). It's unclear from this file whether the guard actually covers `CLAUDE.local.md` edits or not. Unsure whether this is a real gap in the guard or just an incomplete restatement in this brief.

9. **"the box needs `ssh nedlern@ned-box -t 'claude agents'`"** (line 28) — Taken literally, the subject "the box" needs to run an ssh command targeting itself (`ned-box` sshing to `ned-box`), which is circular and contradicts the topology stated in the global CLAUDE.md (the user runs Ubuntu-side commands from his Mac seat via `ssh nedlern@ned-box '<command>'`; the box does not ssh into itself). The intended meaning — "to see the box's list, run this ssh command from wherever you are" — requires the reader to supply an elided premise. The companion document `docs/issues/queue/45-session-seat-and-isolation-riders.md` states that premise explicitly ("It must run on the machine whose sessions you want. Typed on the Mac it lists the Mac's jobs; the box needs..."), but that clarifying sentence is not present here, so this file's version is harder to resolve on its own. Sure the literal reading is wrong; sure the intended meaning is recoverable only with an assumption this file doesn't state.

10. **"## Your standing invariant" / "Two live sessions must never share a working directory."** (lines 33, 35) — Calling this a "standing invariant" asserts it always holds. But the file's own referenced queue document (`docs/issues/queue/45-session-seat-and-isolation-riders.md`, explicitly linked under "Your queue") records that this exact rule was broken in practice ("one had forked into another's checkout") and that no enforcement exists yet — "Detection is the open problem." This file's own next line admits the same: "the gap is the trigger, not the capability," i.e., nothing currently triggers a stop when the rule is violated. Labeling an unenforced, already-violated rule an "invariant" overstates what currently holds. Sure this is inconsistent with the file's own admission two sentences later and with the linked queue document; unsure whether "invariant" was meant descriptively (what is true) or prescriptively (what should be true) — the file doesn't say which.

11. **"If it has, verify the renamed launchers work from the Mac and tell the user."** (line 39) — This instructs whichever agent reads this brief to personally verify launcher behavior "from the Mac." Per the global CLAUDE.md loaded into this session, Claude sessions here run on `ned-box`; the user, not the agent, sits at the Mac, and Mac-native steps are the user's to perform. This file gives the reading agent no mechanism (no cross-machine command, no instruction to ask the user to test and report back) for carrying out a verification step that happens "from the Mac" itself. As written, the instruction asks the agent to do something it has no described way to do. Sure.

clean sections: Your queue

