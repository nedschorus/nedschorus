<!-- provenance: runtime=claude model=claude-sonnet-5 effort=high cell=defect-hunt tier=floor target=/home/nedlern/Projects/nedschorus/.claude/worktrees/gatekeeper-walk-fork-continuation/docs/agents/seat-first-prompt.md -->

I read the target file in full, plus its explicitly-referenced companions (`docs/agents/agent-seat-model.md`, the seven `docs/agents/*-instructions.md` files) and the checkout's root `CLAUDE.md`, to check every claim this file makes about them.

**Findings (document order)**

1. **Line 3** — *"Your seat name is the name of your working directory. Run `pwd`: if it is `/home/nedlern/agents/<name>`, then `<name>` is your seat."*
   This is an if/then with no else. Nothing in Step 1 (or anywhere else in the file) says what to do if `pwd` does *not* match `/home/nedlern/agents/<name>` — e.g., if `NEDSCHORUS_AGENTS_ROOT` is set to something other than the default `~/agents` (an override the launcher itself supports), the agent's home would sit at a different path and this step gives no instruction for determining the seat name in that case. An agent hitting this branch has no defined next action. Category (g)/(e). Confidence: fairly confident this is a real, reachable gap, though I can't rule out that it's considered out of scope by the author.

2. **Lines 5–7** — *"The launcher makes your home a checkout of the project on your own branch before your session starts, so `git rev-parse --show-toplevel` should answer with your own directory..."*
   This directly contradicts the companion file this document tells the reader to read next (Step 3, item 2): `docs/agents/agent-seat-model.md` states *"The launcher creates that home as an **empty directory, not a checkout**; the first-prompt file has the agent make it one on its first run (`git worktree add` into an empty existing directory works, verified 2026-08-14)."* One document says the launcher pre-creates the checkout; the other says the launcher deliberately leaves an empty directory and it is *this very file* that is supposed to create the checkout. An agent reading both in the order Step 3 prescribes gets two incompatible accounts of who is responsible for creating the checkout and what a bare (non-checkout) home means. Confidence: sure this textual conflict exists between the two documents; I did not go outside the agent's given context to establish it (agent-seat-model.md is explicitly referenced by this file).

3. **Line 7** — *"Tell the user, and let him relaunch you rather than working on."*
   This clause is grammatically incomplete — "working on" has no object ("working on" what?). As written it does not clearly state the alternative being declined (continuing to work without a relaunch, presumably). Confidence: unsure how much this actually impedes a reader — the intent is recoverable from context — but it is not a grammatically complete instruction as written.

4. **Lines 10–13** — *"(drop `-b` and pass the branch name last if the branch already exists — git permits a branch in only one worktree at a time, which is what keeps seats from colliding).”*
   This describes how to modify the repair command `git ... worktree add /home/nedlern/agents/<name> -b <name> origin/main` when the branch already exists, but is ambiguous about whether `origin/main` should also be dropped. A literal reading of "drop `-b` and pass the branch name last" — remove only the `-b` token and move `<name>` to the end — yields `git ... worktree add /home/nedlern/agents/<name> origin/main <name>`, which is not valid `git worktree add` syntax (it takes at most one ref/commit-ish after the path). The correct existing-branch form is `git ... worktree add /home/nedlern/agents/<name> <name>` (dropping `origin/main` entirely), but the sentence never says to drop `origin/main`. Category (b)/(c). Confidence: fairly confident this is a genuine ambiguity — someone unfamiliar with `git worktree add`'s two calling forms could easily produce the invalid command from the literal instruction.

5. **Line 23** — *"Every brief's first action is to read, verify, and report to the user — never to start building."*
   This absolute claim is false for at least one of the seven briefs this file itself points to (Step 3, item 1: `docs/agents/<name>-instructions.md`). `docs/agents/sidebar-instructions.md`'s First Action reads: *"Greet the user briefly and ask what he needs. No status report, no inventory, no plan — that is what the topic seats are for."* This involves no reading, no verifying, and explicitly no report — the opposite of "read, verify, and report." Category (f). Confidence: sure — directly verified against the referenced file.

6. **Line 25** — *"Changes reach `main` only through the user's Mac-side review seat: commit to your own branch, push it, and let him merge — never push to `main` yourself."*
   This conflicts with the root `CLAUDE.md`'s definition of the same rule: *"How a change reaches main: the git-gatekeeper (...) is the **permanent path** — one program, one credential, one door; agents never push to main. **Until its credential work lands** (activation waits on build slice 6), the gate is dormant and **the interim lane applies**: commit to the working branch, push it, and the user's Mac-side agent reviews and merges."* CLAUDE.md frames the Mac-side-review path as a temporary, conditional stand-in for the git-gatekeeper (the stated permanent path); this file states it as an unconditional "standing rule" using the absolute word "only," with no mention of the gatekeeper or the condition under which the rule changes. Once the gatekeeper activates, this sentence becomes false as written, and an agent reading only this file (its declared zero-context starting point) would not know the gatekeeper exists as an alternative path at all. Category (h) — quoting both: this file's *"Changes reach `main` only through the user's Mac-side review seat"* vs. CLAUDE.md's *"the git-gatekeeper ... is the permanent path ... Until its credential work lands ... the interim lane applies."* Additionally, CLAUDE.md calls this reviewer *"the user's Mac-side agent,"* while this file calls it *"the user's Mac-side review seat"* — a second, smaller terminology mismatch in the same sentence. Confidence: sure on the conflict; sure on the terminology mismatch being present, unsure whether the author considers "seat" and "agent" interchangeable enough to not matter.

7. **Line 25** — *"the user's Mac-side review seat"*
   Separately from finding 6's rule conflict: `docs/agents/agent-seat-model.md` (this file's own Step 3 reference) scopes the term "seat" explicitly to Ubuntu-box agents — its opening line is *"How work on the **Ubuntu box** is divided among named agents"* — and its seat roster table lists exactly seven Ubuntu-side seats, none Mac-side. This file uses "seat" for a Mac-side agent that the seat-model document never describes as a seat, extending a term the file otherwise treats as precisely defined (Step 1 is entirely about identifying "your seat") to an entity outside where it's defined. Category (d)-adjacent: not strictly un-executable, but an unexplained extension of a term with a specific meaning elsewhere. Confidence: unsure — this may just be loose, harmless phrasing rather than a real ambiguity, since no instruction in this file depends on the reader resolving what a "Mac-side review seat" precisely is.

**clean sections:** opening paragraph (line 1); Step 3 / "read your instructions, in this order" (lines 15–21).

