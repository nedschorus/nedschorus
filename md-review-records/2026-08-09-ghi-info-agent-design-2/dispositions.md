# md-review dispositions — ghi-info-agent-design.md (grid of 2026-08-09)

Eight reviews read in full (all cells present). Triage classes: (A) genuine design gaps needing the user's ruling, (B) document defects with an obvious fix — applied on approval, (C) reviewer challenges to already-ruled decisions — answered with the ruling citation, no change without new evidence. Findings below are deduplicated across the four defect-hunt reports; the two restate pairs confirmed intended readings except where noted.

## Walk order (opened 2026-08-09, new-vp session 3a11d08f)

1. Purpose: what the grid found, the three triage classes, and the bar for fixes
   *processed 2026-08-09 → accepted (purpose item; user confirmed the per-class breakdown expectation: bundles list every fix concretely, any fix can be pulled out, C confirms per ruling)*
2. (A) The output contracts — the ask's bare-list-plus-notes channel, the adjudication verdict grammar, and a precedent/absence mode so the post-check stops eating correct closed-issue answers
   *processed 2026-08-09 → APPROVED as the five-step submission flow, with two rulings: (1) the too-similar outcome is named and instructed as a MERGE — "merge this into #18 by editing it" — because agents are trained on merge vocabulary (user); (2) role names used consistently — "GHI author" for the writing agent, ghi-info for the knowledge agent; bare "agent" is ambiguous and the design rewrite names the role at every use. The verdict grammar and malformed-reply→fail-open are covered by the approved flow (steps 3–5). The remaining ask-path machinery split out to new item 3 per the user's too-many-steps-together correction; walk re-planned 10 → 11 items.*
3. (A) The ask-path mechanics, split from item 2 — the script-authored note lines on the ordinary ask, and the `--include-closed` precedent/absence mode (no filtering there; closed pointers marked mechanically; excluded from the staleness rate)
   *processed 2026-08-09 → REVISED to the user's drift-notice design, superseding both the remove-and-note and tag-only proposals: a stale pointer in an answer triggers a drift notice back to ghi-info — "you recommended #31, it closed on <date>; double-check and answer again" — one recheck per ask, then the answer is delivered with truthful tags on any closed pointer remaining; nothing is silently removed, so a recheck can surface a successor issue no script could know. Single drift instances never force a reset (that would demand perfection from an LLM); instances count toward the accumulated-drift recycle triggers, which stay as ruled. `--include-closed` remains the deliberate closed-history mode with the same tagging; note lines are written as plain sentences for a zero-context reader. Effectiveness bet (user): tight coupling of notice and recheck ≈ 99.9%, testable at build with probe questions.*
4. (A) The flag problem — maintenance flags have no destination; duty list understates; proposal: a standing draft-labeled maintenance issue written through the tool, extending ghi-info's write class
   *processed 2026-08-09 → APPROVED as revised to fixes-not-reports (user): the standing-flag-issue proposal is superseded — maintenance detection spawns a one-shot focused fixer agent (#26's class, launched per the nedschorus#41 run-agent work) with a tight brief; fixers write through the normal path and consult ghi-info like any GHI author; ghi-info itself repairs link integrity only, never substance (shared serial resource; the adjudicator keeps no authorship stake; fixers are cheap and parallel); a blocked fix escalates as one draft-labeled issue; whether an old ruling still binds always escalates to the user. The flag-not-fix rule is thereby narrowed to detection-spawns-fixer; write-time split stays with the author. Viability rests on the corpus discipline keeping GHIs naive-agent-fixable — the quality machinery and fixability are one loop. OPEN QUESTION recorded (user): which model/runtime best serves each role — ghi-info, fixers, adjudication — across claude/codex and fable/opus/sonnet tiers; settled empirically at build and in live use, not pre-ruled.*
5. (B) Write-path corrections bundle — length-check timing wording, fail-open scope, edit-vs-its-own-issue exclusion, `close --comment` hole, non-body edits pass through, delete carries the override, comments as the one taught exception, tool self-interception note, check-to-write race named as residual
   *processed 2026-08-09 → all nine APPROVED as a batch (user), on the recommendation that seven restate ruled decisions and only two carry choices, both endorsed: close --comment gets deny-and-teach rather than silent comment-stripping (consistent with comment handling; stripping discards the author's text untold), and non-body edits pass through with the rename-disguise residual accepted (adjudicating renames would cost a model turn per label tweak for a rarity the sweep catches).*
6. (B) Mirror mechanics bundle — refresh becomes rebuild-from-full-list plus per-issue comment delta (kills first-run/boundary/deletion cases), "tiered" defined or dropped for v1, mirror home and machine named, git-feed ref named, wrapper concurrency lock
   *processed 2026-08-09 → APPROVED with two in-walk revisions (user) and riders. (1) Refresh cadence REVISED: delta query per ask, full mirror rewrite at every session recycle — uniform to ~20K GHIs, no routine full fetch; deletions/transfers have no cheap API report (audit-log is enterprise-gated), accepted because delete is denied by design and the recycle-time snapshot bounds residual staleness; comments always delta-fetched per changed issue. (2) "Tiered" deleted: a closed issue's line is number, title, close reason, close date. (3) Places: mirror at `<checkout>/ghi-mirror/` (gitignored; collision-checked), script at `scripts/ghi-mirror-refresh.py`, wrapper state at `~/agents/ghi-info/`. (4) Git feed: fetch origin, read `git log origin/main` — MD changes enter the corpus when they land on main. (5) Concurrency REVISED from lockfile to lock-free: a second concurrent caller cold-starts a throwaway session and saves nothing; mirror writes go temp-then-rename so concurrent refreshes are safe; nothing waits, nothing blocks. Riders: the reference check is reactive-only — no GHI is required to link an MD; cited in-repo paths must resolve on main, with the refusal teaching both branches (land the MD first, or file without the reference and add it by edit); ghi-write gains the pair-sequence step (write MD, land it, then cite it) as a rider to its paused walk; the dropped-reference perverse incentive is an accepted residual with partial sweep catch.*
7. (A) The naming set — the write tool, the wrapper, the mirror directory and files, section headings; `ghi-info` itself is user-ruled and stays
   *processed 2026-08-09 → APPROVED (user): scripts/ghi-issue-write.py (write umbrella covers create/edit/comment/label as subcommands), scripts/ghi-info-ask.py, scripts/ghi-mirror-refresh.py, .claude/hooks/ghi-issue-write-redirect.py, the Superseded-by: #<n> marker literal, greppable section headings; ghi-info and ghi-mirror stand as previously ruled. The naming-ordering doctrine rider was DECLINED (user: keep mum — training already produces subject-first prefix families and role suffixes, so a rule restating the default is noise; consistent with the checklist's match-instruction-to-failure principle). Discussion finding recorded: subject-first ordering matches the open-source corpus (git-*, systemd-*, pytest-* prefix families; *_test/-hook/-guard role suffixes) and left-to-right recall; grounded inference, cheap to micro-test if ever load-bearing.*
8. (B) Absolutes and overclaims — never-correctness, exactly-as-trained, "exact pattern", cannot-notice, moment-of-action, "demonstrably", the unbounded corpus-fits premise gains its bound and growth point
   *processed 2026-08-09 → APPROVED as the nine-subtraction set (user), superseding the expanded-hedge drafts including the part-1 version approved mid-walk: overclaims are fixed by deleting the excess words, not by qualifying — four argument-class sentences deleted outright (moment-of-action, demonstrably-lose, pointers-fail-visibly, one-in-fifty), five contracts trimmed by subtraction or ≤7 added words (never-correctness dropped, exact dropped, reliably added, measured-today parenthetical, refreshed-mirror wording); the document gets shorter and nothing remaining is false. Sorting principle ruled with it: the design keeps statements of behavior and bounds; argument for already-ruled decisions is deleted once common sense, its trace being the decision trail. META-RULING to carry forward (user): the correctness/wording guidance needs review to state that the ideal fix for over-specification is usually removing extra words, not a carefully qualified multipart statement — destination: the skill-authoring checklist Register section or the CLAUDE.md writing floor, routed through its own review; proposed wording: "When a claim over-specifies, prefer deleting the excess words to qualifying them: the corrected statement should usually be shorter, not longer."*
9. (B) Zero-context pointers — revision convention, pair/queue/bare MD, guard-pattern path, launch-claude, cooperative posture unified, ordinary rules, box convention, two credentials named, Codex scope line, ghi-write build status
   *processed 2026-08-09 → all nine APPROVED (user): citations not definitions — founding plan § Project organization covers the four lifecycle terms; guard pattern by path; launch-claude by script path plus nedschorus#45, which also covers the box convention; cooperative posture unified with a one-clause meaning and the honest-singleton citation; ordinary rules named as ghi-write plus the lifecycle rule; both box credentials named; Codex in scope with hook equivalence verify-at-build and accepted-holes interim; ghi-write status one clause.*
10. (B) Constants and verify-at-build additions — length limit, thresholds, timeout: value locations plus proposed defaults; comment-volume measurement; failed-verification outcomes
    *processed 2026-08-09 → APPROVED (user): named constants at the top of the owning script, no config file in v1; starting values — body limit 400 words (tool owns it, sweep imports), closes-since-birth 20, stale-match 2-in-10, transcript threshold set at build from NM values, ask timeout 5 minutes inside the 600 s hook budget, one drift recheck; every verify-at-build item carries its failure branch — recycle rewrite bounds unmoved-timestamp staleness, tool reply carries all if context injection cannot combine, Codex rides accepted-holes if hooks cannot rewrite, output mimicry dissolves into relay-gh-verbatim-and-append, auth expiry is a named failure plus a sweep credential check; comment-fetch cost at volume added as a build measurement.*
11. (C) Ruled-no-change confirmations — ghi-info name, bare-list form, soft block, uncommitted mirror, no vector DB: the challenges and the rulings that answer them
