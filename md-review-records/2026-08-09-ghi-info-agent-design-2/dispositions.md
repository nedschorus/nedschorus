# md-review dispositions — ghi-info-agent-design.md (grid of 2026-08-09)

Eight reviews read in full (all cells present). Triage classes: (A) genuine design gaps needing the user's ruling, (B) document defects with an obvious fix — applied on approval, (C) reviewer challenges to already-ruled decisions — answered with the ruling citation, no change without new evidence. Findings below are deduplicated across the four defect-hunt reports; the two restate pairs confirmed intended readings except where noted.

## Walk order (opened 2026-08-09, new-vp session 3a11d08f)

1. Purpose: what the grid found, the three triage classes, and the bar for fixes
   *processed 2026-08-09 → accepted (purpose item; user confirmed the per-class breakdown expectation: bundles list every fix concretely, any fix can be pulled out, C confirms per ruling)*
2. (A) The output contracts — the ask's bare-list-plus-notes channel, the adjudication verdict grammar, and a precedent/absence mode so the post-check stops eating correct closed-issue answers
3. (A) The flag problem — maintenance flags have no destination; duty list understates; proposal: a standing draft-labeled maintenance issue written through the tool, extending ghi-info's write class
4. (B) Write-path corrections bundle — length-check timing wording, fail-open scope, edit-vs-its-own-issue exclusion, `close --comment` hole, non-body edits pass through, delete carries the override, comments as the one taught exception, tool self-interception note, check-to-write race named as residual
5. (B) Mirror mechanics bundle — refresh becomes rebuild-from-full-list plus per-issue comment delta (kills first-run/boundary/deletion cases), "tiered" defined or dropped for v1, mirror home and machine named, git-feed ref named, wrapper concurrency lock
6. (A) The naming set — the write tool, the wrapper, the mirror directory and files, section headings; `ghi-info` itself is user-ruled and stays
7. (B) Absolutes and overclaims — never-correctness, exactly-as-trained, "exact pattern", cannot-notice, moment-of-action, "demonstrably", the unbounded corpus-fits premise gains its bound and growth point
8. (B) Zero-context pointers — revision convention, pair/queue/bare MD, guard-pattern path, launch-claude, cooperative posture unified, ordinary rules, box convention, two credentials named, Codex scope line, ghi-write build status
9. (B) Constants and verify-at-build additions — length limit, thresholds, timeout: value locations plus proposed defaults; comment-volume measurement; failed-verification outcomes
10. (C) Ruled-no-change confirmations — ghi-info name, bare-list form, soft block, uncommitted mirror, no vector DB: the challenges and the rulings that answer them
