<!-- provenance: runtime=claude model=claude-opus-5 effort=high cell=defect-hunt tier=good target=/Users/el/Projects/nedschorus/.claude/worktrees/shared-conversation-discussion-eb34e2/docs/drafts/ghi-info-agent-design.md -->

Read complete. Referenced paths resolve: `docs/drafts/ghi-info-agent-plan-draft.md`, `docs/drafts/ghi-gatekeeper-plan-draft.md`, and `docs/issues/26-dynamic-agent-team-model.md` all exist. No `AGENTS.md` in the checkout; `CLAUDE.md` read in full.

Findings are in document order. Mode respected: no edits made.

---

**1. Frontmatter — `status: design awaiting the user's walk (md-review first)`** vs line 8 **"this file's design decisions were ruled 2026-08-07"**
The frontmatter says the design has not yet been walked; the body says its decisions were already ruled, and points at a "walk anchor with per-item dispositions." A reader cannot tell whether the contents are settled rulings they must respect or proposals still open for the user to overturn. Harm lands when the next agent picks up this file to build from: treating ruled decisions as open re-opens closed questions, and treating open ones as ruled implements something the user never approved. Sure the two statements conflict; unsure which is current, and the file gives nothing to break the tie.

**2. Line 6/8 — the name `ghi-info`**
The name has two parts and the second, `info`, does not say what the thing is (an agent, a knowledge store, a query service, a CLI). CLAUDE.md instructs: "When creating or inventing names, for directories, file names, globals, functions, classes, scripts, section headings, and other names likely to be grepped, use explicit, clear and precise multi-part names… If these checks return collisions or ambiguity, choose a more explicit name, with 3 or 4 parts, not 1 or 2." `ghi-info` is exactly the two-part, ambiguous shape that rule warns against; it will also appear as a substring of unrelated prose about GHI information. Harm is at search time: an agent grepping for the agent's definition gets noise, and one reading `ghi-info` in a stack trace or a path cannot infer its role. Unsure — this may be a pre-existing project name (the file presents it as the first build of a class defined in issue 26), in which case CLAUDE.md's "use the existing name" clause governs; the file does not say whether the name is inherited or coined here.

**3. Line 10 — "Mechanical work never spends agent tokens: scripts fetch, format, measure, and filter for free"** vs line 35 **"A cold start loads `issues-open.md` whole"**
Script *execution* is free of agent tokens, but everything the scripts produce is consumed by the agent: the whole open-issues mirror enters context on every cold start, and the ~109 KB figure at line 28 is the size of that payload. The claim is repeated as the "free" cost column in the Division of labor table and as "Refreshing is fast and cheap" at line 36. Harm: this is the load-bearing economic argument for the whole design ("Mechanical work never spends agent tokens"), so an agent sizing recycle thresholds or corpus growth against it will budget as if reloads cost nothing, when a reload is the single largest token event in the system. Sure.

**4. Line 10 — "the corpus fits in its context window"**
Stated as a present-tense property with no bound, no measurement to re-check it against, and no stated behavior when it stops being true. Line 28 gives one datapoint (45 issues, ~109 KB) and the cuts table discards a vector/graph database "Never on current evidence," but the open-file growth path is the one that breaks the premise, and the tiering answer at line 83 is offered only for closed issues. Harm arrives silently and late: when the open corpus outgrows the window, a cold start truncates or fails, and every duty that depends on whole-corpus loading degrades without any defined signal. Sure the case is reachable and unaddressed.

**5. Line 16 — "The answer is a bare list — 'read #13, #24, #31' — no why-lines, no summaries"** vs line 18, line 40
Three other passages require the answer channel to carry something other than a bare list: duty 3's verdicts are three named categories plus, for "related but compatible," a set of GHIs to familiarize with and, for "too similar," "a reconsider-and-read reply"; line 40's post-check removes a closed pointer "with a note before the asker sees it"; and line 40 closes with "print the bare list" as the wrapper's sole output. A wrapper that prints a bare list cannot convey a verdict, and a note is prose. Harm: the implementer must pick one contract for the ask output, and the two callers (a curious agent, the write tool) need different ones; picking either makes the other caller's described behavior unbuildable. Sure.

**6. Line 17 — "pair MD"** (also line 20 "queue vs GHI vs pair vs bare MD", line 28 "pair-MD edits", line 51 "the linked pair MD")
The term is used five times and never defined; neither is "queue" nor "bare MD." A reader with only this file cannot tell what a pair is (an MD file paired 1:1 with an issue? a naming convention? a directory?), how to recognize one, or how the pairing is recorded. Duty 2 makes verifying pair backlinks a standing obligation and line 51 makes creating/updating a pair the prescribed remedy for an over-length body, so the undefined term sits directly on the execution path. Harm: an agent told to "move the substance to the linked pair MD" cannot determine which file that is, or create one correctly. Sure.

**7. Line 17 — "Maintain the reference graph"** vs line 34 **"active only while taking a turn, otherwise exited… There is no idle state"**
Duty 2 is a standing, corpus-wide obligation ("every GHI→MD reference resolves; every pair MD backlinks its correct GHI(s)"), but the lifecycle gives ghi-info no moment in which to perform it: the agent exists only while answering an ask, and the ask section describes a wrapper that refreshes, asks the question, post-checks, and prints. No trigger, no schedule, and no completion criterion for the graph pass is stated anywhere. Harm: an implementer builds the ask path, and duty 2 is silently never executed; alternatively an agent takes duty 2 literally and attempts a full-corpus integrity pass inside a question turn, blowing the timeout. This is also the "no stopping point" class — "every reference resolves" is a condition to be restored continuously, with nothing saying when the work is done. Sure.

**8. Line 17 — "Cross-link edits are ghi-info's only write class"**
Cross-link edits to issue bodies are issue writes. Line 50 routes `gh issue create`/`edit` through a PreToolUse hook into the write tool, and line 18 has that tool consult ghi-info for a similarity verdict before the write lands. Taken together, ghi-info's own writes either route into a tool that consults ghi-info (a self-consultation the file never describes) or bypass the hook entirely (an unstated exemption). The file also does not say whether the hook exists on the Ubuntu box where ghi-info runs. Harm: at build time there is no answer for what happens when the knowledge agent edits an issue — a deadlock, an infinite consult, or an undocumented exemption, chosen by whoever writes the code. Sure the case is reachable and unhandled.

**9. Line 18 — "**too similar** to an existing GHI (duplicate, overlapping, conflicting) — the write is refused"**
The adjudication is described identically for creates and edits ("before filing or editing an issue," line 16; "`gh issue create`/`edit`," line 50), but an edit's draft body is by construction most similar to the very issue being edited. Nothing in the file excludes the target issue from the comparison set. Harm: every ordinary edit — including the lean-split rewrite line 51 demands, and ghi-info's own cross-link edits — trips the duplicate verdict and is refused, forcing an override on routine work. Sure the case is reachable; unsure whether the author considered it obvious enough to omit, since the file gives no hint either way.

**10. Line 20 — "answers only from the issue corpus"** vs line 17 **"every pair MD backlinks its correct GHI(s)"**
Duty 2 requires ghi-info to verify the MD side of the issue–MD boundary, which requires reading MD file contents. The only store the file defines is the mirror, and the mirror is explicitly issues only (line 26: two files, open issues and closed issues). The second delta feed (line 28, "git log since last run") reports that pair-MD edits happened; it does not supply their contents, and the file does not say ghi-info reads the repo. Harm: duty 2 is unbuildable as written for one of its two directions, and the gap is easy to miss because "link integrity across the issue–MD boundary in both directions" reads as one uniform job. Sure.

**11. Line 20 — "asked about the wiki or the code, it says so rather than guessing"**
"The wiki" is introduced here and nowhere else in the file; nothing establishes that a wiki exists, where it lives, or how it differs from the MDs the design does discuss. Harm: an agent applying the scope boundary cannot tell whether a question about a repo MD is a wiki question (out of scope) or a pair-MD question (duty 2, in scope). Unsure — "the wiki" may name a well-known project artifact, but this file gives no way to resolve it.

**12. Line 20 — "issue bodies grown past the length limit"**
"The length limit" is referred to as a known quantity here and again at line 51 ("Over the limit"), but no value is given and no location where the value is defined is named. Harm: the tool's central mechanical check (line 50, "body length") cannot be implemented, and an agent asked whether a body is over the limit cannot answer. Sure.

**13. Line 20 — "issue bodies grown past the length limit while nobody was writing"**
Taken literally this is impossible: an issue body changes only when something writes it. The clause is presumably reaching for bodies that grew through a path the tool did not see (the known holes at line 56) or bodies that crossed a limit that was itself lowered — but neither is said. Harm: an implementer looking for the condition to detect has no detectable condition, and a reader trying to understand why the flag-never-fix rule exists is given a cause that cannot occur. Sure the literal reading is impossible; unsure which of the plausible intents was meant.

**14. Line 20 — "ghi-info **flags and never fixes**"**
"Flag" is given as an action with no destination: no file, no label, no reply field, no report is named, and duty 1's answer format is a bare list of issue numbers with no room for a flag. Harm: the behavior cannot be executed — an agent instructed to flag has nowhere to put the flag — and a stale-MD or over-length observation is therefore lost at the end of the turn that noticed it, which is exactly the outcome the rule was written to prevent. Sure.

**15. Line 22 — section headings "## The mirror", "## The session", "## The ask", "## The write path"**
CLAUDE.md instructs: "When creating or inventing names, for directories, file names, globals, functions, classes, scripts, **section headings**, and other names likely to be grepped, use explicit, clear and precise multi-part names… with 3 or 4 parts, not 1 or 2." These headings are one- and two-part generic nouns. "The ask" is the worst case — it is a common English word used in the file's own prose ("A failed ask never blocks a write") and cannot be searched for. Harm: another document trying to cite the section that defines the wrapper script, or an agent grepping the repo for the mirror's specification, has no distinctive string to search. Sure.

**16. Line 26 — "`issues-closed.md` — one tiered line per closed issue"** (and line 83, "mirror tiering answers it")
"Tiered" and "mirror tiering" are used as if defined: nothing states what the tiers are, what determines an entry's tier, or what a tier changes about the line's content. Line 83 makes tiering the reason a whole alternative (GitHub-side purge) was cut, and names "Mirror tiering no longer suffices" as the condition for that decision to reverse — a condition that cannot be evaluated without knowing what tiering does. Harm: the closed file cannot be generated, and the cut cannot be revisited on evidence. Sure.

**17. Line 26 — "the closed file joins in exactly two cases"** and **"the path, not an instruction, keeps them out of the other forty-nine"**
Two problems in one bullet. First, "exactly two" is contradicted within the file: line 40 and the Division of labor row "Answer post-check against closed file" have the wrapper consult the closed file on every ask, and line 36's closes-since-birth counter needs closure data too. Second, the sentence claims the file layout does the work rather than an instruction, but the sentence immediately preceding it *is* the instruction ("routine relatedness checks grep the open file only; the closed file joins in exactly two cases") — the layout separates the data, but an agent still has to be told which file to grep. Harm: an implementer reading "exactly two" as a closed enumeration will treat the wrapper's own closed-file access as out of spec; a reader taking the no-instruction claim at face value may omit the grep-the-open-file-only guidance from `ghi-write`, on the theory that the path enforces it. Sure on the first; sure the second is self-undercutting as written.

**18. Line 27 — "**Home**: a gitignored conventional path in any checkout"**
The path is called conventional but is never given, and no file is named as the place it is recorded. Harm: no one can find, regenerate, gitignore, or grep the mirror; the fallback ladder's second rung ("grep the mirror," line 44) is unexecutable. Sure.

**19. Line 27 — "in any checkout, regenerated by script on any machine"** vs line 33 **"the Ubuntu box… Mac-side callers reach it over SSH"**
These describe two different mirrors without saying so. ghi-info runs on the box and reads the box's mirror; a Mac-side caller whose ask fails and falls back to "grep the mirror" reads its own checkout's mirror, which is refreshed only when something on the Mac runs the script. The file never says which mirror the wrapper's delta-refresh step touches, whether the Mac even has one, or how a worktree (this checkout is one) relates to "any checkout." Harm: the fallback path can silently read a mirror that is arbitrarily stale, or absent, producing a confident "no issue covers X" absence claim from empty data — the precise failure the two-file absence rule was designed to prevent. Sure the ambiguity exists in the text.

**20. Line 28 — "full pull of all 45 issues with bodies took 0.82 s (~109 KB)"** vs line 26 **"every open issue near-raw (number, title, labels, updated time, body, **comments**)"**
The mirror's open file is specified to include comments; the verification that underwrites "free" and "sub-second" measured bodies only. Comments are not returned by an issue search and are fetched per issue, so the measured figure does not cover the artifact being built. Harm: the refresh-is-cheap premise — which justifies eager recycling (line 36), refreshing on every ask (line 40), and the "free" column throughout — rests on a measurement of a smaller job than the one specified. Sure the quoted evidence does not cover comments; unsure how much larger the real cost is, as the file gives no comment-volume figure.

**21. Line 28 — "one `gh` search call (`updated:>` the mirror's newest entry)"**
Three reachable cases in the refresh mechanism are neither handled nor discarded: the first run, when there is no newest entry to compare against; the boundary itself, where an issue updated in the same second as the recorded high-water mark is either fetched twice or skipped depending on inclusivity, which the file does not state; and issues that disappear from GitHub (deleted or transferred), which a "changed since" query can never report, so their entries persist in the mirror forever. Harm: a mirror entry for an issue that no longer exists produces pointers that fail at read time and absence claims computed over phantom data; a skipped boundary issue is invisible until something else touches it. Sure these cases are unaddressed in the text.

**22. Line 28 — "A second feed — git log since last run — covers pair-MD edits"**
No repository, checkout, or branch is named for this `git log`. ghi-info is seated on the Ubuntu box (line 33), and the file never says the box holds a checkout, nor that anything fetches or pulls it. Harm: implemented against an unpulled or absent checkout, the second feed reports nothing and does so silently — pair-MD edits stay invisible, which is the exact condition the feed was added to fix. Sure the file does not supply the needed context.

**23. Line 29 — "agents cannot infer what is relevant now versus before"**
Stated as an absolute incapacity. The ordinary counterexample is in this same file: line 29 goes on to have an agent write an explicit supersession marker naming a successor, which is precisely an agent judging what is relevant now versus before; likewise an agent reading a dated ruling in a body can compare it to a later one. The real claim seems to be that agents cannot infer currency from an *undated corpus alone*. Harm: as written it licenses removing agent judgment from currency questions entirely, which would contradict line 29's own boundary that no script decides whether an old ruling still binds. Unsure — this may be intended as compressed rhetoric rather than a rule, but a future agent reading it as a rule gets the wrong boundary.

**24. Line 29 — "an activity-relative freshness (how much of the project has moved since this issue last did; project activity, not calendar time, is the aging clock)"**
The quantity is named but not defined: what counts as an activity event (commits? issue writes? both? on which branches?), over what window, in what units, and what value the field carries. Harm: the script cannot be written, two implementations will produce incomparable numbers, and an agent reading a freshness value in a mirror entry cannot tell what magnitude means stale. Sure.

**25. Line 29 — "two open issues claiming the same ground with neither naming the other is a mechanically flaggable defect"**
The stated mechanism is a grep over a marker pattern. A grep can determine that no supersession marker links two issues; it cannot determine that two issues "claim the same ground" — that is a similarity judgment, and the file assigns exactly that judgment to ghi-info at a cost of one model turn (Division of labor, "similarity verdicts | ghi-info"). As written, the mechanical sweep either flags every unmarked pair of open issues (all of them) or requires the semantic step it claims not to need. Harm: the row "supersession sweep | script | free" in the Division of labor table budgets a model-judgment task as a free script, and an implementer following it produces a sweep that emits noise. Sure. Related: the "grep-able pattern" is asserted but the pattern itself is never given, so the greppable half is also unimplementable.

**26. Line 33 — "`~/agents/ghi-info` per the box convention"** and **"the path the launch-claude work built"**
Both "the box convention" and "the launch-claude work" are referred to as known things, with no path, document, issue number, or description. Harm: an agent seating a second domain-knowledge agent cannot follow the convention it is told to follow, and one debugging the SSH path has no artifact to read. Sure. (`launch-claude` is also the kind of two-part invented name CLAUDE.md's naming rule targets, and without a path it is not findable by search.)

**27. Line 36 — "All three are config constants tuned in live use"**
Tuning is prescribed with no target, no measurement, and no condition under which tuning is finished — an obligation with no stopping point. Compounding it, one of the three constants is a threshold on "the stale-match rate," a rate that requires a denominator and a window (per turn? per session? rolling over how many answers?) that the file never supplies. Harm: the wrapper cannot compute trigger two at all, and "tuned in live use" leaves an agent either fiddling indefinitely or never starting. Sure.

**28. Line 40 — "run by any agent (and by `ghi-write` step 1)"**
`ghi-write`'s steps are cited as a numbered procedure, but `ghi-write` is described in this file only as a skill that "front-loads the right behavior" (line 60); no step list appears here and no path to the skill is given, though a sibling draft exists in the same directory. Harm: a reader cannot verify that step 1 is in fact the ask, and cannot follow the pointer. Sure.

**29. Line 40 — "resume by stored session id, or cold-start when none exists"**
The store for the session id is never named — no path, no file, no key — and the wrapper is simultaneously declared runnable "by any agent," which makes concurrent runs against one stored id an ordinary occurrence (two agents drafting issues at the same time; a hook-invoked adjudication overlapping a human-driven ask). Nothing states whether concurrent resumes are serialized, rejected, or allowed to interleave, nor what a recycle does to a run already in flight. Harm: two callers resuming the same headless session interleave turns and corrupt each other's context, and the failure looks like bad answers rather than a lock error. Sure both gaps are in the text.

**30. Line 40 — "a returned pointer to a closed issue is filtered out with a note before the asker sees it"**
The filter is unconditional, but line 26 defines two legitimate reasons to consult the closed corpus — an absence claim and "an explicit precedent hunt" — and in both, closed issue numbers are the correct answer. As written, the post-check deletes exactly the pointers a precedent hunt asked for, and then counts those deletions toward recycle trigger two, so correct answers to precedent questions also drive spurious recycles. Harm: the precedent-hunt use case silently returns empty, and the recycle signal is polluted by the one case where closed pointers are right. Sure.

**31. Line 40 — "plus the changed-issue numbers since the last turn on resume"**
The resumed agent is handed a list of numbers, but nothing states what it does with them: whether it must re-read those entries from the mirror, whether it should distrust its in-context copies, or whether the numbers are informational. Line 36 establishes the agent "cannot notice" drift, which makes an unexplained list of numbers an unlikely repair. Harm: the mechanism that is supposed to bridge the gap between a per-turn-refreshed mirror and a stale in-context copy has no defined effect, so drift correction depends on unstated agent behavior. Sure the file does not say; unsure whether the omission is intended to leave it to the prompt.

**32. Line 40 — "One overall timeout; a killed run is a named failure"**
No duration is given, and "a named failure" states that a name exists without giving it. Harm: the wrapper cannot be implemented (the timeout must also fit inside the 600 s hook budget cited at line 50, a relation the file never states), and a caller cannot match on the failure it is told to expect — which matters because line 44 makes failure a first-class path that triggers the fallback ladder. Sure.

**33. Line 42 — "nedsmessenger runs this exact pattern live (… watchdogs on stuck runs)"** vs line 84 **"Multi-watchdog process supervision | … | One overall timeout suffices"**
The proof-of-mechanics cites watchdogs, plural, as part of the pattern being claimed as proven; the cuts table explicitly removes multi-watchdog supervision from version 1. The pattern is therefore not the exact one. Harm: the sentence's job is to convert a design risk into a settled fact, and it overstates by including the one component this design does not adopt — a later reader debugging stuck runs will look for a proven watchdog design that was never built here. Sure.

**34. Line 44 — "The fallback ladder: ask → grep the mirror → `gh` search"** vs line 50 **"Fail-open: ghi-info unreachable means the write proceeds unchecked"**
Two different behaviors are specified for the same event. When the write tool's consultation fails, line 44 says fall through to grepping the mirror and then a `gh` search; line 50 says the write proceeds unchecked. The file also never says who climbs the ladder — the writing agent, the wrapper, or the tool — and the three have different capabilities and different reasons to. Harm: the implementer picks one, and if they pick fail-open the ladder is dead code, while if they pick the ladder the hook can exceed its timeout budget on a degraded path. Sure.

**35. Line 44 — "then proceed under the ordinary rules"**
"The ordinary rules" is not defined here and no document is named. Harm: this is the terminal instruction of the degraded path — the state an agent is left in when everything else has failed — and it resolves to nothing actionable. Sure.

**36. Line 44 — "the cooperative posture"** (and line 56, "the cooperative threat model")
Two phrasings of an assumption that is never stated. It is load-bearing twice: it licenses accepting the missing-pointer residual, and it licenses leaving `gh api`, MCP tools, and creative quoting unguarded. Harm: an agent asked to decide whether some newly discovered bypass is acceptable has no stated criterion, and cannot tell whether the posture assumes agents are non-adversarial, that the repo is private, or something else. Sure the term is used without definition; the two spellings also make it hard to find both uses with one grep.

**37. Line 44 — "Self-correction needs no feedback channel: a writer who finds a relation ghi-info missed adds the cross-link while editing, and the next delta refresh teaches the corpus"**
The refresh updates the mirror on disk, but line 36 establishes that a resumed session's in-context copy does not update — that is the drift problem the recycling machinery exists to solve. So the correction reaches the corpus and not the agent, until an unrelated trigger happens to recycle it. Until then ghi-info keeps giving the same incomplete answer that prompted the correction. Harm: the sentence closes off building any feedback channel on the grounds that self-correction is already handled, when the mechanism described does not reach the component that was wrong. Sure. Secondary: "a writer… adds the cross-link" conflicts with line 17's "Cross-link edits are ghi-info's only write class" and the Division of labor row assigning cross-link edits to ghi-info — read one way, only ghi-info writes cross-links; read the other, writers do too.

**38. Line 48 — "Agents write issues with `gh` exactly as trained; the machinery makes the right thing happen without asking them to change"** vs line 52
The comment path does exactly the opposite: `gh issue comment` is denied, the agent is taught two alternatives, and one of them requires it to use "the tool's comment verb" with an event kind from a catalog — a new interface, not `gh` as trained — at an admitted cost of "One lost turn per attempt." Harm: the section's opening promise is the reason a reader would trust that no agent behavior change is needed; taken at face value it hides the one place where the design does require retraining, which is also the place issue #13 says the recurring problem lives. Sure.

**39. Line 50 — "the project write tool"** (and "the tool" throughout the section)
The tool is the central new component of the design and is never named. There is no identifier, path, or command to grep for. Harm: nothing else in the repo can reference it, the hook's `updatedInput` target cannot be written, and a future reader cannot find its implementation or its tests. Sure. This is also the naming shape CLAUDE.md's rule is aimed at — "the tool" is unsearchable prose.

**40. Line 50 — "The tool runs the mechanical checks (body length; openable references)"** vs line 51 **"the tool measures the body after the write"**
Body length appears in the pre-write check list of a tool whose whole purpose is to act "before an issue write lands" (line 18), and one paragraph later the measurement is explicitly after the write. Harm: an implementer cannot tell whether an over-length body blocks the write or is reported after it landed — a difference that determines whether over-length content is ever public, and whether the "told immediately" reply accompanies a success or a refusal. Sure.

**41. Line 50 — "replies in a form that cannot confuse an agent that believes it ran `gh`"** vs line 93 **"`gh` output mimicry: the tool's reply format against what agents expect from `gh` "**
The write path asserts the property as achieved and absolute; the verify list schedules it as an open question. Harm: a reader of the write path takes reply mimicry as solved and does not build for the case where an agent misreads the reply — for example treating a refusal as a successful write and moving on. Sure the two statements conflict in confidence; separately, "cannot confuse" is stronger than any reply format can guarantee, since the ordinary counterexample is an agent that parses `gh`'s exit status or its JSON output rather than its prose.

**42. Line 50 — "Fail-open: ghi-info unreachable means the write proceeds unchecked"**
"Unchecked" contradicts the same bullet's own sequence, in which the tool runs mechanical checks (body length, openable references) independently of ghi-info. Only the similarity adjudication is lost. Harm: an implementer reading "unchecked" may bypass the tool entirely on ghi-info failure, dropping the mechanical checks and the internal `gh` write path along with the consultation. Sure the word overstates what the bullet describes.

**43. Line 51 — "Over the limit, the writer is told immediately: keep a good summary in the body, move the substance to the linked pair MD"**
Nothing states what happens if the writer does not do the split — no re-check, no follow-up, no record that a body is outstanding over the limit. Line 20 gives ghi-info a flag-never-fix duty for over-length bodies, but as covered in finding 14 there is no defined place for a flag to go. Harm: an ignored or interrupted split leaves an over-length body in the corpus permanently, with no state anywhere marking it, which is the condition line 20 says the design cares about. Sure the case is reachable and its handling is not stated.

**44. Line 52 — "the body edit the revision convention requires"** and "the genuinely new events the convention permits"
"The revision convention" is treated as an established rule set — it determines where content lands, what supersession looks like, and which events legitimately warrant a comment — and it is neither described nor pointed at. Harm: the comment denial's entire justification rests on it, and the event catalog's boundary ("the genuinely new events the convention permits") cannot be applied without it. An agent that hits the denial is told to comply with a convention it cannot read. Sure.

**45. Line 52 — "resubmit through the tool's comment verb"**
No route exists for an agent to reach the tool directly. Every other interaction with the tool is described as a hook rewriting a `gh` invocation the agent already knows; the tool has no name (finding 39) and no invocation syntax anywhere in the file. Harm: the teaching reply instructs the agent to do something the design gives it no means to do, so the "one lost turn" becomes an unrecoverable loop rather than a retry. Sure.

**46. Line 52 — "an event kind from the fixed catalog (instance outcome, completion, ruling challenge; growth only by explicit ruling)"** vs the same line's **"One catalog detail is open… whether 'completion' collapses into close-with-reason"**
The catalog is called fixed and closed to growth in one clause, and one of its three members is declared unresolved in the next. Harm: an implementer cannot tell whether to build three kinds or two, and an agent choosing an event kind for a completion has two spellings available with no rule for picking. The three kind names are also not defined — what distinguishes an "instance outcome" from a "completion" is exactly the question left open, and "ruling challenge" is not explained at all. Sure.

**47. Line 53 — "**Close**: a state change with a reason (completed / not planned), not a comment; passes the hook untouched"**
Two reachable cases are left open by letting close through untouched. `gh issue close` accepts a comment argument, which routes comment text into the corpus through the one verb the design exempts from inspection — directly defeating the comment denial one bullet above. And `gh issue reopen` is never mentioned anywhere in the write path, though line 89 lists reopen as a state change the delta feed depends on. Harm: the comment rule has a hole its own section does not list among the known holes, and reopen's write-path treatment is undefined. Sure on both.

**48. Line 54 — "**Delete**: denied flat"** vs line 55 **"every deny path carries the audited one-use override"**
"Flat" states an unconditional denial; the next bullet states that no denial is unconditional. Harm: an agent facing a delete cannot tell whether the override is available, and an implementer cannot tell whether to wire one. The conflict also cuts against the file's own reasoning at line 82 ("absolutes backfire") and against CLAUDE.md's instruction that "absolute imperatives like 'always' or 'never' can backfire in unforeseen conditions. Use them cautiously" — the file argues that position while asserting a flat denial. Sure.

**49. Line 55 — "the audited one-use override (the instruction-file guard pattern already live on main)"** and **"the audit showing overrides used to dodge the tool"**
Three things are referenced as existing and none is locatable from this file: the guard pattern (named as "already live on main" with no path, no file, no example — and it is not in this checkout's CLAUDE.md), the override's invocation mechanics (how an agent requests one, what makes it one-use), and "the audit" itself (no log, no location, no reader, no cadence). Harm: the override is the pressure valve that every deny path depends on, and the audit is the named hardening trigger — the condition under which the design's softest choice gets revisited. Neither can be built or checked. Sure.

**50. Line 56 — "The maintenance sweep catches what slips — an unchecked write still appears in the delta"**
"The maintenance sweep" is referred to with a definite article as a known process; the file describes no such process — the nearest things are the supersession sweep and link-integrity scan listed in the Division of labor table, neither of which detects a duplicate or a too-similar body. And "appears in the delta" states only that the write becomes visible; nothing is defined that consumes the delta for after-the-fact adjudication, nor who acts on what it finds. Harm: this sentence is the sole compensating control for the accepted bypasses (`gh api`, MCP tools, creative quoting), so those holes are in practice uncompensated. Sure.

**51. Line 60 — "so the agent is never blocked into a retry"**
Not achievable even on the skill's happy path: a create that the adjudication judges "too similar" is refused (line 18), which is a block and a retry, and the skill cannot prevent it because similarity is only knowable after the draft exists. Harm: this claim is the stated reason the skill's description can stay firm rather than pushy (line 61), so an overbroad version of it is being used to discharge a design obligation. Sure.

**52. Line 61 — "a missed trigger costs efficiency (a late duplicate catch, one comment retry), never correctness"**
The absolute holds only when the hook fires and ghi-info answers. The file supplies two ordinary counterexamples of its own: line 50's fail-open, where an unreachable ghi-info lets a write land with no similarity check at all, and line 56's enumerated bypasses, where `gh api` or unusual quoting skips the hook entirely. In both, a missed skill trigger costs correctness, not efficiency. Harm: this claim is the load-bearing justification for the whole three-layer split — that the backstop makes undertriggering safe — so the overstatement propagates into the decision not to harden the skill description. Sure.

**53. Line 61 — "the pushy register's false-trigger test debt is never incurred"**
"The pushy register" and "false-trigger test debt" are used as established terms and neither is explained: what register is being contrasted with, what the debt consists of, or how it would have been paid. Harm: the sentence is the conclusion of the layering argument — why the skill description is written one way and not another — and a future agent editing that description cannot evaluate whether the reasoning still applies. Sure. Neither phrase is a searchable name for anything in the repo.

**54. Line 62 — "Written conventions demonstrably lose to training"**
The design depends on written conventions throughout: the revision convention governs where comment content lands (line 52), the fixed event catalog constrains comment kinds (line 52), the supersession marker convention makes currency legible (line 29), and the open-file-first grep default is stated as guidance in this very file. If written conventions lose to training, those mechanisms are unreliable by the file's own argument — and the evidence offered is one recurrence (issue #13). Harm: an agent applying this claim consistently would discount the conventions the design relies on; an agent applying it only where the file applies it has no stated rule for where the line falls. Sure the tension is internal to the file; unsure how strong the claim was meant to be, since "demonstrably" is doing work one datapoint does not support.

**55. Line 81 — "GitHub MCP server as the write surface | Generic writes carrying none of our checks | Never — the tool is ours"**
The column is headed "Grows back when" and asks for a condition; this cell supplies an unconditional refusal and a reason instead. The ordinary counterexample is in this file: line 91 flags that Codex-side hook equivalents are unverified, so a runtime where the hook cannot be installed is a foreseeable route to needing a different write surface. Harm: a cut recorded with no re-entry condition cannot be revisited on evidence, which is the table's purpose. Sure.

**56. Line 85 — "A cross-checkout grep need the per-machine regenerate cannot meet"**
The phrasing does not parse cleanly — "a cross-checkout grep need" reads as a noun phrase but scans as a missing word, and it is unclear whether the condition is a need to grep across checkouts, a need arising from cross-checkout greps, or something else. Harm: this is the trigger condition for committing the mirror, a decision that reverses the "Derived churn never enters git history" property; an agent evaluating whether the condition has been met cannot determine what it says. Unsure — I may be missing an intended reading, but I cannot recover one confidently from the text.

**57. Line 89 — "An issue's `updated` timestamp moves on close, reopen, and label changes as it does on body edits and comments (documented; untested here)"**
This is listed as unverified, yet two mechanisms in the body are already stated as working on the assumption that it holds: line 28's delta refresh moves entries between the two files "on state change," and line 53 asserts flatly that for a close "the delta feed carries it." Harm: if the assumption fails, closed issues never migrate out of `issues-open.md`, which breaks the file split, the absence-claim rule, the answer post-check, and the closes-since-birth recycle trigger — a wide blast radius presented in the body as settled behavior. Sure the confidence levels conflict.

**58. Line 91 — "Codex-side pre-tool hook equivalents (the runtime has hooks; field names unverified)"**
Codex appears here for the first and only time. Nothing earlier establishes that non-Claude agents are in scope, and the entire write path is specified against one runtime's PreToolUse hook and `updatedInput`. The file does not say whether Codex agents must be covered, what happens to writes from a runtime with no equivalent hook, or whether such writes fall under the accepted known holes. Harm: a reader cannot tell whether an uncovered runtime is a blocking gap or an accepted one. Sure the scope question is unresolved in the text.

**59. Line 92 — "The cross-reference timeline event as the backlink source for the graph (API shape)"**
The graph this file defines spans the issue–MD boundary (line 17: GHI→MD references and MD→GHI backlinks). GitHub cross-reference timeline events record references between GitHub objects; a Markdown file in a repo is not one, so this source appears to answer a different question than duty 2 asks. Harm: a build-time verification aimed at the wrong data source would be reported as passing while leaving the actual backlink direction unsourced — which compounds finding 10. Unsure — the item may be intended for an issue↔issue portion of the graph, but the file describes the graph only in issue–MD terms.

---

**clean sections:** Division of labor (its rows are cited within findings 3, 17, 25 and 37 but contain no defect anchored there).

