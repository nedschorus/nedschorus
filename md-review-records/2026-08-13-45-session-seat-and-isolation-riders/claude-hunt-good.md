<!-- provenance: runtime=claude model=claude-opus-5 effort=high cell=defect-hunt tier=good target=/home/nedlern/Projects/nedschorus/.claude/worktrees/gatekeeper-walk-fork-continuation/docs/issues/queue/45-session-seat-and-isolation-riders.md -->

Read the target file, `CLAUDE.md`, and every path the file names by explicit path (`.claude/settings.json`, `.claude/hooks/instruction-file-guard.py`, `scripts/launch-claude-*`, `docs/cross-project/fleet-machine-paths-and-checkouts.md`, `~/.claude/jobs/`).

## Findings

**1.** Title: `# Session seat and isolation riders` — and line 37, `Not riders, but hard-won and easy to lose:`

"riders" is never defined anywhere in the file, yet it is load-bearing: the last section's opening sentence draws a line between things that *are* riders and things that are not, so a reader must already know the criterion to place new material correctly. Nothing in the checkout's `CLAUDE.md` defines it either. Harm: a future agent asked to "add a rider to 45" or deciding whether a newly-discovered item belongs in this file has no test to apply, and item 5 ("Considered and deferred", explicitly not to be built) sits under the same heading as items 1–3 (proposals to build), so the term evidently covers at least two different dispositions. Also class (i): "rider" is a single generic word that will not be found by a search for anything a future agent would actually search for (session, isolation, queue, backlog). Confidence: sure.

**2.** Line 3: `Each item below was discussed and left undone deliberately — none is in flight.` versus line 9: `Resolve this before building the hook`

The preamble states the file is a record of *deliberately* dormant material; section 1 issues an imperative to an unnamed actor. The two support incompatible readings of what the file is: an inert queue entry that a reader should leave alone, or a work order with a prerequisite step. Harm: an agent that opens this file while draining the queue cannot tell whether starting the detection work honours the file or violates the "left undone deliberately" framing, and the imperative has no addressee — the file never says whether resolving detection is the reader's job, the user's, or a thing to be scheduled. Confidence: sure.

**3.** Line 7: `**The invariant:** two live sessions must never share a working directory.`

An absolute (class f) that is broader than the file's own mechanism and broader than the file's own reasoning. Two counterexamples from within the file: (a) the proposal in the next paragraph deliberately permits two live sessions in one directory so long as only one of them writes — "Reads and searches stay unblocked"; (b) line 9 describes attached viewer sessions, which are live sessions that occupy a directory harmlessly. So the guard being proposed does not enforce the invariant as stated; it enforces something narrower ("at most one live *writing* session per directory"). Harm: a later reader who takes the invariant at face value will judge the proposed hook incomplete, or will extend it to block reads, which the same paragraph says is wrong. Confidence: sure.

**4.** Line 7: `a background job launched from a session inherits the same cwd, which is what made several jobs look like duplicates in ~/agents/choirmaster`

`~/agents/choirmaster` appears here with no explanation of what it is — an agent's home, a worktree, a checkout — and its relationship to `<agents-root>/<name>` in section 2 is left for the reader to infer (i.e. that agents-root is `~/agents` on this machine). Harm: an agent implementing section 2's `--directory` flag needs to know the default layout it is overriding, and this file is the only place the layout is hinted at; the hint is in a different section, in a different notation, with no statement that they are the same thing. Confidence: sure that the connection is unstated; unsure whether the reader is expected to get it from `docs/cross-project/fleet-machine-paths-and-checkouts.md`, which is named in section 4 but never cited as the source of the path convention.

**5.** Line 9: `a PreToolUse hook on Edit|Write that refuses when another live Claude process shares this session's working directory` versus, three sentences later, `A working detector needs the session's own view of its working directory ... not the process table.`

The proposal is stated in terms of "another live Claude *process*", and the detection paragraph then rules out exactly that framing. Harm: the two sentences give a builder contradictory specifications of what is being detected. The conflict is not merely verbal: liveness ("another *live* session") is a process-table or job-directory property, while the working directory must come from the session's own view — the file never says how the two sources are to be joined, so the one piece of the detector it does prescribe is incomplete in a way the text hides. Confidence: sure.

**6.** Line 9: `Reads and searches stay unblocked; they never collide.`

Absolute (class f). The ordinary counterexample is the case this whole section is about: two sessions in one directory, one mid-edit, the other reading. The reader gets a half-written or since-superseded file and acts on it — a collision in the sense that matters here (wrong work product), even though nothing is corrupted on disk. Grep results and file listings go stale the same way. Harm: the sentence is used as the justification for scoping the guard to writes; if the justification is stated more strongly than it holds, a later reader cannot tell which part of it the scoping decision actually rests on. Confidence: sure the claim is overbroad as written; unsure how much the author intended "collide" to mean only "corrupt".

**7.** Line 9: `a PreToolUse hook on Edit|Write` and line 11: `The project already runs this exact pattern in .claude/hooks/instruction-file-guard.py.`

Two problems. First, the cited precedent is not the same pattern: `.claude/settings.json` registers that hook with matcher `Edit|Write|NotebookEdit`, and the hook's own docstring says "Wired as a PreToolUse hook on Edit, Write, and NotebookEdit". A builder who takes "this exact pattern" literally and copies the cited wiring will not get the `Edit|Write` matcher the proposal specifies, and will not know which of the two is intended. Second, class (g): `Edit|Write` leaves reachable mutation paths neither covered nor discarded — `Bash` (`sed -i`, `git checkout`, `git commit`, `rm`), `NotebookEdit`, and any MCP write tool all mutate the shared directory without tripping the hook. The file states the invariant as absolute (finding 3) but proposes a matcher that covers only part of the write surface, and never says the remainder is accepted risk. Harm: the guard would be believed to hold an invariant it does not hold, which is the precise failure mode line 9 warns about ("a guard whose detection is wrong is worse than none"). Confidence: sure.

**8.** Line 9: `teaching the fix — "another session is live here; call EnterWorktree first"`

The message is under-specified in two ways that block execution. (a) It does not say *which* session is blocked. In the fork case the file describes, the collision is symmetric: whichever session writes first after the fork gets refused, which may be the long-running parent rather than the fork that caused the collision — so the message can instruct the wrong session to relocate, moving an established seat out of its own directory. The file never states whether the newcomer or the incumbent is meant to yield, or how the hook would tell them apart. (b) It does not say which worktree `EnterWorktree` should enter — a new one, an existing one, on which branch — and the file's own section 3 says branch choice per session is itself an open discipline question. Harm: the deny message is the entire user-facing surface of this proposal, and as quoted it cannot be acted on without a decision the file does not record. Confidence: sure.

**9.** Line 9: `A working detector needs the session's own view of its working directory (its transcript records it, and the session itself answers correctly when asked)`

Taken literally as a detection method for a PreToolUse hook, the parenthetical's second half is impossible (class c): a hook is a short-lived subprocess evaluated synchronously in the blocking session; there is no described mechanism by which it can put a question to another live session and receive an answer, and the file offers none. The first half (transcripts) is feasible but incomplete — the file's own last bullet establishes that a transcript's *existence* says nothing about whether that session is still live, so a transcript-based detector needs a liveness source the file has just declared unreliable. Harm: the sentence reads as though two working approaches are available when one is not usable by a hook at all and the other is not sufficient on its own, so a builder starting from this paragraph will discover the gap only after building. Confidence: sure on the "answers when asked" half; sure the liveness gap is unaddressed.

**10.** Line 9: `Resolve this before building the hook`

Class (e): no stopping point. There is no statement of what a resolved detector must do — no accuracy bar, no enumerated cases it must handle (attached sessions, forks, background jobs, dead-but-untidied transcripts), no artifact that marks resolution (a note back in this file, a comment on the issue, a prototype). Harm: an agent that picks this up cannot tell when it may proceed to the hook, and a second agent cannot tell whether the first one finished. Confidence: sure.

**11.** Line 13: `a PreToolUse hook runs for every session on the machine, so a defect in it is felt everywhere`

Wrong as written (class c), and it contradicts the first half of its own sentence. The same sentence scopes the change to `.claude/settings.json` — this repository's project settings, which the existing wiring resolves through `$CLAUDE_PROJECT_DIR`. Those settings apply to sessions whose project is this repository (including its worktrees), not to every session on `ned-box`; sessions in `~/Projects/nedlern` or any other project are unaffected. Harm: the caveat is the file's stated reason to be careful, and it overstates the blast radius, which distorts the cost side of a decision a future reader is expected to make. If the author meant "every session in this project, including all worktrees and background jobs" — a real and non-obvious hazard — that is a different and narrower claim than the one written. Confidence: sure.

**12.** Line 13: `(instruction-class, so it lands through the user's walk)` and line 25: `Instruction-class text, so it lands through the user's walk.`

Class (d): "instruction-class" and "the user's walk" are both used as though they name a defined category and a defined procedure, and neither is defined in this file or in the checkout's `CLAUDE.md`. `CLAUDE.md` describes only one landing path — the git-gatekeeper and the interim "commit to the working branch, push it, and the user's Mac-side agent reviews and merges" lane — and never mentions a walk. Harm: this is the disposition instruction for two of the five items; an agent that has this file and `CLAUDE.md` cannot determine what it must do to land them, cannot tell whether "the walk" is a substitute for or an addition to the interim lane, and has no way to find the procedure by search — "the user's walk" is not a self-documenting or greppable name (class i). Confidence: sure. (The phrase does appear in `.claude/hooks/instruction-file-guard.py`'s docstring, but this file never points there, so it is only findable by luck.)

**13.** Line 17: `place every agent in <agents-root>/<name> by convention and accept no directory argument, so a seat cannot adopt an existing worktree`

`<agents-root>` is a placeholder with no expansion given anywhere in the file — and the two launchers named are for different machines, so a single expansion may not even exist. Harm: this is the entire statement of current behaviour that the proposed flag would modify; a builder cannot verify the claim or write the override without opening the scripts, which the file gives no indication is necessary. Confidence: sure.

**14.** Line 17: `so a seat cannot adopt an existing worktree` versus line 11: `EnterWorktree already relocates a running session correctly`

Direct conflict (class a) plus an overbroad "cannot" (class f). By the file's own section 1, a launched seat *can* adopt an existing worktree — by calling `EnterWorktree` after launch. What section 2 actually establishes is narrower: it cannot be launched directly into one. Harm: the sentence is the justification for the whole item; as written it claims an impossibility the file elsewhere refutes, so a reader cannot tell how much of the item's value survives (which is exactly the question the item's own last paragraph tries to answer for a different reason). Confidence: sure.

**15.** Line 17: `Roughly ten lines per launcher.`

Class (g): the item defines a mechanism — a `--directory` flag — and leaves every failure and limit case unstated: what happens when the directory does not exist, is not a git worktree, is not writable, already contains a live session, or conflicts with the `<agents-root>/<name>` path the launcher would otherwise use. The last of these matters most: a `--directory` flag is the most direct way to put two seats in one directory, which section 1 declares must never happen. The file proposes both items without reconciling them, and never says the interaction was considered. Harm: the ten-line estimate is only credible if none of these cases needs handling, and the file does not say so; a builder either implements a flag that can break the file's own invariant, or discovers the scope is larger than advertised. Confidence: sure.

**16.** Line 19: `Weakened, though not eliminated, by a fact discovered the same day: claude attach <job-id> already opens a background session in a terminal`

Two issues. Minor: "the same day" has no local antecedent — the nearest date in the section is none, and the reader must reach back to the preamble's "conversation 2026-08-13" to resolve it; a reader who lands in this section from a search will not. Substantive (class a/b): section 1 states that an *attached* background session is precisely the case that defeats cwd detection ("an attached background session's process reports the directory the `claude attach` command was typed in"). So this sentence recommends, as the cheap alternative to item 2, the practice that item 1 identifies as the source of its open problem — and never says so. Harm: a reader weighing these items together gets no signal that adopting the attach route makes item 1's detection problem more common, not less. Confidence: sure the tension is unstated; unsure whether the author considered and dismissed it.

**17.** Line 21 heading: `A CLAUDE.md rule: background jobs push their own branch` versus line 23: `One branch per session removes the class ... so the discipline only has to cover which branch a session pushes.`

The heading binds *background jobs*; the body reasons about and prescribes for *sessions*. The file itself insists these are different things — line 39: "**Job ids are not session ids.**" — and a session can exist without being a background job (an interactive tmux seat) while a job hosts a session. Harm: this item is to become a rule in `CLAUDE.md`, i.e. text that agents must obey literally; as drafted it supports two incompatible scopes (only background jobs must branch, or every session must), and the two produce different behaviour for exactly the interactive-seat case the rest of the file is about. Confidence: sure.

**18.** Line 23: `git already refuses one branch in two worktrees`

Overbroad (class f) and load-bearing, since it is the premise for "the discipline only has to cover which branch a session pushes". Counterexamples, in rising order of relevance to this file: `git worktree add --force` overrides the refusal; a detached HEAD at the same commit is not refused; two separate clones have no such protection at all — and the checkout's `CLAUDE.md` establishes that the Mac and `ned-box` are exactly that ("The same `~/Projects/...` paths exist on both machines and are different clones"), so nothing stops both machines' agents from working the same branch name; and most directly, the hazard this whole file exists to address — two sessions sharing one working directory — is a single worktree with a single branch, where git refuses nothing whatsoever. Harm: the premise is used to shrink the scope of a proposed rule, so if it holds less widely than stated, the rule is scoped too narrowly and the failure it was meant to remove (merge commits from concurrent pushes to a shared branch) can recur through any of these routes. Confidence: sure.

**19.** Line 21–25 (the whole item)

Class (e)/(d): the item calls for a `CLAUDE.md` rule but supplies neither the rule's text nor the branch-naming scheme it depends on. "their own branch" gives no answer to: named how, derived from what (session id, job id, agent name, task), and what a session that was started on an existing branch — as this very checkout was, on `seat-launch-first-prompt` — is supposed to do. Harm: an agent asked to land item 3 must invent a naming convention, which the checkout's `CLAUDE.md` explicitly regulates ("use explicit, clear and precise multi-part names ... If the thing you are naming already has a name in the project, use the existing name"), and there is no stated criterion for when the rule is written correctly enough to stop. Confidence: sure.

**20.** Line 21/23: `background jobs push their own branch` / `One branch per session` versus checkout `CLAUDE.md`: `the interim lane applies: commit to the working branch, push it, and the user's Mac-side agent reviews and merges`

The two texts use different, unreconciled models of what a session pushes. `CLAUDE.md` says "**the** working branch" — definite article, one branch per unit of work, inherited by whoever is doing that work; this file says one branch per *session*, so two sessions working the same change must push two branches, and the Mac-side reviewer then has two branches to merge for one piece of work. Harm: an agent obeying both cannot tell whether joining an in-progress change on an existing working branch is permitted or forbidden, and the reviewing seat's workload changes materially depending on the answer. Confidence: sure the two readings conflict in practice; unsure whether the author intends the new rule to supersede or merely refine the `CLAUDE.md` sentence, because the item does not say.

**21.** Line 29: `Not yet reviewed.` (and, supporting it, line 3: `none is in flight`)

This appears to be false in this checkout. `md-review-records/2026-08-13-fleet-machine-paths-and-checkouts/` exists at the repository root and contains nine review artifacts (`claude-hunt-good.md`, `codex-restate-floor.md`, `reference-check.md`, and so on) — i.e. an md-review of exactly this document was run, dated the same day the file says it was written. Harm: this is a claim of current state with no stated expiry and no pointer to where the answer actually lives, so it goes stale silently; a future agent that trusts it will re-run a review that has already been performed, or conclude the records directory is for something else. The file gives no way to check the claim short of guessing at the records directory's existence. Confidence: sure the directory exists and matches this document by name; unsure only whether that review was completed or abandoned, which is itself the point — the file provides no way to tell.

**22.** Line 29: `which is the md-review skill's stated target`

Class (d): "the md-review skill" is referenced by bare name, with no path, no invocation, and no quotation of the "stated target" being appealed to. Harm: the entire justification for item 4 is an appeal to a document the file does not locate; a reader with only this file cannot check whether the fleet paths reference actually qualifies, and cannot find the skill by path search from anything written here. Confidence: sure it is unlocated; unsure how much this matters in practice, since a Claude Code agent is shown its available skills by the harness — but that is a property of the runtime, not of this file, and the file is meant to stand on its own.

**23.** Line 31 heading: `Migrating choirmaster to a machine-suffixed name`

The target naming scheme is never given — no example, no rule (suffix with hostname? `-ned-box`/`-mac`? separator?). Harm: this is the deliverable of the item, deferred rather than cancelled, so a future reader deciding to "revisit" has to re-invent the decision the heading implies was already made. The checkout's `CLAUDE.md` requires explicit multi-part invented names, so the missing scheme is not a detail that can be safely improvised later. Confidence: sure.

**24.** Line 33: `The two machines share no agent state, so the same name on both is two unrelated agents rather than a conflict`

Overbroad (class f) and contradicted within the file (class a). The machines share the git remote, and the file itself quotes `Merge remote-tracking branch 'origin/choirmaster' into choirmaster` — a branch named after the agent, living on the shared remote, reachable from both machines. Handoff files are likewise committed (line 33 says migrating means "renaming its handoff files", so they are files under version control). So agent-scoped state *is* shared, through the repository, and two identically-named `choirmaster` agents on two machines pushing `choirmaster` is exactly the concurrent-push collision item 3 exists to remove. Harm: this sentence is the sole premise for deferring the item ("the suffix buys legibility ... nothing more"), and it is the premise that fails — the suffix would also disambiguate branches and handoff files on the shared remote. A future reader revisiting this deferral will re-derive the same wrong conclusion. Confidence: sure.

**25.** Line 33: `renaming its handoff files`

Class (d): no path, no naming pattern, no count. Harm: this is one of three enumerated costs used to justify the deferral, and a reader cannot size it or perform it; nothing in the file says where handoff files live or how an agent's name appears in them. Confidence: sure.

**26.** Line 33: `Revisit only if a cross-machine listing becomes a routine view.`

Class (g): the revisit condition is, by the file's own facts section, unreachable with the tooling the file documents. Line 41 states that the agent view "must run on the machine whose sessions you want", and line 42 states "**No side-by-side view exists** in the harness". A listing that spans machines therefore cannot become routine without a capability the file says does not exist — and the file does not say what would have to change for the condition to become satisfiable, nor does it discard the item outright. Harm: the item is left in a state that reads as "deferred pending a trigger" but functions as "dead", and no reader can tell which; the difference determines whether the item should stay in the queue at all. Confidence: sure the two statements are in tension; unsure whether the author meant "listing" loosely enough (e.g. two ssh commands side by side, which line 42's own next sentence describes) to escape it — which is itself the ambiguity (class b).

**27.** Line 41: `**It must run on the machine whose sessions you want.**`

The bolded lead of a standalone bullet has no subject; "It" resolves only by reading the *previous* bullet. Harm: the bullets in this section are written as individually liftable facts (each is bolded-lead, self-contained, and the section is explicitly framed as facts to preserve), so this one breaks when quoted, moved, or reordered — and a reader who arrives here from a search for "must run on the machine" gets a sentence about no identifiable command. Confidence: sure.

**28.** Line 40: `Ctrl+T pins against the ~1-hour idle reap`

Class (d)/(g): the "idle reap" is referenced as a known mechanism but never explained — what is reaped (the session? the job directory? the transcript?), whether the reaped session's work is recoverable, and whether it applies to attached sessions, pinned sessions after some longer interval, or sessions with running tools. Nor is the pin's own limit stated: whether it lasts for the session's life, until unpinned, or until restart. Harm: the reap is the reason the file's fleet lost sessions, and every decision about leaving a background job unattended depends on the answers; a reader gets the mitigation keystroke without the model it mitigates. The "~" honestly signals imprecision in the interval, which is fine; the missing part is the consequence. Confidence: sure.

**29.** Line 42: `**No side-by-side view exists** in the harness. Simultaneous views mean one terminal (or tmux window) per session.`

Mild internal conflict (class a/f): the second sentence describes an arrangement — several tmux windows, one per session — that is a working side-by-side view of multiple sessions, so the absolute in the first sentence holds only under an unstated restriction ("no *single-pane, harness-native* multi-session view"). Harm: small, but the bullet is written to be quoted, and quoted alone it tells a reader that simultaneous viewing is unavailable when the file's own next clause says how to do it. Confidence: unsure — the qualifier "in the harness" arguably carries the restriction, but it is doing more work than it visibly can.

**30.** Line 43: `the modification time of its transcript under ~/.claude/projects/<project>/<session-id>.jsonl`

Wrong when obeyed literally (class c). `<project>` reads as the project's name, but the actual directory is the project's absolute path with separators mangled — on this machine, `~/.claude/projects/-home-nedlern-Projects-nedschorus/`. An agent substituting a project name (`nedschorus`) into that path finds nothing and may conclude the transcript is missing, which inverts the staleness check the bullet exists to teach. The file gives the placeholder no expansion, no example, and no note that the encoding is non-obvious. `<session-id>` has the same shape of problem in a milder form: the preceding bullets establish that ids come in two kinds and that mixing them fails, and this placeholder does not say the UUID (not the 8-character job id) is the one that names the transcript. Confidence: sure on `<project>`; sure that `<session-id>` is unhelpfully bare given the file's own warning about the two id kinds.

clean sections: none — every section (preamble, 1, 2, 3, 4, 5, and "Session-management facts worth keeping") carries at least one finding.

