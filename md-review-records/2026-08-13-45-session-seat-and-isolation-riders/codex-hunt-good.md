<!-- provenance: runtime=codex model=gpt-5.6-sol effort=xhigh cell=defect-hunt tier=good target=/home/nedlern/Projects/nedschorus/.claude/worktrees/gatekeeper-walk-fork-continuation/docs/issues/queue/45-session-seat-and-isolation-riders.md -->

1. “Each item below was discussed and left undone deliberately — none is in flight.” The status has two compatible readings: none was in flight on 2026-08-13, or none is in flight whenever the file is read. The date grammatically qualifies the preceding conversation, not this status claim. A later agent cannot know whether starting an item would duplicate active work. Confidence: unsure—the author may intend the date to scope the whole paragraph, but the sentence does not say that.

2. “A guard that enforces one live session per directory”; “two live sessions must never share a working directory”; and “a PreToolUse hook on `Edit|Write` that refuses when another live Claude process shares this session's working directory” describe different guarantees. A fork or background session already shares the directory before either session invokes a tool, so the hook does not enforce occupancy. It also leaves mutation through `Bash`, `NotebookEdit`, and other tools unguarded; the referenced existing hook explicitly includes `NotebookEdit`. Conversely, a second read-only session violates the literal invariant even though the proposal deliberately leaves it unblocked. This makes it impossible to tell whether the required invariant is exclusive directory occupancy or merely protection against concurrent mutation. Confidence: sure.

3. “two live sessions” and “another live Claude process” switch the identity being counted without defining either “live” or the session-to-process relationship. The same paragraph acknowledges that viewer processes masquerade as sessions and real sessions can hide. Pinned idle sessions, stopped-but-not-reaped jobs, foreground sessions, background sessions, and multiple viewers attached to one session therefore have no stated classification. The detector cannot make a consistent allow/deny decision from this specification. Confidence: sure.

4. “call `EnterWorktree` first” and “`EnterWorktree` already relocates a running session correctly” introduce an undefined executable name. None of the allowed material identifies whether it is a tool, command, skill, or procedure; gives its invocation or preconditions; or explains what “correctly” guarantees when the directory has changes, an existing worktree, or another live session. A blocked agent therefore cannot reliably obey the denial message. Confidence: sure.

5. “Reads and searches stay unblocked; they never collide.” “Never” is broader than the actual safety property. A search running while another session performs a multi-file refactor can observe a mixture of old and new files and produce an internally inconsistent result. The statement is true only of concurrent non-mutating operations considered in isolation, not of reads or searches racing with the writes this document is concerned about. Confidence: sure.

6. “A working detector needs the session's own view of its working directory (its transcript records it, and the session itself answers correctly when asked), not the process table. Resolve this before building the hook.” This is not an executable investigation or a bounded task. It does not explain how to enumerate live sessions, associate them with transcripts, obtain the current rather than historical directory, query another session from a synchronous hook, distinguish an attached viewer, or classify unavailable and malformed state. “Resolve” has no stated validation cases or stopping criterion, so different implementations can claim success while retaining the failures already described. Confidence: sure.

7. “Detection is the open problem” may already be stale. The installed Claude Code 2.1.232 exposes `claude agents --json`, documented as returning active interactive and background sessions, and its output contains `cwd`, `kind`, `sessionId`, and `state`. That is a plausible machine-readable source for exactly the identity and directory information sought here. Leaving the claim unqualified can send a future agent back into unnecessary detector design. Confidence: unsure—the document records 2.1.231, and I did not establish whether `--json` existed there or whether its `cwd` remains correct for every attached-session case.

8. “a guard whose detection is wrong is worse than none” is an overbroad absolute. An ordinary counterexample is a conservative detector with some false negatives but no false positives: it blocks some genuine collisions, never teaches a false lesson, and is more useful than no guard. “Wrong” also fails to distinguish false positives, false negatives, transient failures, and incomplete coverage, although those have different consequences. Confidence: sure.

9. “The project already runs this exact pattern in `.claude/hooks/instruction-file-guard.py`.” The referenced hook is not an exact behavioral pattern: it matches `Edit|Write|NotebookEdit`, classifies protected paths, and permits a one-use approval-marker override. The proposed guard names only `Edit|Write`, uses session detection, and states no override. “Exact pattern” can therefore mean merely “a denying PreToolUse hook with an instructional message” or the referenced hook’s full behavior; those readings produce incompatible implementations. Confidence: sure.

10. “instruction-class, so it lands through the user's walk” introduces `instruction-class` as a category without defining it. The referenced guard partially explains approval through `.walk-approved`, but neither it nor `CLAUDE.md` explains whether “lands through the user's walk” means approval, installation, commit, merge, or some combination. This matters when deciding when the queued work is complete. Confidence: unsure—the referenced guard supplies part of the intended workflow, but not the meaning of the category or “lands.”

11. “a PreToolUse hook runs for every session on the machine, so a defect in it is felt everywhere.” The referenced `.claude/settings.json` is project-local, and both launcher files say those settings are read from `.claude/` in the session’s working directory. A session in another repository, a bare directory, a stale checkout, or `claude --bare` does not run this hook. The sentence consequently overstates both coverage and failure radius. Confidence: sure.

12. “a seat cannot adopt an existing worktree”; “a visible tmux seat”; and “a seat is not the only route to visibility” use `seat` without defining whether it means a tmux session, terminal, Claude conversation, named agent, working directory, or the whole launcher-managed bundle. Those meanings differ materially for attaching, moving, renaming, and counting sessions. Confidence: sure.

13. “`scripts/launch-claude-ubuntu` and `scripts/launch-claude-mac` place every agent in `<agents-root>/<name>` by convention and accept no directory argument, so a seat cannot adopt an existing worktree.” The conclusion does not follow literally. Both referenced launchers accept `NEDSCHORUS_AGENTS_ROOT` and derive the directory as that root plus the supplied name. Pointing the root at an existing worktree’s parent and supplying its basename causes the launcher to adopt it without a directory flag. The current interface is inconvenient for arbitrary paths, but adoption is not impossible. Confidence: sure.

14. “That makes ‘promote this background thread to a visible tmux seat’ harder than it needs to be” does not describe an outcome a directory flag can produce. The launchers start a new supervisor and Claude session in the selected directory; they do not attach to or preserve the background conversation. That can create the duplicate-directory situation prohibited by section 1. The next paragraph says `claude attach <job-id>` already opens the actual background session, but “weakened, though not eliminated” never identifies what requirement remains unmet—visibility, tmux persistence, naming, supervision, or handoff behavior. There is consequently no acceptance boundary for the launcher change. Confidence: sure.

15. “A CLAUDE.md rule: background jobs push their own branch” and “One branch per session removes the class” give incompatible scopes. The first applies only to background jobs; the second applies to every session. A foreground seat, attached background job, and long-lived launcher-managed agent would be treated differently under those readings. Confidence: sure.

16. “One branch per session removes the class: git already refuses one branch in two worktrees.” Git’s refusal is limited to linked worktrees sharing one repository’s administrative state. Two sessions can still share the same worktree—the problem section 1 describes—and independent Mac and Ubuntu clones can each check out and push the same branch. The referenced fleet document explicitly identifies two clones pushing one remote branch as the cross-machine collision surface. A session can also push to a remote branch other than its checked-out branch. Thus the Git behavior does not establish or enforce the claimed session-level isolation. Confidence: sure.

17. “the discipline only has to cover which branch a session pushes.” The branch mechanism leaves reachable cases unclassified: how a session obtains its dedicated branch, how session identity maps to a branch name, whether “branch” means the local source or remote destination, what happens when a session starts on a shared branch, and what happens when it changes worktrees or resumes after another session. Without those semantics, the proposed rule cannot determine compliance or completion. Confidence: sure.

18. “Instruction-class text, so it lands through the user's walk.” This repeats the undefined category and workflow from finding 10 without adding an artifact path, action, or stopping condition. A future agent cannot tell whether drafting text, receiving approval, writing `CLAUDE.md`, or integrating the change is the work represented by this item. Confidence: unsure—the referenced instruction guard describes an approval override, but not the full process asserted here.

19. “which is the md-review skill's stated target. Not yet reviewed.” No `md-review` definition, skill location, invocation, review criteria, output location, or completion condition appears in the permitted context. The name is also not self-documenting enough to reveal whether it performs prose editing, issue-spec validation, or another review. The queued item cannot be executed or conclusively marked complete from this file. Confidence: sure.

20. “The two machines share no agent state, so the same name on both is two unrelated agents rather than a conflict” and “the suffix buys legibility in a listing that spans machines, nothing more.” The referenced launchers use the agent name as the Git branch name, while the referenced fleet document says pushed branches are the shared cross-machine state and that two clones pushing the same branch fight over one remote ref. Using `choirmaster` on both machines can therefore reproduce the shared-branch conflict described in section 3. A machine suffix can separate remote branch names, so its effect is not limited to listing legibility. Confidence: sure.

21. “Migrating the live seat means `git worktree move`, renaming its handoff files, and a restart.” The referenced launcher documentation says the name controls the tmux session, home directory, handoff files, supervisor identity, and branch creation. The stated sequence does not account for the checked-out branch name, tmux/session identity, supervisor lock identity, or any name stored in the agent’s local instructions. Taken as an exhaustive description—“means”—it can leave a partially renamed agent that still shares or pushes the old branch. Confidence: sure.

22. “Revisit only if a cross-machine listing becomes a routine view.” “Routine” supplies no observable threshold or decision boundary. Occasional daily use, automated display, or repeated manual use can all satisfy different ordinary readings, so a future agent cannot determine whether the deferral condition has occurred. Confidence: sure.

23. “`Ctrl+S` groups by directory, which makes the shared-directory hazard visible” is in unexplained tension with “Detection is the open problem” and the assertion that a detector needs the session’s own directory view. The document says the agent view already has session-aware directory grouping but does not explain why its underlying data is unusable by the hook. The current command’s machine-readable `--json` mode makes that omission more consequential. Confidence: unsure—the interactive grouping may have no supported programmatic source in the recorded 2.1.231 version.

24. “No side-by-side view exists in the harness. Simultaneous views mean one terminal (or tmux window) per session.” “Harness” is undefined, making the boundary of the absolute claim unclear. An ordinary counterexample is a terminal or tmux window split into two panes, each running `claude attach` for a different session; the views are side by side in one tmux window. If “harness” means only the built-in agent-view UI, the first sentence may be true, but the file does not state that narrower meaning and the second sentence remains literally false about tmux windows. Confidence: unsure because the intended narrow meaning of “harness” may exclude terminal layout.

25. “the honest staleness check is the modification time of its transcript under `~/.claude/projects/<project>/<session-id>.jsonl`.” The procedure is not executable from the preceding facts: the encoding of `<project>` is unstated, the visible job ID is explicitly not the session ID, no job-to-session mapping is supplied, and no age defines “stale.” Transcript mtime is also not a reliable synonym for activity: a live long-running tool call may produce no transcript write until it finishes, while an automated or non-meaningful append can refresh the mtime. Missing, rotated, or unwritable transcripts are unclassified reachable cases. Calling this the “honest” check overstates what the store can establish. Confidence: sure.

clean sections: none
