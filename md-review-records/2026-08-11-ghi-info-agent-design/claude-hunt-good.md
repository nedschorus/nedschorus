<!-- provenance: runtime=claude model=claude-opus-5 effort=high cell=defect-hunt tier=good target=/Users/el/Projects/nedschorus/.claude/worktrees/linters-tool-usage-f8b0a0/docs/drafts/ghi-info-agent-design.md -->

## Findings

**1. `design-as-of: 2026-08-09` (line 3)** — The frontmatter dates the design at 2026-08-09, but the body carries rulings and content dated two days later: "processed 2026-08-11 → APPROVED (user)" (line 19), "(user-ruled 2026-08-11: worded here rather than left to build)" (line 119), and seven prompt subsections labeled "approved 2026-08-11". Harm: an agent that trusts the frontmatter as the currency marker — the obvious use of a field named `design-as-of` — will treat every 2026-08-11 ruling as unrecorded or as a later unincorporated change, and may re-litigate settled items. Confidence: sure.

**2. `status: ... awaiting the user's integration walk` (line 2)** — The walk block immediately below records the integration walk as opened 2026-08-09 and five of six items already processed, with item 6 marked "open 2026-08-11". The walk is under way, not awaited. Harm: a reader deciding whether the walk needs scheduling gets the opposite answer from the two adjacent statements. Confidence: unsure — "awaiting" could be a loose way of saying "not yet closed," but nothing in the file says which reading is meant.

**3. `the over-specification correctness rule lands in the skill-authoring checklist's Register section` (line 11)** — Three referents appear once each and are never defined or given a path anywhere in this file: "the over-specification correctness rule," "the skill-authoring checklist," and its "Register section." The sentence also asserts the rider was "applied same day," so an agent cannot even verify the disposition without knowing what document is meant. Harm: any successor asked to confirm this rider landed, or to reconcile it with the design, is stopped at the first term. Confidence: sure.

**4. `which the standing reviewer counterpart rule protects` (line 13)** — "the standing reviewer counterpart rule" is named as an operative protection but is never stated, defined, or pointed at. The clause that follows ("re-flagged only with evidence matching the named reopening trigger, never as an ordinary finding") reads like a paraphrase of it, but the sentence presents the rule as an external standing thing, not as this text. Harm: a reviewer told a rule constrains their findings, and unable to read that rule, must either ignore it or guess its scope. Confidence: sure.

**5. `Prompt drafting in progress; § Prompts' owed/final labels are the per-prompt marks.` (line 21) vs `**Status: every prompt is final**` (line 119)** — Item 6 is the one open walk item and says drafting is still under way, pointing at per-prompt owed/final labels; § Prompts says all prompts are final and every subsection is labeled approved. Line 19 likewise records "four prompts still owed there." Harm: an agent resuming this work cannot tell whether its next job is to draft prompts or to run the md-review; both statements are current-state claims, not one snapshot and one update. Confidence: sure.

**6. `→ landing decision ... → file the build GHI → queue the riders` (line 21)** — Three of the four close-out steps are un-executable from this file. "the build GHI" is never defined (no scope, no content, no statement of what issue it is); "queue the riders" names no queue and no filing procedure; and the third rider, "#26 lifecycle revision" (line 20), never says what about #26's lifecycle is to be revised. Harm: the close-out sequence is the file's own definition of what remains to be done, so the file's last unfinished work is precisely the part it does not describe well enough to perform. Confidence: sure.

**7. `the corpus fits in its context window (measured today: 45 issues ≈ 109 KB)` (line 25)** — "today" is a relative date in a durable document. Elsewhere the same measurement session is dated absolutely: "Measured 2026-08-07 against the live repo" (line 43). Harm: a reader in six months cannot tell whether the 45-issue figure is from 2026-08-07, from the design-as-of date, or from some third day — which matters, because the whole "no vector database" decision rests on that number and on watching it grow. Confidence: sure.

**8. `every GHI→MD reference resolves on main; every pair MD backlinks its correct GHI(s)` (line 32)** — Stated as the maintained invariant of duty 2, but the file later concedes the scan cannot establish half of it: "an author who lands the MD but never completes the cite step goes uncaught by this sweep; the link-integrity scan does not catch it either (a never-added link resolves vacuously)" (line 94). A missing link is undetectable by a scan that only checks links that exist. Harm: an agent relying on duty 2 will treat a clean sweep as proof that every pair is mutually linked, which it is not; the absolute "every" is what makes the residual on line 94 read as a contradiction rather than a scope note. Confidence: sure.

**9. `Detected problems beyond links ... spawn fixers` (line 32)** — This sits inside `ghi-info`'s duty list, attributing detection and spawning to the agent, while § Maintenance and fixers assigns both to scripts: "The sweep is script work riding the two feeds ... Findings spawn one-shot focused fixer agents" (line 94), and the Division of labor table gives the scans to "sweep scripts" (line 108). Harm: at build time, whoever implements duty 2 must decide whether the length check and staleness check live in `ghi-info`'s turn or in a script, and the two passages point opposite ways — with a cost difference (a model call vs. free) the table itself tracks. Confidence: unsure — line 32's "spawn fixers" may be describing the system rather than the agent, but the sentence's subject is `ghi-info`.

**10. `the write is refused with a merge instruction: read #n, then merge this content into it by editing it` (line 33)** — Adjudication runs on edits as well as creates (the same paragraph says "plus, for edits, the target issue number, which is excluded from the comparison"), so a `too-similar #n` verdict is reachable when the author is editing an existing issue #x. In that case the instruction tells the author to merge the content of an edit-in-progress into a different issue, and the corresponding refusal text adds "do not file a new issue" (line 252) — advice about an action the author was not taking. Nothing states what becomes of #x, which still exists and still holds the old body. Harm: the author is blocked from editing an issue and handed an instruction that does not describe the situation. Confidence: sure.

**11. `asked about the wiki or the code, it returns a fixed `out-of-scope` reply` (line 35)**, restated in the prompt as "Asked anything beyond the issue corpus — the wiki, the code, anything else — reply exactly: out-of-scope." (line 201) — Request form 4 asks `ghi-info` to repair a link in a repository document ("docs/issues/31-foo.md backlinks #29, but its issue is #31", line 236) and to commit that change. A document in the checkout is "anything else" beyond the issue corpus; obeyed literally, the boundary rule makes `ghi-info` refuse its own single write class. Harm: the one write duty the design assigns to `ghi-info` is refused by `ghi-info`'s own boundary instruction, in the same prompt. Confidence: sure.

**12. `Whether an old ruling still binds always escalates to the user.` (line 35)** — No path exists in this file from `ghi-info` to the user. Its replies go to `ghi-info-ask.py` (which post-checks pointers and prints a list, lines 64–65) or to the write tool (which reads one `verdict:` line and treats anything else as unavailable, lines 33 and 216). An `escalate: <one sentence...>` reply (line 202) therefore reaches the user on neither path; on the adjudication path it is silently consumed as a malformed reply and the write proceeds. Harm: the escalation the design calls mandatory never arrives, and on the write path its only visible effect is to skip adjudication. Confidence: sure.

**13. `The authoritative copy lives in `ghi-info`'s checkout on the box.` (line 39)** — The same paragraph says the mirror is "regenerated by `scripts/ghi-mirror-refresh.py` on any machine" and that "GitHub is the source of truth; the mirror is derived data." What authority a particular copy of regenerable derived data carries, and what depends on it, is not stated. Harm: a builder deciding whether a Mac-side copy is usable, or whether the box copy must be reachable for anything, has a word that sounds decisive and no rule behind it. Confidence: unsure — this may be shorthand for "the copy `ghi-info` actually reads," but the sentence claims more than that.

**14. `a "no issue covers X" receipt is invalid unless both files were searched` (line 41)** — The adjudication path issues exactly such receipts and never searches the closed file. `verdict: unrelated` is an absence claim, and `too-similar`/`related` rest on the same absence reasoning about everything not named; but the adjudication request (lines 218–228) carries no closed-history instruction, and the cold-start prompt tells `ghi-info` to "grep it only when a request asks about closed history" (line 188). Only the ask path has `--include-closed` (line 63), and there is no equivalent flag for the write tool's consult. Harm: by the file's own rule, every adjudication verdict is an invalid receipt — including the `too-similar` verdicts that refuse writes. Confidence: sure.

**15. `Routine relatedness checks grep the open file` (line 41)** — The actor is unnamed, and both candidate readings conflict with something. If `ghi-info` is the grepper, it contradicts "Read this file whole now, before anything else" (line 187) and the design's premise that the corpus lives in the context window (line 25). If the grepper is a fallback agent, this belongs to the ladder at line 69, not to a paragraph describing the mirror's two files. Harm: whoever builds the adjudication step cannot tell whether grep is part of the expected behavior or the degraded path. Confidence: unsure — the surrounding paragraph is about file layout, which weakly favors the second reading, but nothing settles it.

**16. `Mirror writes go temp-then-rename, so concurrent refreshes are safe.` (line 43)** — Temp-then-rename makes each file's replacement atomic, but the mirror is two files whose contents are coupled: a delta "mov[es] entries between files on state change" (same paragraph). Two concurrent refreshes, or one reader between the two renames, can therefore see an issue in both files or in neither — the reachable failure the mechanism does not cover. Concurrency here is not hypothetical: the file requires it ("If another ask holds the session, cold-start a throwaway session instead", line 62), and every ask begins with a refresh. Harm: a `ghi-info` cold start can load a corpus in which a just-closed issue is still open, or has vanished; the post-check reads the same possibly-torn pair. Confidence: sure.

**17. `the refresh fetches origin and reads `git log origin/main` ... they enter the corpus when they land on main` (line 45)** — The mirror is defined as exactly two files, both derived from issues ("Two files, split by state", line 41), so this feed's output has no stated destination — nothing says what is written, where, or what `ghi-info` sees of it. The sentence also says MD edits "enter the corpus," while the corpus is elsewhere the issue corpus and anything beyond it is out of scope (line 35). Harm: the sweep's link-integrity scan and the staleness check both depend on this feed (line 94), and a builder has neither a store nor a format for it; a reader also gets two incompatible meanings of "the corpus." Confidence: sure.

**18. `an activity-relative freshness — project events since this issue last moved` (line 47)** — "project events" is never defined: it is not stated whether an event is an issue touch, a commit on main, a sweep run, or something else, so the number cannot be computed or interpreted. The field is also absent from both enumerations of what an entry carries — "number, title, labels, updated time, body, comments" (line 41) and the identical list in the cold-start prompt (line 187) — so `ghi-info` is never told the field exists. Harm: a stated currency signal is uncomputable at build and invisible to the agent meant to use it. Confidence: sure.

**19. `Supersession is the marker literal `Superseded-by: #<n>`, written at change time by the author who knows.` (line 47)** — Where the marker goes is not stated (issue body? pair MD? both?). The sweep "greps the marker" and the drift notice tells `ghi-info` to re-read an entry "including any `Superseded-by:` link" in `issues-closed.md` (line 177) — but `issues-closed.md` is defined as "one line per closed issue: number, title, close reason, closed date" (line 41), a shape with no room for a body-carried marker. Harm: the drift notice instructs a re-read of something the closed file's stated format cannot contain, and the sweep's grep has no defined target. Confidence: sure.

**20. `Recycle fires on the first of three script-observable triggers: closes since session birth, the stale-match rate, transcript size.` (line 55)** — The mechanism is defined but leaves throwaway sessions unaccounted for. Line 62 creates throwaway sessions on contention, and line 64 says "Unexpected closed pointers count toward the stale-match trigger" — with no statement of whether a throwaway's answers count toward the stored session's counters, whether a throwaway is itself subject to recycle, or what becomes of it afterward. The "stale-match 2 in the last 10 answers" constant (line 299) also has no stated behavior for a session with fewer than 10 answers. Harm: on a busy day most answers may come from throwaways, and the stored session's staleness clock either never advances or double-counts, depending on an unstated choice. Confidence: sure.

**21. `run by any agent — and by `ghi-write` step 1` (line 59)** — "step 1" refers to a numbered step of a skill that is still in draft; the skill's steps are not reproduced here, and the path appears only much later (line 98). Harm: a reader cannot check that the reference is accurate, and a step number in an unlanded draft will silently rot if the draft renumbers. Confidence: unsure — the referenced draft exists in the checkout and could be opened; this is a durability concern more than a blocker.

**22. `1. Run the mirror refresh (delta).` (line 61)** — Which machine's mirror is refreshed is not stated. `ghi-info` reads the box copy (line 39), Mac-side callers reach it over SSH (line 51), and the script is described as runnable "on any machine" (line 39). If a Mac-side agent runs `ghi-info-ask.py` locally, step 1 refreshes a copy the agent being asked will never read, and step 3's changed-entry list describes changes absent from the box mirror. Harm: the wrapper's central guarantee — that the agent answers from a just-refreshed mirror — silently fails for the caller class the file explicitly supports. Confidence: sure.

**23. `If another ask holds the session` (line 62) and `a killed run is a named failure` (line 67)** — The mechanism has no stated way to observe that the session is held (no lock, lease, or state file is described beyond "wrapper state — session id, counters", line 51), and no stated disposition of that state when a run is killed at the timeout. A run killed mid-turn plausibly leaves the session marked held, after which every subsequent ask cold-starts a throwaway — an expensive silent degradation with nothing in the file to detect or clear it. Harm: the concurrency and timeout mechanisms are both defined, and the interaction between them is a reachable case neither one covers. Confidence: sure.

**24. `every returned pointer is verified against the mirror` (line 64)** — Since "The write tool's adjudication consult rides this same wrapper" (line 59), this post-check also runs over adjudication replies, whose pointers are the `#n` in `verdict: too-similar #n` / `related #n,#m`. The remedy the step defines does not fit that path: the drift notice ends "give a corrected reading list" (line 64, repeated verbatim at line 177), and a reading list from `ghi-info` on the adjudication path is a reply "in any other shape," which is "thrown away" (line 195) and treated as unavailable (line 33) — so the recheck's only possible effect there is to convert a usable verdict into fail-open. Harm: the drift recheck actively destroys adjudication results when it fires. Confidence: sure.

**25. `An unexpected closed pointer triggers one drift notice` (line 64)** — The post-check enumerates one failure of a returned pointer, closure, and leaves the others unstated: a pointer to an issue number absent from both mirror files (a hallucinated or deleted number), or a reply carrying no pointers at all. Nothing says whether these are dropped, tagged, counted toward the stale-match trigger, or passed to the caller. Harm: verification is presented as total ("every returned pointer is verified"), so a caller reasonably assumes a delivered pointer exists; an unrecognized number has no defined disposition. Confidence: sure.

**26. `proceed under the ordinary rules — the `ghi-write` skill and the artifact-lifecycle rule` (line 69)** — "the artifact-lifecycle rule" appears once, is not defined, and is not given a path; it is named as one of the two rules the whole fallback ladder terminates in. Harm: the ladder's last rung — the behavior every degraded path lands on — cannot be followed by an agent reading this file. Confidence: sure.

**27. `The ladder: ask → grep the local mirror ... → `gh` search → proceed under the ordinary rules` (line 69)** — The executor is unnamed. The preceding sentences describe `ghi-info-ask.py`'s internals, which would put the ladder in the script; but the last rung ("proceed under the ordinary rules — the `ghi-write` skill") is something only the calling agent can do, and the script's stated output is a printed list (line 65). Harm: at build, either the script silently substitutes grep results for a model answer without telling the caller the answer is degraded, or the caller must implement a ladder it is never told it owns. Confidence: unsure — the mixed rungs suggest the ladder is meant as the caller's, but the placement suggests otherwise.

**28. `the next refresh carries it into the corpus, and answers reflect it after the next reload — a lag bounded by the recycle triggers` (line 69)** — The resume prompt already hands the agent changed entries every turn: "these mirror entries changed: #<n>, #<m>. Re-read them in the mirror before answering" (line 208). An added cross-link is a body edit, which moves the issue's `updated` timestamp (line 291) and therefore appears in the delta, so the correction is visible on the very next ask, not at the next reload. Harm: the stated lag bound is longer than the mechanism's actual behavior, which will mislead anyone reasoning about self-correction speed or tuning the recycle thresholds. Confidence: unsure — it depends on the line 291 timestamp assumption, which the file itself lists as untested.

**29. `rewrites body-bearing `gh issue create`/`edit` into `scripts/ghi-issue-write.py`` (line 73)** — "body-bearing" is the hook's trigger condition and is never defined. `--body`, `--body-file`, a heredoc, and an editor-launched body are all body-bearing in the ordinary sense but detectable in different ways or not at all. The accepted-holes paragraph discards only deliberate evasion — "`gh api`, MCP tools, creative quoting" — under a posture where "enforcement targets mistakes, not evasion" (line 88), so a plain `--body-file` from a well-meaning author is a mistake-class miss the residual does not cover. Harm: the covered surface is what layer 2's "correctness backstop" claim (line 99) rests on, and its boundary is undetermined. Confidence: unsure — `--body-file` may be intended as covered, but nothing in the file says how the condition is computed.

**30. `comments are the one taught exception` (line 73)** — The write path defines at least two further behaviors authors must be taught rather than performed as trained: Delete is denied with an instruction to close instead (line 86), and an over-length body triggers an instruction to split into a pair MD (line 80). Both change what the author must do relative to plain `gh` use. Harm: a reader building the `ghi-write` skill from this sentence will front-load one exception and be surprised by the others. Confidence: unsure — "taught exception" may be a narrower term of art about the skill's contents than it appears, but it is not defined.

**31. `In-repo paths cited in the body must resolve on main.` (line 77)** — This check makes the over-length fixer brief unsatisfiable as written. Template B tells the fixer to "merge the substance into its pair document <path>, creating it if it does not exist" and to "have the body's summary cite where it went" (lines 153, 161), while committing document changes "on your branch" (line 159). The body edit then goes through the tool (line 159: "The body edit goes through gh as normal"), whose first step refuses it because the newly created path does not resolve on main. The same trap catches `ghi-info`'s link repairs, told to commit document changes "on your branch" while making issue edits that cite them (lines 197, 238). Harm: the routine, expected path of two of this design's four write-producing roles ends in a refusal, and the brief's own rules leave the fixer no permitted response but `blocked:`. Confidence: sure.

**32. `Over the limit, the reply instructs: ... The author holds the context, so the author does the split` (line 80)** — Length is measured after the write lands, so an over-length body exists in the corpus and is also visible to the sweep, which independently spawns a fixer for exactly this ("Stale-MD and over-length repairs found by the sweep | spawned focused fixers", line 113). Nothing states which owns the split, whether the sweep suppresses findings the author was already told to fix, or how long the author has. Harm: two agents can be told to split the same body — the author who holds the context and a fixer that does not — with no defined precedence; the Division of labor table assigns "the lean-split merge" to the author (line 112) and the same repair to fixers (line 113) without reconciling them. Confidence: unsure — a timing rule may be implied by "found by the sweep", but the overlap is never addressed.

**33. `resubmit through the tool's comment verb naming an event kind from the fixed catalog` (line 84)** — The tool has no stated interface. Every other author interaction with it is indirect — the author types `gh`, the hook rewrites the command (line 73) — so an instruction to invoke a "comment verb" describes a calling convention that appears nowhere in this file: no command form, no argument shape, no statement of whether the comment path also runs the reference check or adjudication. The same instruction is baked into the final, verbatim comment-denial reply (line 258). Harm: the reply teaches the author a path they cannot follow, and the one lost turn per attempt (line 84) becomes unbounded retries. Confidence: sure.

**34. `**Delete** is denied ... the denial carries the override like every other.` (line 86) vs `Refused: issues are never deleted` (line 264)** — The design makes deletion possible through the audited one-use override, so the reply's absolute is false in the file's own terms; the counterexample is exactly the case the override exists for. Harm: an author who reads "never" and then sees an override offered in the next line of the same reply gets a mixed signal about whether the override is legitimate here; the file's whole soft-block posture depends on overrides reading as sanctioned. Confidence: sure.

**35. `Bypassed writes still appear in the delta, where the sweep finds their symptoms.` (line 88)** — Deletion is precisely what the delta cannot see: "anything the delta cannot see — a deletion, a same-second boundary clip" (line 43). Since the delete denial is soft and carries an override like every other, a bypassed delete is reachable and is invisible to the delta until the next whole-mirror rewrite at recycle — and once the issue is gone from GitHub, the rewrite records its absence, not a symptom to find. Harm: this sentence is the stated compensating control for every accepted enumeration hole, and it does not hold for the one write class the design most wants to prevent. Confidence: sure.

**36. `the override audit showing overrides used to dodge the tool` (line 88)** — The audit is the named reopening trigger for the soft block here and the growth trigger for a hard block in the cut table ("The override audit shows dodging, not breakage", line 282), but the file never says where overrides are recorded, who reads the record, on what cadence, or what distinguishes dodging from breakage in it. Harm: the one condition under which reviewers are permitted to re-raise this ruling (line 88) is unobservable, which makes the ruling effectively unreopenable in practice while being written as conditional. Confidence: sure.

**37. `Codex-side writes ride the accepted-holes class with the skill as their up-front layer` (line 90)** — The skill is `ghi-write`, described as a skill in the three-layer stack (line 98); the file gives no basis for assuming Codex loads or fires it, while it explicitly flags the corresponding hook question as unverified ("Codex-side pre-tool hook equivalents ... field names unverified", line 293). If the skill does not fire under Codex, Codex writes have no layer at all, not one. Harm: the sentence presents a fallback that may be empty, and the build verification list checks the hook half but not the skill half. Confidence: unsure — Codex skill support may be known to the author and simply unstated.

**38. `The sweep is script work riding the two feeds` (line 94)** — The sweep has no stated trigger, cadence, or runner anywhere in the file. The refresh has one ("Per ask", line 43), the recycle has three triggers (line 55), and the sweep — which spawns every fixer, does every link scan, escalates blocked fixes, and checks credential expiry (line 296) — has none. Harm: the maintenance duty and every fixer in the design depend on something with no defined invocation; a builder has nothing to implement. Confidence: sure.

**39. `with tight briefs — "issue #31 moved on <date>; its pair MD has not: update the MD."` (line 94)** — This is a second, differently worded version of the brief that § Prompts says lands there verbatim ("Job: Issue #<n> changed on <date>; its pair document <path> is untouched since <date>. Update the document to match the issue's current state.", line 127), in a file whose prompts rule is "a prompt that exists only as description is not buildable or reviewable" (line 117). Harm: a builder copying the inline quote ships a brief with none of Template A's rules, blocked conditions, or reply contract. Confidence: unsure — the inline quote may be intended as illustration, but it is punctuated as a quoted brief.

**40. `A blocked fix escalates as one `draft`-labeled issue naming what blocked it.` (line 94)** — No actor can perform this. The fixer is forbidden to write issues ("You do not write to any issue", line 133) and its output is constrained: "Your final message is exactly one of: done: ... blocked: ..." (lines 142–145, 170–173). The sweep is script work and is nowhere said to file issues; `ghi-info` writes links only ("`ghi-info` repairs links only, never substance", line 94). Harm: the escalation path for every blocked fixer — the design's only route out of a stuck repair — has no owner, so blocked fixers terminate silently. Confidence: sure.

**41. `Fixers write through the normal path and consult `ghi-info` like any GHI author.` (line 94)** — Contradicted by the brief contract: "The sweep fills every slot, including the reading list, which it gets by running `ghi-info-ask.py` itself before spawning — the fixer invokes nothing" (line 123), and neither template contains any instruction to consult `ghi-info`. Harm: a builder reading line 94 gives fixers ask capability and prompts that never use it; a builder reading line 123 does not, and the two disagree about whether a fixer can get help when the pre-fetched reading list turns out to be wrong. Confidence: sure.

**42. `Which model and runtime serve each role best ... is an open question, settled empirically.` (line 94)** — No criterion, owner, experiment, or stopping point is given: nothing says what "best" is measured on, who decides, or when the question is closed. Harm: an agent told to settle this has no way to know it is finished, and no way to know it has started correctly; the open question can be carried forward indefinitely without anyone being wrong. Confidence: sure.

**43. `a missed trigger costs efficiency — a late merge catch, one comment retry` (line 99)** — The hook-and-tool layer checks references, similarity, and length; it does not check routing, which is explicitly the skill's judgment and out of the tool's scope: "Out of scope: routing (queue vs GHI vs pair vs bare MD — `ghi-write`'s judgment...)" (line 35). A missed skill trigger therefore permits a note that belonged in the queue to be filed as an issue — a correctness cost, not an efficiency one, and one nothing downstream detects. Harm: the claim is the stated justification for calling layer 2 "the correctness backstop," and it is bounded by an enumeration that omits the layer's largest gap. Confidence: sure.

**44. `**CLAUDE.md** — ambient documentation only` (line 100) vs `nothing from this design touches CLAUDE.md` (line 11)** — CLAUDE.md is presented as the third layer of the design's stack while the walk rider records that the design puts nothing there. Either the layer is a description of pre-existing content (not stated) or it is an empty layer. Harm: an implementer working the stack top-to-bottom cannot tell whether layer 3 is a deliverable, and a reviewer cannot tell whether its absence is an omission. Confidence: unsure — "ambient documentation only" may be intended to mean exactly "nothing lands here," but then it is a layer with no content.

**45. `| Cross-link repairs | `ghi-info` | its one write class |` (line 111)** — The table and duty 2 ("its only write class", line 32) both count one write class, but the link-repair request has `ghi-info` writing in two places: "Issue edits go through gh as normal; document changes are committed on your branch with a message stating what and why" (lines 197, 238). A git commit in the checkout is a second write surface, governed by none of the write path's checks and not accounted for anywhere in the table. Harm: a reviewer auditing what `ghi-info` can write, or a builder scoping its permissions, will provision issue-write access only and the document half of every repair fails. Confidence: sure.

**46. `Angle-bracket `<slots>` are filled by the invoking script, never by the agent receiving the prompt.` (line 117)** — Contradicted by the prompts in this very section, where several slots exist specifically for the receiving agent to fill in its reply: "done: \<what changed — files\>" and "blocked: \<what stopped you...\>" (lines 144–145, 172–173, 197, 238), "escalate: \<one sentence naming the ruling and the doubt\>" (line 202), and the `#n` in `verdict: too-similar #n` (line 195). Harm: obeyed literally, a fixer or `ghi-info` refuses to fill the reply slot the same section requires it to fill; the rule as stated also gives a reader no way to distinguish the two slot kinds, which are visually identical throughout. Confidence: sure.

**47. `**Status: every prompt is final**` (line 119) with `\<the audited one-use override line, per the instruction-file-guard pattern\>` (lines 248, 254, 260, 266)** — Four of the six write-tool replies end in a slot whose content is not a runtime value but prompt text that was never written; the section's own premise is that "a prompt that exists only as description is not buildable or reviewable" (line 117). The slot is also described as filled by the invoking script (line 117), which is not how a fixed refusal line would work. Harm: the md-review this section is awaiting will review four replies whose last line is a description, and a builder must invent the override wording — the wording that governs the design's only sanctioned bypass. Confidence: sure.

**48. `Every prompt this design depends on, verbatim` (line 117)** — At least one dependent prompt is absent: the question the sweep asks `ghi-info` on the fixer's behalf. Line 123 says the sweep "gets [the reading list] by running `ghi-info-ask.py` itself before spawning," and the resume-ask prompt relays "the asker's question, relayed verbatim" (line 210) — so a question must be composed, from a defect finding, by a script, with no author present. Its wording is nowhere in this section. Harm: the reading list embedded in every fixer brief is produced by an unwritten prompt, so brief quality depends on something neither built nor reviewable. Confidence: sure.

**49. `the fixer invokes nothing` (line 123)** — Contradicted by Template B, which requires the fixer to invoke `gh`: "The body edit goes through gh as normal" (line 159), and by both templates' commit instructions (lines 133, 159). Even Template A's "Read first: issue #<n>" (line 129) requires some invocation to read the issue, since the brief never points the fixer at the mirror. Harm: read as a capability statement, this sentence would have a builder spawn fixers without tool access, which makes Template B unperformable. Confidence: sure.

**50. `Update the document to match the issue's current state.` (line 127)** — This inverts the pair convention the rest of the file states: the body holds "a good summary" and the pair document holds the substance (lines 80, 153, 274). A document made to match a 500-word summary would lose the substance the pair document exists to carry. Harm: a zero-context fixer — which this brief is explicitly written for — has only this sentence to work from and can defensibly truncate a pair document down to the issue's contents. Confidence: sure.

**51. `committed on your branch with a message stating what and why` (line 133, repeated at lines 159, 197, 238)** — "your branch" is never defined: no naming convention, no statement of whether the agent creates it, and — for a one-shot agent that exits after replying (line 125) — no stated actor who reviews, merges, or lands it. This collides with the reference check ("must resolve on main", line 77) and with duty 2's link integrity ("resolves on main", line 32): work left on an unmerged branch does not satisfy either, so the sweep re-finds the same defect on its next run and spawns a fixer again. Harm: fixers can loop indefinitely on already-fixed defects, and their commits accumulate on abandoned branches. Confidence: sure.

**52. `text marked as ruled ("user-ruled", "boss-ruled", "Accepted residual", a dated ruling)` (lines 138, 161)** — "boss-ruled" appears only inside these two templates; every ruling in the body is attributed to the user ("user-ruled 2026-08-11", "APPROVED (user)"). Whether the boss and the user are the same person, and therefore whether this is one authority or two, is not stated. "a dated ruling" is likewise a pattern the fixer must recognize with no example or shape given. Harm: the brief's blocked-conditions are the fixer's only protection for ruled text, and a zero-context fixer cannot reliably apply a marker list it half-understands — over-blocking wastes the fixer, under-blocking rewords rulings. Confidence: sure.

**53. `Read first: issue #\<n\>; its pair document at \<path\>` (line 129)** — How the fixer reads the issue is unstated: the mirror is never mentioned in either brief, and line 123 says the fixer invokes nothing. Harm: a fixer either invents an access method or reports blocked at its first instruction. Confidence: unsure — `gh` read access may be assumed from Template B's "goes through gh as normal", but Template A never mentions `gh` at all.

**54. `merge the substance into its pair document \<path\>, creating it if it does not exist` (line 153)** — When the document does not yet exist, `<path>` is a name someone must invent, and this file states no naming or placement convention for pair documents anywhere. The slot is filled by the sweep (line 149, "Same slot-filling contract as above"), so the choice falls to a script with no stated rule. Harm: pair documents are the backlink target of duty 2's link integrity; inconsistent paths defeat the scan that depends on finding them. Confidence: unsure — a convention may exist elsewhere in the project, but the design's own rule is that this file must stand alone.

**55. `The ask itself follows, worded per the resume-ask prompt.` (line 181)** — Cold start is reachable from all three request paths, not only reading-list asks: the adjudication consult rides the same wrapper (lines 59, 216) and the link-repair request does too (line 232). On a cold start for either of those, what follows the cold-start prompt is the adjudication or link-repair request, not the resume ask. The resume-ask prompt's own scope line agrees it is for reading lists only: "Sent on every reading-list request" (line 206). Harm: a builder implementing step 2 from line 181 sends a reading-list request after cold-starting a session that was cold-started to adjudicate a write. Confidence: sure.

**56. `Never call GitHub — no gh, no API, no web.` (line 190) vs `Issue edits go through gh as normal` (line 197)** — Two instructions seven lines apart in the same prompt, one absolute and one requiring exactly what it forbids. Request form 4 is `ghi-info`'s only write class, so the contradiction is not on a rare path. Harm: whichever instruction the agent weights, it either refuses every link repair or violates the prompt's stated invariant that the mirror is its only view of GitHub — and the file gives no precedence rule between them. Confidence: sure.

**57. `Closed issues belong in a reply only when the request says closed history is wanted` (line 194)** — Request form 3 conflicts with this: it is triggered by an issue closing and asks for "a corrected reading list" (line 196) after re-reading the entry "including any `Superseded-by:` link" — a superseding issue may itself be closed, and the wrapper expects to deliver tagged closed pointers ("Whatever remains is delivered with truthful tags ("#31 (closed 2026-08-08)")", line 64) on requests that did not ask for closed history. Harm: after a drift recheck the agent must either withhold the closed pointer the recheck was about or violate the rule; the wrapper's own tagging behavior implies the second, and the prompt states the first. Confidence: unsure — the tagging sentence may describe only pointers the agent already returned, which is not stated.

**58. `A reply in any other shape is thrown away.` (line 195)** — The Boundaries section of the same prompt mandates two reply shapes that are not one `verdict:` line: "reply exactly: out-of-scope" (line 201) and "Reply: escalate: \<one sentence...\>" (line 202). On the adjudication path both are therefore discarded, and discarding is not neutral — it means fail-open, so the write proceeds (line 216). Harm: an out-of-scope or escalation signal raised during adjudication is indistinguishable from `ghi-info` being unreachable, and its only effect is to skip the check. Confidence: sure.

**59. `**You are asked to repair a link** ... document-side changes are committed on your branch` (line 197)** — The prompt gives `ghi-info` no repository access and states the opposite two paragraphs earlier: "Your knowledge is the local mirror at \<mirror-path\>" and "the mirror is your only view of it" (lines 185, 190). Repairing a backlink in `docs/issues/31-foo.md` requires reading and writing a file outside the mirror. Harm: the write class the design assigns to `ghi-info` is unperformable under the prompt that defines the agent, and neither the prompt nor § The ask path says where its checkout is or that it has one. Confidence: sure.

**60. `Sent by the write tool for every body-bearing create or edit, before the write.` (line 216)** — Not every one: the reference check runs first and refuses on a failing citation (lines 77, 246), so those writes never reach adjudication. The counterexample is ordinary — the pair sequence's normal intermediate state produces exactly such bodies. Harm: anyone reasoning about adjudication coverage (for example, whether every filed issue was checked for duplicates) will over-count. Confidence: sure.

**61. `Sent by the write tool for every body-bearing create or edit` (line 216)** — Applied to `ghi-info`'s own link repairs, this recurses: a link repair that edits an issue body (line 197) passes through the hook and the tool, whose step 2 consults `ghi-info` — while `ghi-info` is mid-turn performing the repair, so the wrapper finds the session held and cold-starts a throwaway (line 62) to adjudicate `ghi-info`'s own write. The same applies to fixers' body edits. Nothing in the file addresses this. Harm: every link repair costs an extra full corpus load and can be refused as too-similar against the very issue it is repairing, since only edits declare their own issue number for exclusion (line 222) and it is not stated that a link repair declares one. Confidence: sure.

**62. `verdict: related #n,#m` (lines 195, 228) and `Related issues worth knowing: #\<n\>, #\<m\>.` (line 270)** — Both the verdict grammar and the appended reply are written with exactly two slots, while the reading-list form freely shows three ("read #13, #24, #31", line 194). Whether one related issue, or four, is expressible is not stated, and the adjudication reply is validated strictly ("Reply with exactly one line", "A reply in any other shape is thrown away"). Harm: if the grammar is literal, a three-way relation must be truncated or the reply is discarded into fail-open; if it is illustrative, the strictness elsewhere gives the agent no way to know. Confidence: unsure — `#n,#m` reads as a list idiom, but nothing says so.

**63. `a comment cannot be mechanically rewritten into the body edit that requires` (line 258)** — The sentence is missing its subject: "the body edit that requires" has no noun for "that" to attach to and no object for "requires." The intact counterpart in the body reads "the body edit the revision convention requires" (line 84), which shows a phrase was lost. Harm: this is a final, verbatim, author-facing refusal in a section whose whole premise is that prompts must be shippable as written; the author reading it is told a convention requires something, without being told which convention or what it requires — and the reply's next clause ("The revision convention keeps the body current") is the only hint. Confidence: sure.

**64. `| Vector or graph database | The context window is the database | Retrieval quality measurably degrades, or the open corpus outgrows the window |` (line 280)** — The growth trigger is "measurably" degrading retrieval quality, but the design defines no retrieval-quality measure: nothing is logged about answer quality except the stale-match count (line 55), which counts closure drift, not relevance. Harm: the cut's stated reopening condition cannot be observed, so the decision is effectively permanent while written as provisional — the same defect as the override audit at line 88. Confidence: unsure — a measure may be intended to come later, but the cut table presents these triggers as usable.

**65. `Each with its failure branch:` (line 289) vs item 7 (line 297)** — Item 7 is "Comment-fetch cost at real volume (measured once: 0.42 s for one issue with comments)." — a measurement with no "else" clause and no stated consequence if the cost proves high at volume. Every other item except 6 has an explicit "else". Harm: the list's contract is that each unverified assumption comes with the fallback that makes failure survivable; item 7's failure has none, which matters because the delta fetches comments per changed issue (line 43) inside a 5-minute ask timeout (line 299). Confidence: sure.

**66. `the sweep checks validity and flags expiry before it bites` (line 296)** — The mechanism is defined but its output has no recipient: nothing states where the flag goes, who acts on it, or what happens between the flag and the expiry. The dependency is also circular for one of the two credentials — if the box's Claude token expires, `ghi-info` cannot answer, and the sweep that would have flagged it has no stated schedule at all (see finding 38). Harm: the design's answer to a failure it says has already happened ("the box's auth has expired before") is a signal with no destination. Confidence: sure.

**67. `transcript threshold set at build from NM's working values` (line 299)** — "NM" is never expanded; it appears only inside a parenthetical at line 67 alongside "nedsmessenger," from which a reader must infer the abbreviation. More importantly, the working values are not given and no location for them is stated — the one nedsmessenger path in the file (`~/Projects/nedsmessenger/adapter/adapter.py`, line 67) is cited for a different thing, the `ask_claude` call pattern. Harm: one of the three recycle triggers has no value and no stated way to obtain one, so the trigger cannot be implemented from this file. Confidence: sure.

**68. `starting values, tuned in live use` (line 299)** — No owner, cadence, criterion, or record is stated for the tuning; `BODY_WORD_LIMIT`'s history shows the mechanism is real ("raised from the 400 in the approved constants batch — neither value derived, both starting guesses") but not how a later change is decided or where it is recorded. Harm: an agent asked to tune a constant has no stopping point and no way to know whether a value is a guess still under observation or a ruling it must not touch — a distinction the fixer briefs make load-bearing (lines 138, 161). Confidence: unsure — tuning may be understood as always the user's call, which the file does not say.

**69. `## The GHI write path (ghi-issue-write)` (line 71), `scripts/ghi-issue-write.py`, `ghi-issue-write-redirect.py` vs `**`ghi-write`** (skill...)` (line 98)** — Two distinct components are named `ghi-issue-write` and `ghi-write`, one the tool and one the skill, differing by one interior word; a grep for `ghi-write` does not match `ghi-issue-write`, and vice versa, so neither search finds both, while a reader scanning prose confuses them easily. The tool is additionally referred to by three unstable descriptions — "the project write tool" (line 23), "The write tool" (line 33), "the tool" throughout — none of which is its name. This bears directly on the checkout's CLAUDE.md instruction: "When creating or inventing names ... use explicit, clear and precise multi-part names. Check newly invented names with glob ... or grep ... If these checks return collisions or ambiguity, choose a more explicit name". Harm: a future agent asked to change "the write tool" cannot tell from a search which of the two components is meant, and the confusion is worst in exactly the passages where both appear (lines 98–99). Confidence: sure.

clean sections: none — every section, including the YAML frontmatter, the unheaded introduction, and each § Prompts subsection except the drift notice, carries at least one finding above.

