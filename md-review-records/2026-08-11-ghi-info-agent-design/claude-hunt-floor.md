<!-- provenance: runtime=claude model=claude-sonnet-5 effort=high cell=defect-hunt tier=floor target=/Users/el/Projects/nedschorus/.claude/worktrees/linters-tool-usage-f8b0a0/docs/drafts/ghi-info-agent-design.md -->

I read the full file, including frontmatter. Findings below, in document order.

**1. Frontmatter status vs. walk-order body (lines 2, 8–21)**

> status: integrated design; decisions ruled 2026-08-07 in the plan walk, md-review corrections ruled 2026-08-09; awaiting the user's integration walk

This says the integration walk has not yet started. But the body immediately below it (`## Walk order — integration walk (opened 2026-08-09...)`) shows the walk already opened on 2026-08-09 and items 1–5 already processed/approved, with item 6 "open" as of 2026-08-11. A reader relying on the frontmatter alone would believe the walk is pending; the body shows it is nearly finished. Sure this is a contradiction.

**2. `design-as-of: 2026-08-09` is stale (line 3)**

The frontmatter dates the design as of 2026-08-09, but the document contains material dated later: item 5 "processed 2026-08-11," item 6 "open 2026-08-11," most of § Prompts marked "approved 2026-08-11," and the `BODY_WORD_LIMIT` ruling "user-ruled 2026-08-11" (line 299). A field meant to timestamp the design's content doesn't cover roughly a third of that content. Sure this is a defect — the field's purpose (letting a reader know how current the design is) is defeated.

**3. "Prompt drafting in progress" vs. "every prompt is final" (lines 21 vs. 119)**

Line 21: "Prompt drafting in progress; § Prompts' owed/final labels are the per-prompt marks."
Line 119: "**Status: every prompt is final** — the two fixer briefs, the drift notice, the cold-start prompt ... the link-repair request ... and the write tool replies."

Item 6 is marked "open" (i.e., current, not a closed historical record), so its claim that prompt drafting is in progress should reflect present status — but § Prompts, which item 6 itself points to as authoritative, says all prompts (including the four that were "owed") are now final. One of these two statements is stale. Sure this is a contradiction.

**4. "measured today" — undated relative reference (line 25)**

> the corpus fits in its context window (measured today: 45 issues ≈ 109 KB)

Every other empirical measurement in the file carries an absolute date (e.g., line 43: "Measured 2026-08-07 against the live repo"; line 297: "measured once"). This one says "today" with no date attached, so a future reader has no way to know when the 45-issue/109 KB figure was taken or how stale it might be — the CLAUDE.md durability principle ("usable without the conversation that produced it") is exactly what this fails. Sure this is a defect.

**5. "Cross-links between issues" duty has no described mechanism (lines 32, 94, 294)**

Line 32 states maintenance covers two things: "Cross-links between issues, and link integrity across the issue–MD boundary in both directions: every GHI→MD reference resolves on main; every pair MD backlinks its correct GHI(s)." Only the second half is then given criteria and, later, a mechanism: line 94's "link-integrity scan in both directions (the MD side read from the repo checkout, not the mirror)" — whose parenthetical makes clear "both directions" means GHI↔MD, not issue↔issue. Nowhere does the document say how "cross-links between issues" is established, written, or checked. The term first resurfaces in Verify at build item 4: "The cross-reference timeline event as the backlink source for issue↔issue links (API shape) — else backlinks derive from body parsing alone" (line 294) — introducing a specific mechanism (a GitHub cross-reference timeline event) that was never described in the body it's meant to verify. I'm unsure whether this is an oversight or whether issue↔issue cross-links are meant to be entirely out of `ghi-info`'s and the sweep's remit (in which case line 32 overstates the duty), but either reading leaves a stated duty without an executable procedure.

**6. "The authoritative copy" vs. "GitHub is the source of truth" (line 39)**

> `<checkout>/ghi-mirror/`, gitignored; regenerated by `scripts/ghi-mirror-refresh.py` on any machine. The authoritative copy lives in `ghi-info`'s checkout on the box. GitHub is the source of truth; the mirror is derived data.

If the mirror is derived data regenerable identically "on any machine," no single copy should need to be singled out as "authoritative" — and if GitHub is the source of truth, no mirror copy is authoritative at all. Calling the box's copy "authoritative" implies some other machine's freshly-regenerated mirror is less trustworthy, which conflicts with both surrounding claims. Sure this is a contradiction, and it matters because it's unclear what a caller should do if the box's mirror and a Mac-regenerated mirror disagree.

**7. "enter the corpus" broadens "corpus" beyond its defined scope (line 45)**

> The second feed: the refresh fetches origin and reads `git log origin/main` — pair-MD edits touch no issue timestamp, and they enter the corpus when they land on main.

Everywhere else "the corpus" means the issue corpus specifically — "the corpus fits in its context window (measured today: 45 issues ≈ 109 KB)" (line 25), "this project's knowledge agent over its GitHub-issue corpus" (line 183), the two mirror files holding only issues. This sentence says MD edits "enter the corpus," but no mirror file or store for MD content is ever described — the sweep's link-integrity scan reads MD "from the repo checkout, not the mirror" (line 94), so MD content is explicitly *not* mirrored. It's unclear what "enter the corpus" concretely causes to happen (is a timestamp updated? a flag set? nothing but the git-log read itself?). Sure the wording is inconsistent with the rest of the document's usage of "corpus"; unsure what behavior it's actually meant to describe.

**8. "two request forms" undercounts (line 59 vs. 232)**

Line 59: "The write tool's adjudication consult rides this same wrapper (user-ruled 2026-08-11): one stored session, one refresh-and-resume machinery, two request forms."

Line 232 (link-repair request): "Sent by the sweep for each link-integrity finding ... Rides the same wrapper as the other requests, hence the same changed-entries preamble."

If both the adjudication consult and the link-repair request ride the same `ghi-info-ask` wrapper as the reading-list ask, that's three request forms feeding into the wrapper, not two — and the cold-start prompt itself lists four request forms from `ghi-info`'s own perspective (line 192). I'm unsure whether "two" was meant to exclude link-repair for some reason not stated, but as written it's inconsistent with line 232's explicit "rides the same wrapper."

**9. "a killed run is a named failure" (line 67)**

> One overall timeout (inside the hook budget); a killed run is a named failure.

No name is given, and no failure-naming scheme appears elsewhere in the document (contrast with the explicit "Accepted residual" labels used throughout). A reader can't tell what "named failure" refers to concretely — an error code, a specific string, or just "not a silent failure." Unsure whether this is a real gap or shorthand for something obvious to the author; flagging because I can't resolve it from the file.

**10. "the artifact-lifecycle rule" — undefined and uncited (line 69)**

> A failed ask never blocks a write. The ladder: ask → grep the local mirror (stale if not regenerated) → `gh` search → proceed under the ordinary rules — the `ghi-write` skill and the artifact-lifecycle rule.

This names a rule that is never defined or cited anywhere in the document. Contrast with "the revision convention" two paragraphs later (line 84), which does carry a citation: "([nedschorus-founding-plan.md] § Project organization)". Without a citation or inline definition, an agent following the write-path fallback ladder has no way to know what "the artifact-lifecycle rule" requires. Sure this is a gap.

**11. Self-adjudication is a reachable, unaddressed case (lines 73, 78, 94)**

Line 73: "`ghi-info`'s and fixers' writes route through the tool like any author's." The tool's step 2 is "Similarity adjudication (§ What ghi-info is; the edit's own issue excluded)... `ghi-info` unreachable means the write proceeds without adjudication" (line 78) — i.e., every body-bearing write routed through the tool, including `ghi-info`'s own link-repair writes (its one write class, line 32/94), triggers a consult to `ghi-info` for adjudication. The document never addresses what happens when `ghi-info` is asked to adjudicate its own in-flight write — self-consultation, a possible session conflict (the ask path already handles "another ask holds the session" by spawning a throwaway session, line 62, but that's about concurrent asks, not an agent adjudicating its own action), or whether link-repair writes are meant to skip step 2 entirely. Sure this case is reachable given how the mechanism is defined; unsure whether it's meant to be excluded implicitly (e.g., link edits might not be "body-bearing" and so never hit the hook at all) since the document doesn't say either way.

**12. "the tool's comment verb" — undefined interface (lines 84, 258)**

Line 84: "or, if this is a genuine event ... resubmit through the tool's comment verb naming an event kind from the fixed catalog." Line 258 repeats: "resubmit through the tool's comment verb naming that event kind." Nowhere is this verb's actual syntax, command name, or invocation given — the tool's "sequence per write" (lines 75–81) describes generic create/edit handling (reference check, adjudication, write, length check) with no mention of a distinct comment-submission mode. An author told to "resubmit through the tool's comment verb" has no executable instruction for how to do so. Sure this is a gap.

**13. Sweep's credential-check duty appears only in Verify at build, not in its own definition (line 94 vs. 296)**

§ Maintenance and fixers defines the sweep's scope as exactly three things: "the length check over changed bodies, the `Superseded-by:` marker scan, and the link-integrity scan in both directions" (line 94). But Verify at build item 6 says: "Both box credentials survive unattended operation (the box's auth has expired before) — the sweep checks validity and flags expiry before it bites" (line 296) — assigning the sweep a fourth duty (credential-validity checking) that its defining section never mentions. Sure this is inconsistent with the sweep's stated scope; unsure whether it's an intentional addition that just wasn't folded back into § Maintenance and fixers.

**14. "your branch" — undefined term, used four times (lines 133, 159, 197, 238)**

Fixer brief 1: "You change only the pair document, committed on your branch with a message stating what and why." Fixer brief 2 (line 159): same phrasing. Cold-start prompt request form 4 (line 197) and the link-repair request (line 238) both repeat "committed on your branch." Nowhere in the document is "your branch" defined — not how it's created, named, or landed (is there a PR step? auto-merge? who reviews it?). An agent instructed to commit "on your branch" has no way to execute this from the file alone. Sure this is a gap, and it recurs across four separate prompts.

**15. "Never call GitHub" contradicts "gh as normal" for link repairs (lines 190 vs. 197, 238)**

Cold-start prompt: "GitHub is the source of truth and the mirror is your only view of it. Never call GitHub — no `gh`, no API, no web." (line 190) — an absolute instruction to the `ghi-info` agent.

The same prompt's own request form 4, two paragraphs later: "You are asked to repair a link ... Issue edits go through `gh` as normal; document-side changes are committed on your branch..." (line 197). The link-repair request in § Prompts repeats this: "Repair exactly this link and nothing else. Issue edits go through `gh` as normal..." (line 238).

Link repair is stated elsewhere to be `ghi-info`'s "own writes — its only write class" (line 32), i.e., something `ghi-info` itself does. "gh as normal" mirrors the phrase used for ordinary GHI authors ("GHI authors write with `gh` as trained," line 73), suggesting `ghi-info` itself is expected to issue `gh` commands to repair an issue-side link — directly violating the "Never call GitHub — no gh" boundary stated in the same prompt. Sure this is a contradiction as written; it's the kind of thing that would either make the agent refuse a legitimate repair task (correctly following "never call gh") or silently violate its own stated boundary (if it follows "gh as normal" instead) — and the document gives no way to tell which is meant to win.

**16. Verify at build item 7 has no failure branch, contradicting the section's own framing (lines 289, 297)**

Line 289: "Each with its failure branch:" — a promise that applies to every item in the following numbered list. Items 1–6 all comply (each ends in "— else ..."). Item 7 does not: "7. Comment-fetch cost at real volume (measured once: 0.42 s for one issue with comments)." has no "else" clause. Sure this violates the section's own stated structure.

---

clean sections: ## The ghi-info session, ## The three-layer stack, ## Division of labor, ## Deliberately not in version 1

