<!-- provenance: runtime=claude model=claude-sonnet-5 effort=high cell=defect-hunt tier=floor target=/home/nedlern/Projects/nedschorus/.claude/worktrees/gatekeeper-walk-fork-continuation/docs/agents/gatekeeper-instructions.md -->

Good, both confirmed. I've completed the review. Here are my findings.

**1. Line 3 — "Every task here shares one specification, one program, and one threat model, so each makes the next cheaper."**
"one threat model" is an unexplained reference: neither this file nor either of its two explicitly-linked documents (`agent-seat-model.md`, and the "Read first" pair `git-gatekeeper-design.md` / `3-git-gatekeeper-build-slice-plan.md`) names, sections, or points to a "threat model." I grepped both design documents for "threat model" and found no occurrence. The claim is used as grounds for the seat's grouping rule (per `agent-seat-model.md`'s test — "does the first task make the agent smarter about the second?" — new work is supposed to be checked against what this seat already knows), but a future agent has no artifact to check a candidate task against to see whether it "shares" this threat model. Confidence: sure the phrase is unpointed-to and undefined in-context; unsure whether the author considers this a problem (it may be intended as loose scene-setting rather than a checkable claim).

**2. Line 31 — "Do not start the evidence-format design without his go-ahead: it is instruction-class work and its shape is his ruling to make."**
This labels the *design of the walked-approval evidence format* itself as "instruction-class work." Elsewhere in this same repo's agent-brief family, "instruction-class" is used consistently and narrowly for files literally read as instructions by the harness/agents — CLAUDE.md, skills with their prompt templates, injected system prompts, and the wiki (per `docs/cross-project/git-gatekeeper-design.md:189`: "instruction-bearing text (CLAUDE.md files, skills with their prompt templates, injected system prompts, the wiki)"; echoed in `skill-builder-instructions.md:27` — "A skill is instruction-class" — and `doctrine-instructions.md:20` for CLAUDE.md). Step 1 of this same file's own road (line 15) treats the evidence-format design as a distinct, general design task pointed at by issue #31's "class definition and guards," not itself an edit to a CLAUDE.md/skill/system-prompt/wiki file. Applying the term "instruction-class" to the design task conflates two different things this file (and its family) otherwise keeps separate: an artifact that *is* gated instruction-bearing text, versus the work of designing the gate that will check such text. A future agent reading only this file could reasonably ask "instruction-class" work under whose enforcement, exactly — the hook that gates CLAUDE.md edits doesn't cover a design document under `docs/`. Confidence: unsure — the intent may be "this decision carries the same weight/needs the same user sign-off as instruction-class work," which is a defensible non-literal reading, but the sentence as written asserts class membership, not an analogy.

**3. Lines 13–31 — "## The road, in order" … "ask which step he wants first."**
The road is headed "in order," and step 1 states "everything downstream waits on it" (i.e., steps 2 and 3 cannot start before step 1 completes) — a strict dependency chain. The First Action section then instructs: "report to the user where the road stands and ask which step he wants first." Read literally, "which step he wants first" presents the three road items as if any could be chosen to start with, which conflicts with the file's own statement that step 1 blocks steps 2 and 3 (so only step 1 is actually startable — asking "which" is close to moot). Confidence: unsure — a plausible charitable reading is that "which step" really means "confirm whether/when to proceed at all, possibly including non-road work like the open PR #55," in which case there's no real conflict; but taken at face value the phrasing implies a choice the file's own sequencing rules out.

clean sections: "## Where things stand" (paragraph 1, lines 7–9 only), "## What is settled, and must not be relitigated," "## Boundaries"

