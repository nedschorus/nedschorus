<!-- provenance: runtime=claude model=claude-opus-5 effort=high cell=restate tier=good target=/home/nedlern/Projects/nedschorus/.claude/worktrees/gatekeeper-walk-fork-continuation/docs/agents/gatekeeper-instructions.md -->

The file has no YAML frontmatter — it begins directly with the H1 heading. Restatements follow.

## Title line — `gatekeeper` — seat instructions

1. **"# `gatekeeper` — seat instructions"** — This document is the instruction file belonging to the agent seat whose name is the literal string `gatekeeper`; "seat instructions" I take to mean the standing brief that an agent occupying that named role reads to know what its job is (rather than instructions *about* a gatekeeper, or instructions *to* the gatekeeper program).

## Opening paragraph (unheaded, under the title)

1. **"Your pile: **taking the git-gatekeeper from built-but-dormant to live.**"** — The body of work assigned to you (the agent reading this, sitting in the `gatekeeper` seat) is: moving the git-gatekeeper from its present condition — the program's code exists and is finished, but it is switched off / not in the actual path of anything — to the condition of being in real operational use. "Pile" I read as a coined term for the seat's queue or portfolio of work, i.e. the set of tasks that belong to this seat and no other. "Live" means actually enforcing, actually in the path by which changes reach main.
2. **"Every task here shares one specification, one program, and one threat model, so each makes the next cheaper."** — All of the tasks in this pile refer to the same single design document, act on the same single piece of software (`scripts/git-gatekeeper.py`), and are justified by the same single account of what attacks or failure modes are being defended against; because of that shared context, doing any one of these tasks builds up understanding that reduces the effort needed for the remaining ones. The implied consequence I take is: these tasks are deliberately grouped into one seat because context carries over between them.
3. **"Read [the seat model](agent-seat-model.md) for how seats work; this file is your brief."** — If you want to know the general mechanics of what a "seat" is and how seats operate, go read the separate document `agent-seat-model.md` in this same directory; that document covers the general system, whereas the document you are now reading is the specific assignment for this particular seat. "Brief" means the statement of your particular task and background, in the sense of a briefing.

## Where things stand

1. **"`scripts/git-gatekeeper.py` is **built through all five slices** and merged to main (PR #49, 2026-08-12), with a 169-case suite in `scripts/git-gatekeeper-test.py`."** — The program file `scripts/git-gatekeeper.py` has had all five of the planned incremental build stages ("slices" — the numbered increments of the build plan) completed, and that code is already on the main branch, having arrived there via pull request number 49 on 12 August 2026; alongside it there is a test file `scripts/git-gatekeeper-test.py` containing 169 individual test cases. Ambiguity I note: "built through all five slices" could mean the plan had exactly five slices and all are done, or that the plan has more slices and the first five are done — the later mention of "slice 6" makes the second reading the one I take, i.e. slices 1 through 5 are complete and slice 6 is not.
2. **"The gate is **dormant**: no host holds a main-capable credential, so nothing routes through it yet."** — The gatekeeper mechanism is currently inactive in the sense that it is installed but not exercised; the reason given is that no machine anywhere currently possesses the authentication credential that would allow pushing to the main branch, and consequently no change currently travels through the gatekeeper program on its way to main. "Host" I read as a computer/machine (or the account on it) rather than a person. "Routes through it" means passes through the gatekeeper as its path to main.
3. **"Agents currently reach main the interim way — commit to a working branch, push, and the user's Mac-side seat reviews and merges."** — For the time being, the way any agent's work gets onto the main branch is the temporary process, which consists of three steps: the agent makes commits on a non-main working branch, pushes that branch to the remote, and then the separate agent seat that runs on the user's Mac computer performs the review of that branch and carries out the merge into main. "Interim" means this holds only until the gatekeeper is live.
4. **"**Read first:** `docs/cross-project/git-gatekeeper-design.md` (the canonical specification, design-as-of 2026-08-12) and `docs/issues/3-git-gatekeeper-build-slice-plan.md` (the build order, its ruled design points, and the program follow-ups)."** — Before doing anything else, read these two files: first, `docs/cross-project/git-gatekeeper-design.md`, which is the authoritative, single source-of-truth design document, whose contents describe the design as it stood on 12 August 2026 (i.e. it is a snapshot with that date, not necessarily updated since); and second, `docs/issues/3-git-gatekeeper-build-slice-plan.md`, which contains the sequence in which the slices are to be built, the design questions that have already been decided ("ruled") within that plan, and the outstanding items of follow-up work on the program itself. I read "**Read first**" as an ordering instruction relative to the rest of the work, and the two documents' parenthetical labels as descriptions of what each contains.
5. **"Issue: [nedschorus#3](https://github.com/nedschorus/nedschorus/issues/3), whose body is current as of 2026-08-12."** — The GitHub issue that tracks this work is issue number 3 in the `nedschorus/nedschorus` repository; the text of that issue's main description was accurate/up to date as of 12 August 2026 — which I read as a statement that you may trust the issue body up to that date, with no claim made about anything after it.
6. **"The full review history — two md-review grids, a subtraction review, and every ruling with its reasoning — is in `md-review-records/2026-08-09-git-gatekeeper-design/`."** — The complete record of the reviews that were performed on this design lives in the directory `md-review-records/2026-08-09-git-gatekeeper-design/`, and it comprises: two artifacts produced by the project's "md-review" review procedure that are laid out as grids (a tabular arrangement of review findings — I take "grid" to be that procedure's own term for its output table); one review of the "subtraction" kind, which I take to mean a review that asked what should be removed or cut rather than what should be added; and, for every decision made, both the decision itself and the argument behind it.
7. **"Consult `dispositions.md` and `codex-dispositions.md` there **before reopening any settled question**; roughly forty decisions are recorded with dates and grounds, and re-deriving them wastes the user's time."** — Within that directory there are two specific files, `dispositions.md` and `codex-dispositions.md`, holding the recorded outcomes ("dispositions" = how each raised point was disposed of / resolved); if you are about to raise again a question that has already been decided, read those two files first. The justification given: those files contain approximately forty recorded decisions, each stamped with the date it was made and the reasoning that grounded it, so working out from scratch an answer that is already written down consumes the user's time to no benefit. I read the instruction as: not an absolute ban on reopening, but a requirement to check the record before doing so. ("codex-dispositions" I take to be the dispositions arising from a review by a different tool/agent named Codex, though the file name alone does not settle that.)

## The road, in order

1. **"1. **Design the walked-approval evidence format.**"** — The first item on the sequence of work is a design task: to specify a format for evidence of "walked approval." "Walked approval" I take to mean approval the user gave after being walked through the change item by item (as in this project's walk-me-through procedure), and "evidence format" means the defined structure of a recorded artifact that testifies such approval happened.
2. **"What does the user's approval of an instruction-class change look like as a checkable artifact?"** — This sentence states the design question to be answered: when the user has approved a change that falls into the "instruction class" (the category of changes to instruction-type material, whose boundaries are defined elsewhere), what concrete recorded object should that approval take the form of, such that a program can mechanically inspect it and verify the approval occurred? "Checkable" I read as machine-verifiable by the gatekeeper, not merely readable by a person.
3. **"Undesigned today, and everything downstream waits on it."** — As of now no such design exists, and all the subsequent steps in this road are blocked until it does. "Downstream" means the later-in-sequence work items (slice 6 and the credential work).
4. **"Class definition and guards: [nedschorus#31](https://github.com/nedschorus/nedschorus/issues/31)."** — The definition of which changes belong to the "instruction class," and the protective mechanisms ("guards") associated with that class, are documented in GitHub issue number 31 of the `nedschorus/nedschorus` repository. I read this as a pointer for where to find that material, not as an instruction to modify that issue. "Guards" is ambiguous between "the checks that enforce the class" and "the safeguards that protect against mis-classification"; I take the former reading — the enforcement mechanisms.
5. **"2. **Build slice 6** — the review-evidence check, which enforces that format at the gate."** — The second item is an implementation task: build the sixth increment of the program, which consists of a check on review evidence — that is, code inside the gatekeeper that inspects the artifact defined in step 1 and enforces its requirements at the moment a change tries to pass through the gatekeeper on its way to main.
6. **"Ruled 2026-08-10 as a prerequisite of activation, because the deployed gatekeeper copy self-updates from main and that is only safe once its own source cannot reach main without walked approval."** — A decision was made on 10 August 2026 that slice 6 must be finished before the gatekeeper can be turned on. The reasoning: the copy of the gatekeeper that is actually installed and running keeps itself up to date by pulling its own code from the main branch; therefore anyone who can put code on main can change the gatekeeper's own behavior; therefore this self-updating arrangement is only acceptable once the gatekeeper's own source code cannot itself be landed on main unless it carries walked approval — which is exactly what the slice 6 check enforces.
7. **"3. **The credential work** — the dedicated GitHub account (C1/C3: a collaborator with write on this one repository, never an org owner), the dedicated Unix user holding the credential, the sudoers rule pointing at a **root-owned copy outside every checkout** that keeps itself current from main, and moving branch protection's push restriction onto the new account."** — The third item is the body of work concerning credentials, made of four parts: (a) creating a purpose-specific GitHub account — and the constraint labelled C1/C3 in the design document says that account must be a repository collaborator holding write permission on this single repository only, and must under no circumstances be an owner of the GitHub organization; (b) creating a purpose-specific Unix user account on the machine, which is the only identity that holds that credential; (c) writing a `sudoers` rule (the configuration granting one Unix user permission to run a specific command as another user) whose target is a copy of the gatekeeper program that is owned by root and located outside of every git checkout/working copy, that copy being one that keeps its own contents synchronized with main; and (d) reconfiguring the main branch's branch-protection settings so that the rule restricting who may push points at the newly created GitHub account. I read "outside every checkout" as: deliberately not inside any repository working directory, so that an agent working in a checkout cannot edit the program it is about to be checked by.
8. **"Requires an org owner to apply; the user does this part."** — Carrying out that credential work needs permissions held only by an owner of the GitHub organization; the user (not you, the agent) is the one who will perform it. I read "this part" as referring to the credential work of item 3, and the sentence as removing that item from your own hands while leaving it on the road as something to be tracked.
9. **"Also yours: **PR #55** (`gatekeeper audit: account names compared case-insensitively; PR #49 review rulings folded into the slice plan`), open and awaiting review."** — In addition to the three road items, pull request number 55 also belongs to this seat's responsibility. Its title, quoted verbatim, states that it does two things: it changes the gatekeeper's `audit` function so that GitHub account names are compared without regard to letter case, and it incorporates the decisions that came out of reviewing PR #49 into the build slice plan document. The PR's current state is that it has not been merged or closed, and it is waiting for someone to review it. I take "awaiting review" to mean waiting on the Mac-side reviewing seat under the interim process, though the sentence does not say who.

## What is settled, and must not be relitigated

1. **"The user ruled these; each is recorded with reasoning in the disposition files."** — The decisions listed in the following sentence were made by the user personally (with the authority that implies), and for every one of them both the decision and the argument supporting it are written down in the dispositions files mentioned earlier. The section heading tells me the operative instruction: these are closed matters and you are not to argue them again ("relitigate" = reopen and re-argue a decided question).
2. **"Notable: slice 4's async machinery **stays** (slow checks are expected); the deployed copy **self-updates** rather than being hand-deployed (simple-to-operate over simple-to-build); `--issue` **stays** (a mechanical forcing function is never traded for trained habit); the trailer-absence audit is **deleted** (a detector with no consumer is cost without value); the base is **computed, not declared**; C7 is **struck to zero**."** — Here are the noteworthy items among those settled decisions, each followed by the compressed reason in parentheses. (a) The asynchronous/concurrent execution code introduced in build slice 4 is to be kept, not removed or simplified away, because checks that take a long time to run are an anticipated normal condition rather than a defect to be engineered out. (b) The installed, running copy of the gatekeeper updates itself automatically instead of being deployed manually by a person, and the principle behind that choice is a preference for a system that is easy to operate day to day over one that is easy to write in the first place. (c) The `--issue` command-line option remains part of the program; the reasoning is a general principle that a mechanism which mechanically compels the right behavior should not be given up in exchange for relying on people or agents having learned the habit of doing it — the "never" here is stated as a flat principle. (d) The audit feature that detected the absence of a commit trailer has been removed from the program, because a detector whose output nothing acts upon incurs cost while producing no benefit. (e) The "base" — which I take to be the base commit or base branch a change is measured against — is determined by the program computing it, rather than being supplied/asserted by the caller. (f) The design item labelled C7 has been reduced to nothing, i.e. that requirement was eliminated entirely rather than merely trimmed; what C7 originally said is not stated here and must be looked up in the design document. I read the word "Notable" as signalling that this list is a selection of the more important settled points, not the complete set of roughly forty.

## Boundaries

1. **"The handoff and supervisor machinery belongs to `fleet`, review methodology to `sanity-checker`."** — Two areas of the project are owned by other seats and not by yours: the code and processes for session handoff and for supervising agents are the property of the seat named `fleet`, and the question of how reviews are conducted — the methods and procedures of reviewing — is the property of the seat named `sanity-checker`. "Belongs to" I read as ownership of both the work and the decisions in that area.
2. **"If your work needs a change there, say so rather than reaching into it — those seats hold the context."** — Should your own tasks turn out to require a modification inside one of those two areas, the correct action is to state that a change is needed (raise it, report it) instead of going in and making the change yourself; the reason given is that the seats which own those areas possess the accumulated background knowledge needed to make such a change correctly. "Say so" is ambiguous as to the audience — to the user, or to the owning seat — and the sentence does not specify; I take it as at minimum telling the user, since the user is this file's stated interlocutor.

## First action

1. **"Read the specification and the slice plan, check whether PR #55 has merged, then report to the user where the road stands and ask which step he wants first."** — Your opening sequence of actions, in this order: read the two documents named earlier (the design specification `git-gatekeeper-design.md` and the build slice plan `3-git-gatekeeper-build-slice-plan.md`); determine the current state of pull request #55, specifically whether it has been merged yet; then tell the user the current position of the work along the three-item road described above; and finally ask him which of the steps he would like you to take up first. I read the last clause as making the choice of next step his, not yours — you present the state and request a direction.
2. **"Do not start the evidence-format design without his go-ahead: it is instruction-class work and its shape is his ruling to make."** — You are forbidden to begin work on road item 1 (designing the walked-approval evidence format) until the user has explicitly authorized it. Two reasons are given: first, that this design is itself an instance of the "instruction class" of work (the same category the format is meant to govern), and second, that what form the design should take is a decision reserved to the user, one he must rule on personally rather than one you may settle by proposing a design and proceeding. I read "his go-ahead" as an explicit affirmative instruction from the user, not mere absence of objection.

