Read {TARGET_PATH} in full (including any YAML frontmatter). Then restate, in your own words, exactly what you think each sentence means — completely and as clearly as possible. It may take more words than the original to say exactly what you think a sentence means: many sentences use ambiguous words with several meanings, or jargon, or coined expressions that are hard to interpret out of their normal context. When that happens, spell out the meaning you actually took; if you genuinely cannot tell between meanings, say so and give the readings. Do not repair anything, do not fill gaps. Go section by section; for each section write the section name, then a numbered list of your restatements, one per sentence. In the frontmatter, restate prose fields such as a description like any other sentences; skip data fields — a name, ids, dates. No commentary, no evaluation, no summary, no questions at the end.

HOW TO DELIVER YOUR ANSWER. Write your answer to {REPORT_PATH}, with whatever file-writing tool you have. That file is your entire deliverable: this cell discards what you say in conversation, so an answer given only in chat is a lost answer. Write it once, when your analysis is complete, rather than building it up across several writes.

{REPORT_PATH} must restate every sentence in {TARGET_PATH}. The one exception is a target with nothing to restate — an empty file, or one holding only frontmatter data fields: write {REPORT_PATH} saying that, and name what you found there. A missing or empty report is read as a run that did not happen, and it is discarded and rerun. {REPORT_PATH} is the only file to create; write nothing anywhere else.
