Read {TARGET_PATH} in full, including any YAML frontmatter. Your context is deliberately minimal — what your runtime already loaded, the project glossary at `docs/nedschorus-wiki/nedschorus-glossary.md`, which you read before the document, the document under review, and whatever that document references by an explicit path. Nothing else: do not go looking. That limit is the point, because {TARGET_PATH} must be understandable by a future agent who has only this much. {TARGET_PATH} is read-only: do not edit it or anything else in the checkout. The one file you create is your report, described at the end.

Your subject is the terms in {TARGET_PATH}: the words and short phrases it uses as subjects, objects, actions, conditions and shorthands for its ideas. A project-term is a term listed in the glossary. A key-term is a term that is a project-term or should be one, because this project does something unexpected with it or gives it a meaning a reader could not guess. A project-term joined with an ordinary word in its ordinary sense, such as "handoff-supervisor lock", is not a new key-term and needs no entry; only the project-term inside it is hyphenated. First, list the key-terms you find. For each, check whether it is already in the glossary. If it is, check that {TARGET_PATH} writes it exactly as the glossary does. If it is not, propose that it be added to the glossary. Second, check every term against the consistency tests below. The reader of {TARGET_PATH} will have as little context as you, plus a repository and the internet that you cannot see. That is why our names must be carefully chosen and unlikely to collide with a different meaning it could find there. Your goal is to report every term that keeps {TARGET_PATH} from being clear and unambiguous prose, so that its reader need not guess or go looking for a matching term elsewhere. Like you, that reader knows standard software-engineering terms and technical English; those terms are established. So is every project-term, which the user has ruled on: never propose renaming, splitting or replacing a project-term. About a project-term you flag only misuse: a passage that uses it to mean something other than its glossary definition, or a bare generic word used where a project-term names that meaning, and you name the term. A term defined by a design document {TARGET_PATH} cites by explicit path is established too. Say how many key-terms you listed, how many terms you flagged, and how many flags fell under each test. Look where the document sorts things, in column and heading names, in a rule's condition, in a label carried between sections. Look hardest at ordinary words made quietly technical — flavor, slot, rung, band. A word is not a term where it is used in its ordinary sense. For each term, restate what it names in your own words. Do not use the term itself, or the words kind, type, category, sort, class or form. Where a term labels a set of values, list the values and name what varies across them; that is the thing the term should name. The right name comes from the values, not from the word.

A term's meaning is hard to guess when a reader of the sentence where it first appears in {TARGET_PATH}, who knows the word's ordinary or software-engineering sense and any glossary or design document {TARGET_PATH} cites by explicit path, could not work out what it means or would take it to mean something else.

Flag every term in the document that fails one of these tests. Quote it, and quote the term it clashes with, whether that term is in the document, in standard software vocabulary, or in the glossary:

(1) has a standard software-engineering term for the same thing, or for something slightly narrower or wider, and {TARGET_PATH} does not use that term — give the standard term, and say whether the document's term means the same, something narrower or something wider;
(2) names a thing {TARGET_PATH} already calls by another term;
(3) carries a second meaning in {TARGET_PATH}, in software engineering or in technical writing, so the reader must pick a meaning each time;
(4) is established but used in {TARGET_PATH} to mean something different;
(5) is a key-term that is not in the glossary, or is in the glossary but {TARGET_PATH} writes it differently.

For each flag also propose the exact words of the best fix:
(a) the glossary already has the name: give it;
(b) a standard term fits: give it;
(c) {TARGET_PATH} already uses a better term elsewhere: use that one everywhere;
(d) no name exists: propose a project-term, following the rules for one: a hyphenated phrase, explicit and precise, with as many words as it takes to be clear and to clash with nothing that exists, and with the standard term inside it where one exists. Give it a one-sentence definition. Mark it as a candidate for the user, who alone adds project-terms.
For a term flagged under (3), give one fix for each meaning, and say which uses in {TARGET_PATH} take which.

Give each proposed project-term a one-sentence definition, for the author to include where the name is first used in {TARGET_PATH}. A term {TARGET_PATH} defines that the glossary also defines is defined twice; say so in the item, quoting the glossary's entry, and propose dropping {TARGET_PATH}'s definition in favour of the glossary's. When you cannot tell what the author meant, do not guess: quote the term and the passage, and phrase the item as a question for the author.

Write your report in plain words; outside quotation marks, avoid the terms you flagged. Open with one line saying what kind of document {TARGET_PATH} is — for example instructions to an agent, a design, a memo asking a person to decide. Then one numbered item per flag, in document order: the term in quotes; the passage that first uses it, quoted; your restatement; the numbers of the tests it fails; and the fix, or the question. Then a section headed "Proposed definitions": every proposed project-term, if any, one line each, name then definition. Then one line — "terms listed and kept:" — quoting every term you did not flag, established ones included, and the counts. Do not rate severity.

HOW TO DELIVER YOUR ANSWER. Write your answer to {REPORT_PATH}, with whatever file-writing tool you have. That file is your entire deliverable: this cold-read-cell discards what you say in conversation, so an answer given only in chat is a lost answer. Write it once, when your analysis is complete, rather than building it up across several writes.

Write {REPORT_PATH} even when you found nothing: say so in a sentence and name what you examined. A missing or empty report is read as a run that did not happen, and it is discarded and rerun. {REPORT_PATH} is the only file to create; write nothing anywhere else.
