Read {TARGET_PATH} in full, including any YAML frontmatter. Your context is deliberately minimal — what your runtime already loaded, the document under review, and whatever that document references by an explicit path. Nothing else: do not go looking. That limit is the point, because {TARGET_PATH} must be understandable by a future agent who has only this much. {TARGET_PATH} is read-only: do not edit it or anything else in the checkout. The one file you create is your report, described at the end.

Your subject is the words in {TARGET_PATH}; the nouns, verbs, modifiers and short phrases used as subjects, objects, actions, conditionals and shorthands for key ideas — we call these key-terms. The reader of {TARGET_PATH} will have as little context as you, plus a repository and the internet that you cannot see. That is why our names must be carefully chosen and unlikely to collide with a different meaning it could find there. Your goal is to report every key-term that keeps {TARGET_PATH} from being clear and unambiguous prose, so that its reader need not guess or go looking for a matching key-term elsewhere. Like you, that reader knows standard software-engineering terms and technical English; those key-terms are established, and so is any key-term defined by a glossary or design document {TARGET_PATH} cites by explicit path. List every key-term. Say how many key-terms you listed, how many of those you flagged, and how many flags fell under each criterion. Look where the document sorts things, in column and heading names, in a rule's condition, in a label carried between sections. Look hardest at ordinary words made quietly technical — flavor, slot, rung, band. A word is not a key-term where it is used in its ordinary sense. For each key-term, restate what it names in your own words. Do not use the key-term itself, or the words kind, type, category, sort, class or form. Where a key-term labels a set of values, list the values and name what varies across them; that is the thing the key-term should name. The right name comes from the values, not from the word.

Flag, quoting the key-term and any conflicting key-term, every key-term that:

(1) has a standard software-engineering term used the same way and {TARGET_PATH} does not use that term — give the standard term, and say whether the key-term means the same, something narrower or something wider;
(2) names a thing {TARGET_PATH} already calls by another key-term;
(3) carries a second meaning in {TARGET_PATH}, in software engineering or in technical writing, so the reader must pick a meaning each time;
(4) is established but used in {TARGET_PATH} to mean something different;
(5) is a coined key-term that does not follow the naming rule in CLAUDE.md, so far as you can judge without searching.

For each flag also propose the exact words of the best fix: 
(a) the standard term, where its standard definition matches what {TARGET_PATH} means by the key-term, and the standard term has no second or ambiguous meaning in software engineering or in {TARGET_PATH};
(b) {TARGET_PATH}'s own better key-term, where it has one and no standard term fits;  
(c) otherwise a constructed name that follows the naming rule in CLAUDE.md, with the standard term inside it where one exists, and not conflicting with an established key-term. 
Give each constructed name a one-sentence definition, for the author to include where the name is first used in {TARGET_PATH}. A key-term defined both in {TARGET_PATH} and in a document it cites belongs in one shared glossary; say so in the item. You are not asked to find that glossary. When you cannot tell what the author meant, do not guess: quote the key-term and the passage, and phrase the item as a question for the author. 

Write your report in plain words; outside quotation marks, avoid the key-terms you flagged. Open with one line saying what kind of document {TARGET_PATH} is — for example instructions to an agent, a design, a memo asking a person to decide. Then one numbered item per flag, in document order: the key-term in quotes; the passage that first uses it, quoted; your restatement; the numbers of the criteria it fails; and the fix, or the question. Then a section headed "Proposed definitions": every constructed name, if any, one line each, name then definition. Then one line — "key-terms listed and kept:" — quoting every key-term you did not flag, established ones included, and the counts. Do not rate severity.

HOW TO DELIVER YOUR ANSWER. Write your answer to {REPORT_PATH}, with whatever file-writing tool you have. That file is your entire deliverable: this cell discards what you say in conversation, so an answer given only in chat is a lost answer. Write it once, when your analysis is complete, rather than building it up across several writes.

Write {REPORT_PATH} even when you found nothing: say so in a sentence and name what you examined. A missing or empty report is read as a run that did not happen, and it is discarded and rerun. {REPORT_PATH} is the only file to create; write nothing anywhere else.
