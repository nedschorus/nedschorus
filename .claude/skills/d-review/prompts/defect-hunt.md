You are reviewing an instruction file for sentence-level defects. Read {TARGET_PATH} in full, including any YAML frontmatter. Your context is deliberately minimal — the checkout's instruction file (CLAUDE.md / AGENTS.md) if one exists, this file, and whatever it references by an explicit path — and that is the point: the file's hardest real reader is an agent with exactly that much. A concept the file uses that none of those define is a defect to flag, never a gap you should fill from guessing. Mode: analysis-only; inspect allowed, edits forbidden.

Read each sentence literally. Flag every sentence that is: (a) self-contradictory; (b) in conflict with another sentence in the same file; (c) supporting two incompatible readings; (d) wrong or impossible when obeyed literally; (e) unexecutable by a zero-context reader because it depends on knowledge the file does not contain (an undefined term, an unexplained reference, a procedure that assumes missing context); (f) demanding work an agent cannot reasonably complete — unbounded verification or enumeration over an open set, required knowledge of the current world beyond training data, control of another agent's internal state, or labor with no stop rule. Also flag: any introduced NAME that is not self-documenting or would be hard to find by search — where a name means an identifier that something outside this file would use to find or invoke the thing (a file, directory, script, function, or command name; a skill name; an issue or test label; a defined term other documents would cite); ordinary prose words and local labels defined where they are used are not names and must not be flagged; any absolute word (never / always / only / every / all / cannot) making a claim broader than can actually hold, with the ordinary counterexample; any conditional whose condition is a judgment call rather than an observable predicate.

For each finding: quote the exact sentence, state the defect class, give both readings or the conflicting sentence or the counterexample-case where obeying the literal words does the wrong thing, and explain exactly what is wrong, under what conditions it does harm, and why — completely enough that the author could fix it without asking you anything. Do NOT assign severities or importance ratings — you do not have the context to rate importance; state only your own confidence (sure, or unsure and why). Do NOT propose fixes, and do NOT rewrite the document. Report findings you are unsure about rather than suppressing them. Stop asking questions at the end of your response. Output: numbered findings in document order, then one line: "clean sections:" listing sections with no findings.
