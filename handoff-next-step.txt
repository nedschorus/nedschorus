First action: check whether the md-review grids still running have landed, process any new findings into the documents they reviewed, and commit. The user is AFK and asked that this run continue unattended until the seats are up; two are up already, so the remaining work is finishing the reviews. Run from this worktree (/home/nedlern/Projects/nedschorus/.claude/worktrees/gatekeeper-walk-fork-continuation, branch seat-launch-first-prompt, tip 70d9b9f, push with `git push origin HEAD`).

WHAT IS RUNNING, DETACHED, AND WILL OUTLIVE YOU: a sequential grid runner over docs/agents/doctrine-instructions.md, docs/agents/sidebar-instructions.md, docs/cross-project/fleet-machine-paths-and-checkouts.md, docs/issues/queue/45-session-seat-and-isolation-riders.md and docs/issues/queue/45-ubuntu-fleet-open-work-inventory.md, logging to ~/.claude/handoffs/md-review-runner.log (watch for GRID-DONE and ALL-GRIDS-COMPLETE); and a separate grid on docs/agents/ghi-instructions.md logging to ~/.claude/handoffs/md-review-ghi.log. Records land in md-review-records/2026-08-13-<doc>/. A cell that is 0 bytes is still running, not failed. Read claude-hunt-good.md first — it is consistently the deepest cell — then apply, commit per document, push.

WHAT IS DONE: all twelve seat documents were reviewed or are in the queue; seven documents have been fixed against their reviews and pushed (gatekeeper, seat-first-prompt, agent-seat-model, sanity-checker, fleet, skill-builder briefs, plus the four written pre-emptively). Corrections included: vocabulary defined once in the seat model; a completion criterion per seat; every 2026-08-14 stamp corrected to 2026-08-13 (UTC job timestamps misread as local); the seat-first-prompt's repair command removed because `git worktree add` refuses a non-empty path and the "drop -b" variant was invalid; PR states replaced with how to check them (#51/#52/#53 were closed as ALREADY LANDED, not rejected; #55 and #57 merged); machine-local files given full paths so scripts/md-drift-lint.py passes clean on all twelve.

STILL OWED: a second-pass review of the documents changed after their first review — at minimum seat-first-prompt.md, agent-seat-model.md and gatekeeper-instructions.md, since applying findings can introduce new ones. Do this after the current queue drains, not alongside it.

THE SEATS: `gatekeeper` and `sanity-checker` are running in tmux on the box, in ~/agents/<seat> on branches cut from THIS branch (not main), because main still holds the pre-review briefs until PR #58 merges. The method and its expiry are recorded in docs/issues/queue/45-ubuntu-fleet-open-work-inventory.md § 6a. DO NOT type into either seat's tmux pane: the gatekeeper seat has un-submitted text in its input box that is not ours, and pressing enter on it would fabricate the user's approval. The gatekeeper seat has proposed `git reset --hard origin/main` for its branch; the answer is NO until #58 merges, and the reasoning is recorded in that same inventory section.

CONSTRAINTS: never push to main; the user's Mac-side agent merges. Instruction-class files (CLAUDE.md, ~/agents/<seat>/CLAUDE.local.md, anything under .claude/) need his approval quoted into /home/nedlern/Projects/nedschorus/.walk-approved immediately before each guarded write. Worktree isolation refuses git commands aimed outside this worktree and refuses compound shell redirects — write a python script to a worktree file and run it instead. Commits carry the session id and the Co-Authored-By: Claude Fable 5 line. Today is 2026-08-13 local (PDT); job timestamps are UTC and a day ahead after 5pm.
