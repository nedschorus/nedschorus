First action: report to the user, then wait — there is no unfinished build work in this thread. Verify first from this worktree (/home/nedlern/Projects/nedschorus/.claude/worktrees/gatekeeper-walk-fork-continuation, branch launch-claude-machine-named-launchers, tip 36f3b17): `git status --short` (expect clean), `python3 scripts/handoff-supervisor-test.py` and `python3 scripts/git-gatekeeper-test.py` (expect all cases passed), and `gh pr view 57 --repo nedschorus/nedschorus --json state,mergeable`.

What this thread did, all committed and pushed: the git-gatekeeper five-slice build finished and merged via PR #49 (slices 4 and 5 built here; the gate stays DORMANT until its credential work — remaining road is the walked-approval evidence format, then slice 6, then the credential work, all user-gated). Then PR #57, still open for the user's Mac-side review: scripts/launch-claude renamed to launch-claude-ubuntu with a new launch-claude-mac twin (both teach on the no-name case and list running agents); docs/cross-project/fleet-machine-paths-and-checkouts.md, a full two-machine path reference; the supervisor's branch sync (sync_working_branch_with_main in scripts/handoff-supervisor.py — fetches before each session launch, fast-forwards a clean branch strictly behind main, reports and does nothing otherwise, never merges, never runs on the adoption path, eleven tests); and docs/issues/queue/45-session-seat-and-isolation-riders.md, which carries the five ideas discussed but deliberately not built. Read that riders file before proposing any of them — it holds the reasoning, not just the titles.

Machine state changed outside git, so it will not show in any diff: /home/nedlern/.bashrc gained a PATH export ABOVE its non-interactive early-return, so `ssh nedlern@ned-box '<command>'` now finds ~/.local/bin/claude; and /home/nedlern/.claude/CLAUDE.md was created, telling every session on this box that the user sits at a Mac and commands handed to him must name their machine.

The live situation the user is untangling, and where he needs help next: three sessions were sharing /home/nedlern/agents/choirmaster on branch choirmaster at once — background jobs ea663864 (owns the live choirmaster work, blocked awaiting his ruling on the sanity-checker grid-seat walk) and d9eda3ec, plus tmux seat f741668d. Jobs 3d8bf995, 49e0a3cf and d9eda3ec each self-identify as retired or never-tasked and are safe to stop (Ctrl+X in `claude agents`, run ON THE BOX). The consolidation he wants: have ea663864 write a handoff, stop it and the stalled-or-duplicate f741668d, then relaunch one foreground seat with `~/Projects/nedschorus/scripts/launch-claude choirmaster` from the Mac (still the OLD script name until PR #57 merges), whose supervisor boots from that fresh handoff. Do not kill anything without checking transcript modification times under ~/.claude/projects/ — process uptime is not idle time, an error made once already today.

Standing constraints: worktree isolation (git operations target this worktree only; push with `git push origin HEAD:choirmaster` is WRONG for this branch — use `git push origin HEAD`, the branch is launch-claude-machine-named-launchers); never push to main, the user's Mac-side agent merges; CLAUDE.md and .claude/ are instruction-class and need his walked approval through the marker at /home/nedlern/Projects/nedschorus/.walk-approved; commits carry the session id and the Co-Authored-By: Claude Fable 5 line.
