# Minutes — the decisions the AI-native architecture document leaves open

Walk opened 2026-09-03 by the merge-lane seat at the user's direction ("we walk the overview first as I'm hoping it useful for all agents"). Main at 33b75525. Eleven items, one decision each; the walk text is docs/walk/2026-09-03-ai-native-architecture-overview-decisions.md. The document under discussion is docs/cross-project/nedschorus-ai-native-software-development.md, merged 2026-09-03 via PR #247 (fee8e8a1), with its founding-plan references repointed via PR #248 (33b75525).

Draft cold-read by fast-clarify 2026-09-03 (72 s, report at docs/walk/2026-09-03-ai-native-architecture-overview-decisions-suggestions.md): eleven zero-context questions, all answered by naming the file each shorthand referred to; four rule gaps closed with one sentence each (what governs while item 1 runs; item 2 lands after item 1's triage; item 9 when item 4 is deferred; item 8 exceptions become their own items).

The walk docs/walk/2026-09-02-should-a-pr-reviewer-report-code-design-mismatch is PARKED at its item 2 until this walk's item 5 is ruled; its minutes carry the parking line.

## Item 1 — cold-read the document before it governs

processed 2026-09-03 → revised. The user's words: "I was thinking we'd walk first, then cold read." The order is: this walk closes, then the cold-read grid runs on the document (with the edits this walk produces already in it), then a triage walk of the findings; the document governs after both. CAPTURED: task #97 carries the cold read as the step after item 11; nothing else to capture until the walk closes.

## Item 2 — what agents must read, and where the pointer lives

processed 2026-09-03 → revised and accepted ("y"), after four exchanges that replaced the item's premise. The ruling, in the user's terms: the document is the project's MISSION or OBJECTIVE, not a plan ("planned" is a built/in-process/planned word and is reserved for that cycle), so it is not maintained to match reality; reality is measured against it. THE SPLIT: (1) CLAUDE.md carries the mission verbatim from the document — the central rule, § 19 Standing decisions (seventeen), § 20 Project organization and § 21 Accepted residuals (the two sections that are rules the founding plan carried and that twenty files cite as live) — plus a closing paragraph that names the notes file and gives the channel for a question, suggestion or concern about either; (2) everything else (§§ 2-18, 22) becomes docs/cross-project/nedschorus-ai-native-software-development-notes.md with front matter in his words: instructional or explanatory, possible implementation details, not prescriptive, not vetted, not maintained to match reality, ending with the same complaint line; (3) the current file goes away and the twenty citations #248 repointed are repointed again to CLAUDE.md § Project organization in the same pull request. PROMOTION: as the notes mature an item may move up into the objective, or into the built/in-process/planned cycle as an issue with a pair document; the second is dangerous because the cycle moves and the objective does not, so every promotion goes through the user (or the objective agent working with him) as a recorded edit, never a silent drift. THE OBJECTIVE AGENT: a dedicated seat readers complain to when the objective or the notes seem wrong, stale or two-faced; it proposes edits with the user (later with the master) through the ordinary pull-request path; it needs a brief under docs/agents. THE CHANNEL (the first SOP for agents talking to the user, none existed): an agent the user is directing raises it in its own session; an agent he is not directing files a GitHub issue carrying the `draft` label through ghi-write (the label is already his review queue in the issue world); once the master exists it becomes the single channel for every agent he is not directing — his guess, ruled at item 3. CAPTURED: the split is one pull request from this seat after this walk closes (task #98); the objective seat's brief (task #99); the communication SOP (task #100).

RE-PLANNED 2026-09-03 after item 2: eleven items become eight. Removed, because their sections are now non-prescriptive notes and no note can conflict with a dated ruling: former item 4 (diagnosis ladder — the 2026-07-31 four-attempt ruling stands; § 8's own text asks for reconciliation before implementation, which is when it happens, under issue #21; recorded as OPEN there, not here); former item 5 (§ 13 versus "code is master after round 1" — § 13 is a note; the parked walk resumes after this walk, re-planned under the mission's standing decisions 4 and 5 with § 13 as a candidate implementation); former item 6 (reviewer-scope pin — § 7 is a note; the pull-request reviewer rule stands unchanged; the cause-search applies to the review node when it is built). Kept and renumbered: 3 master (unchanged: standing decision 7 is in the mission, so the conflict with the seat model is real); 4 the ghi-write skill line (target now CLAUDE.md § Project organization); 5 the four dropped rules (edits land in CLAUDE.md); 6 the seventeen standing decisions confirmed as written; 7 build step 1 filed as an issue (this IS the promotion path, ruled by him); 8 captures.

## Item 3 — reopen the 2026-08-13 "no master" ruling

open 2026-09-03 — the user ruled the WORD: "I like liaison." The role as he stated it: a go-between for invisible agents to talk indirectly to him, using the built-in cross-session messaging (SendMessage/ListAgents, which this seat used with MD-skills and reboot-test today), reserved as a go-between and NOT a doer; "master" and "go-between" are the wrong words. This seat's split, which he was asked to confirm as the whole item: the document's "master" bundles a human-facing channel (an agent — the LIAISON seat) and a controller that tracks work items, builds input packages, starts/restarts executions and checks transitions (code, the CONTROLLER, per the document's own "boring, inspectable software"); the seat model's "a seat's only channel to another seat is through him" is no longer true and the paragraph should say so; fallback when the liaison is down is the `draft`-labeled issue from item 2. Awaiting his Y on the whole item versus the word alone.

processed 2026-09-03 → accepted ("y"), after his further point "this also obsoletes our roles md" (the roles md is docs/agents/agent-seat-model.md: its roster names seven seats, none running; the eight seats that exist are directories under ~/agents with no entry there). THE RULING: (1) the 2026-08-13 no-master ruling is reopened and replaced by a LIAISON seat — a go-between only, never a doer — through which every agent the user is not directing reaches him, using the built-in cross-session messaging, with the `draft`-labeled issue as fallback when the liaison is down; (2) the CONTROLLER — tracking work items, building input packages, starting/restarting executions, checking transitions — is code, not a seat, per the document's own "boring, inspectable software"; (3) "master" is renamed throughout: "liaison" where a sentence means the channel, "controller" where it means the router code (standing decision 7 in the mission; the notes' §§ 10, 15, 17, 18 and the vocabulary table); (4) the seat model is REDUCED, not deleted (thirteen files point at it): delete § The seats and § How many run, replaced by one sentence — the seats that exist are the directories under ~/agents and the briefs under docs/agents, and the user chooses which run; delete § Why there is no master agent, replaced by a paragraph recording the reopening (the revisit condition named on 2026-08-13 is now the mission's core; the liaison is a router and the user's channel, not a smarter agent; a seat's only channel to another seat is no longer through the user, because cross-session messaging now exists); keep the vocabulary, the two ways a seat ends, launching a seat, and the branches section. CAPTURED: all of it lands in the split pull request (task #98, updated); the seven stale seat briefs the roster pointed at (gatekeeper, sanity-checker, skill-builder, ghi, fleet, doctrine, sidebar) are a separate cleanup (task #101); task #100 (the SOP) reworded from master to liaison.

(Headings below follow the eight-item numbering from the re-plan at item 2; the former items 4, 5 and 6 are recorded as removed in that re-plan note.)

## Item 4 — the ghi-write skill line 36

processed 2026-09-03 → accepted ("y") as presented. THE RULING: in .claude/skills/ghi-write/SKILL.md line 36, the path changes and nothing else — old: the routing doctrine (queues, homes, the drain) is `docs/cross-project/nedschorus-founding-plan.md` § Project organization; new: the routing doctrine (queues, homes, the drain) is `CLAUDE.md` § Project organization. The edit lands inside the split pull request (task #98), with his "y" quoted into the `.walk-approved` marker at the checkout root for that one write, since the instruction-file guard refused it on 2026-09-03 without a marker. CAPTURED: task #96's guard-held remainder folds into task #98.

## Item 5 — four rules the document dropped

open 2026-09-03 — presented as one item with one disposition for the four; the user replied "/walk-me-through these", so the item is split into sub-steps 5.1 to 5.4, one rule each (the `draft` label; the entry checkpoint; the handoff scrub; the check-in terminology), with the disposition asked at 5.4.

5.1 the `draft` label — accepted ("y") 2026-09-03: one sentence enters the mission's standing decisions in CLAUDE.md, exact words: "Issues labeled `draft` are the user's review queue in the issue world; an agent he is not directing reaches him by filing one." The vocabulary entry's Home repoints to CLAUDE.md. Lands in the split pull request (task #98).

5.2 the entry checkpoint — presented 2026-09-03; the user replied with a side point instead of a ruling: "the liaison agent should have a monitor on draft GHIs. Not sure we have a place to start designing/specking out agents like that." SIDE RULING recorded: the liaison seat polls issues labeled `draft` and brings each new one to the user; the label stays the fallback queue, unwatched, while the liaison is down. PLACE: no dedicated place exists; the pattern is a GitHub issue plus a pair document under docs/issues/ (precedents 46-ghi-info-agent-design.md, 26-dynamic-agent-team-model.md), the brief under docs/agents when built; the liaison is a standing decision (item 3) but not yet planned, no issue names it. CAPTURED: task #102, file the liaison issue and pair document through ghi-write after this walk; added to item 8's capture list. 5.2 REVISED by the user 2026-09-03: "I dont thik we are going to bring anything over from the new project directly - only through agents indrectly, so I dont think we need that legacy rules now." Then "continue". THE RULING: the entry checkpoint rule (every legacy import writes a `Gatekeeper-import` trailer) is RETIRED, not rehomed; nothing is copied from the legacy system directly, only reworked by agents, which mission standing decision 16 already governs (legacy is reference; reused features are classified preserve/update/remove/consider). The vocabulary entry says retired and why. NOT in scope here: the gatekeeper program still writes `Gatekeeper-import: none` on every check-in and its design describes the mechanism; harmless with nothing to record; removing it is a separate gatekeeper change if he ever wants one. Lands in the split pull request (task #98).

5.3 the handoff scrub — accepted with a correction 2026-09-03. The user: "the founding plan is obsolete. We have a new handoff system tuned up as recently as yesterday." THE RULING: the scrub is retired outright; no sentence in CLAUDE.md. Correction found while checking the live system: docs/cross-project/fast-handoff-design.md line 431 records that on 2026-08-29 the user expired the queue-status line on the ignition prompt too ("Also useless is the reminder there are files in the queues. Thats what queues are for."); only the supervisor's console print remains. So the vocabulary entry's "the duty now rides the ignition prompt's queue-status line" is stale as well. The entry will say retired, cite the fast-handoff design's 2026-08-29 ruling, and conflict note 5 closes. Lands in the split pull request (task #98).

5.4 the check-in terminology — REVISED by the user 2026-09-03: "I don't think we need to define check-in. And I would not start putting bad words in claude.md. And land isn't that bad. However I do think we should have an approved vocab wiki page. I think we at least already have some notes on that. THough check-in probably would not be on it as its standard SDLC." So: no check-in sentence in CLAUDE.md; no retired-words list anywhere; "land"/"landing" are ordinary words again; check-in is standard SDLC and undefined by the project. NEW CAPTURE PROPOSED: the proposed vocabulary page (docs/wiki/queue/213-project-vocabulary.md, 48 entries plus conflicts, queued since PR #214; docs/wiki has no approved page yet) becomes the approved vocabulary wiki page, drained to docs/wiki/ after a walk of its own, scheduled after this walk and the parked one; its entries for the retired rules and check-in are settled there.

processed 2026-09-03 → revised and accepted ("assuming you agree with my decisions Y"; this seat agrees on all four). THE RULING: (1) one sentence enters the mission's standing decisions in CLAUDE.md, exact words: "Issues labeled `draft` are the user's review queue in the issue world; an agent he is not directing reaches him by filing one."; (2) the entry checkpoint is retired; (3) the handoff scrub is retired; (4) no check-in definition and no retired-words text anywhere, land is an ordinary word; (5) a vocabulary walk, after this walk and the parked one, turns docs/wiki/queue/213-project-vocabulary.md into the approved vocabulary wiki page under docs/wiki/, where the retired rules' and check-in's entries are settled. CAPTURED: (1) rides the split pull request (task #98); (5) is task #103. Also from this item: task #102, the liaison seat issue with pair document.

## Item 6 — the seventeen standing decisions confirmed as written

open 2026-09-03 — presented as written; the user replied with his architecture picture and "Are we in sync - your thoughts?": the design-to-main workflow is not going into CLAUDE.md but into a CPC (code-prompt-code) subsystem; unsure a master is needed, maybe one to prioritize the next GHI; an initiating agent per component (a PR-to-be); an arbitrator for when code fails tests (test or code?); a liaison to talk to the human when agents are headless; after production, users and loggers, then ingestion agents. This seat's answer: in sync on the initiating agent (= the document's work item and producing nodes), the arbitrator (= bounded failure diagnosis, decision 6, and the parked walk's subject; the word arbitrator is better and should enter decision 6), the liaison (item 3), ingestion (production-triage node, decision 10), and the workflow as CPC (already ruled at item 2: §§ 2-18 are notes/candidate implementation; decision 13 states the principle; the seventeen are the principles the CPC implements and stay in CLAUDE.md per item 2). DISAGREED, stated: a master to prioritize the next issue — decision 1 gives priority to the human, decision 8 the next step to the controller's fixed graph; picking the next issue is his call today and code later; a recommendation on what is next is a liaison message, not a decider. REVISED RECOMMENDATION presented: decision 6 "A root-cause agent can inspect..." becomes "An arbitrator can inspect..."; otherwise the seventeen stand with item 3's decision 7 rename and item 5's label sentence. Awaiting his word.

Side ruling 2026-09-03, the user clarifying the "master": "if we ever run out of stuff to keep 3-5 agents busy, I'll ask some agent for recommendations on the next task/GHI or finding from the field to work on. Maybe that should be call the projecct manager?" This seat: an on-demand advisor, not a decider, so the disagreement above dissolves; "project manager" is the wrong SDLC word (owns schedule, assigns work — the deciding he keeps); the role is BACKLOG TRIAGE, output a recommendation; on demand, not a standing seat, built when the fleet first runs dry (decision 12). Changes nothing in the seventeen (decision 1 keeps priority with the human). CAPTURED: one sentence in the notes file § 10 roster, in the split pull request (task #98): "Backlog triage is an on-demand node the human invokes when the fleet has spare capacity; it reads open issues and production findings and recommends the next work item with reasons; the human chooses." Name pending his word.

Side point 2026-09-03, the user: "we'll need a root cause skill for agents to get guidance on how to look further than their tasks, deeper back in the pipeline. and at what point to give up and maybe hand that to a specialized root cause agent (maybe that's exactly what the arbitrator is?" This seat: that skill is issue #21 (open, unbuilt: "Candidate skill: diagnose-failure — bounded causal debugging with a three-fix escalation stop"); § 8 test-failure policy is its design text; decision 4 is the skill half (every node examines its inputs), decision 6 the agent half (root-cause agent); the skill's stop is the hand-off. Yes, the arbitrator IS the root-cause agent; "test or code?" is its commonest question. Consequence: the arbitrator rename of decision 6 is WITHDRAWN (it narrows the role); decision 6 stands as written; arbitration is described in the notes § 8. CAPTURED: task #104, route to the user — #21's design should state both halves as one design. ITEM 6 RECOMMENDATION restored to the original: confirm the seventeen as written plus item 3's decision 7 rename and item 5's label sentence. Awaiting his word.

RE-PLANNED 2026-09-03 at item 6, on the user's ruling: "None of this will go into claude.md - that is not appropriate. This is the objective.md wiki page, or will be when vetted. Claude should have a simple link to that page." This AMENDS item 2: the mission (the central rule, § 19 standing decisions, § 20 project organization, § 21 accepted residuals, plus the closing channel paragraph) goes to an OBJECTIVE WIKI PAGE, in docs/wiki/queue/ until vetted (the walk, then the cold read and its triage, per item 1), then drained to docs/wiki/; CLAUDE.md gains ONE line, a link to that page, and carries none of the text. Consequences: item 4's skill-line target becomes the objective page's § Project organization (not CLAUDE.md); item 5's label sentence enters the objective page's standing decisions (not CLAUDE.md); the twenty citations repoint to the objective page; task #98 and item 8's capture list are updated accordingly. He also asked to REVERT the condensed 18-line list ("way too condensed to be meaningful"; the "too long" message was meant for another agent) to the longer presentation, calling the condensed list "a good summary". SPLIT AXIS CORRECTED 2026-09-03, second re-plan at item 6. This seat first proposed a TOPIC split (concept sections to the objective, implementation-guess sections — 14, 15, 16, 17, 18 — to the notes), after the user objected that item 2's original split demoted the explanatory half ("I want a document humans or agents can read and understand what we are doing and how they might be able to help"). He replied: "BOth the objective page and the notes pages, should be well constructed, human readable docs - one high level, one detail. Is your split high level/low level - I'd think that both might have all relevant sections, though maybe we don't have any details on some." So the axis is ALTITUDE, not topic: both pages are readable documents with parallel sections in the same order; the objective states each subject at high level, the notes carry the detail behind it; a subject with no detail yet simply has no notes section. This seat withdrew the topic split and agreed. Example given: § 5 node contract becomes one paragraph in the objective (explicit input package in; artifact, provenance, evidence and recommended transition out; nothing important only in a chat message) and keeps the field-by-field schema, outcome dimensions and decision packet in the notes. EXCEPTION named: §§ 20 and 21 (project organization, accepted residuals) are operative rules cited by path in twenty files, so they are carried WHOLE in the objective, not summarized; § 19's decisions are already one line each. ACCEPTED ("y") 2026-09-03. THE STANDING SHAPE, superseding item 2's split: TWO readable documents with parallel sections in the same order — (1) the OBJECTIVE page, high level, in docs/wiki/queue/ until vetted then drained to docs/wiki/, opening with the user's verbatim notes, stating every subject at a level a human or agent can read end to end and learn what the project is doing and where to help, carrying §§ 19, 20 and 21 whole because they are operative rules cited by path; (2) the NOTES page, the detail behind each subject, same order, a subject with no detail yet simply absent, front matter in his words (instructional or explanatory, possible implementation details, not prescriptive, not vetted, not maintained to match reality, closing with the complaint channel line); (3) CLAUDE.md gains ONE line linking the objective page and carries none of the text; (4) the current file goes away and the twenty citations repoint to the objective page.

DECISIONS 6 AND 7 CORRECTED by the user 2026-09-04, the rest of the seventeen passed ("The rest look OK"). His words on 6: "Each node is responsible some degree of validation of its inputs - if they are defective (or thought to be defective) they are report that, but not fix that. That is not what your 6 says. REviewers only job is to review its inputs are report its findings - so those are findings, not fixes." His words on 7: "I dont think there is a global controller in these kinds of terms ---- Nodes communicate through the controller; --- there is only the global state machine with nodes. How nodes connect is not defined - my guess is its a mix of subagents, peer agents - of various provenances, humans, CPC, maybe even messaging - how they connect is irrelevant to the state machine or nodes." PROPOSED TEXT presented, awaiting his word: (6) "A node reports defects in its inputs; it does not fix them. Every node validates its inputs to some degree. A defect, or a suspected defect, is reported to the node that produced it. Diagnosis may look broadly across the graph but changes narrowly: a reviewer's output is findings, never fixes."; (7) "The state machine is logically persistent, not process-immortal. Durable events, artifacts, questions and decisions let any process restart. How nodes connect, whether by subagents, peer agents of various provenances, humans, CPC, or messaging, is an implementation choice, irrelevant to the state machine and to the nodes."; (18) "An agent the user is not directing reaches him through the liaison, or by filing an issue labeled `draft`. Issues labeled `draft` are his review queue in the issue world." — the liaison moves out of 7 into 18, which NARROWS item 3's ruling (the liaison remains the human channel; node-to-node traffic does NOT pass through it; no controller mediates communication). ALSO RAISED by this seat: decision 4 ends "A safe nit may be repaired, verified, and recorded locally," which reads two ways (its own artifact, or the defective input); his rule forbids the second; recommended striking the clause.

DECISION 6 REFINED by the user 2026-09-04, report-not-fix is PER ROLE, not universal: "writers (of code or natural language) can and should fix nits. Reviewers report, not fix. Arbitrators (agents used to handle collisions like merge collisions or test failures (is it the test or the code) I think will be able to fix, not just report. Of course no writer purposely writes things that will be later flagged as nits or serious errors." REVISED TEXT presented, awaiting his word: "6. Fixing belongs to writers and arbitrators; reviewers only report. Every node validates its inputs to some degree. A writer fixes nits in its own output, and no writer sets out to produce one. A reviewer reports findings and fixes nothing. An arbitrator, the node that resolves a collision such as a merge conflict or a test failure where either the test or the code may be wrong, may fix rather than only report, and its fix is recorded, never silent. A defect a node finds in its inputs is reported to the node that produced them." (The original 6's no-silent-rewrite property is kept; its "look broadly, change narrowly" headline is replaced by the role split.) Decision 4's nit clause is still struck, now because 6 says who may fix and 4's "every node" would license a reviewer.

NEW DECISION 19 raised by the user 2026-09-04: "while I don't want to read code changes, I do want to be asked to read any natural language changes in the design or test design (and most MD files that are outside the code-main pipeline - so outside this discussion). I'm not going to read the interchanges between nodes." Then, asked gate or notice: "I am a gate, but I may approve without reading - my choice. (Maybe a bad one :) Because I'm a gate I need to minimize when agents are blocked by me with careful instructions and careful design of the states and state machines." Nothing in the seventeen named which artifacts he reads (decision 2 makes everything readable and requires nothing), so this is a new decision, not an edit. TEXT presented, awaiting his word: "19. The human is a gate on the design and the test design. A change to either is put to him before it is accepted, and he may approve without reading it; that is his choice, not the machine's. He is not a gate on code changes or on the traffic between nodes. Because a human gate blocks agents, the instructions and the state machine are designed to keep that wait short: ask one decision at a time, say plainly what is being asked, let independent work continue while he decides, and never queue him a question another node could settle." OUT OF SCOPE here, his bracket: most MD files outside the code-to-main pipeline. NOTE: the last sentence binds the CPC state machine that item 7 files as an issue, not only the human.

NEW DECISION 20, the VALIDATOR NODE, raised by the user 2026-09-04 ("New concept inspired by work with reboot-test agent"): "I think we need to add a new kind of node, a validator. THe validator conceptually, and initally is simple. Can the next agent complete its tasks using these inputs. That will not pollute the next agent, and lets us screen out serious problems faster, and eventually with specific criteria that the previous agent can't handle. In the case of contracts - 'can this contract be fully vetted or executed or validated as written' We could use this to valid designs for code or tests, or what reboot-test is called contracts - the larger conditions before, during and after tests." DISTINCT from decision 4 (the consuming node examines its own inputs): the validator is a SEPARATE node that runs BEFORE the consumer is instantiated, so a defective package burns a validator instead of a producer and the producer stays fresh for the corrected package. Distinct from a reviewer (asks "is this right?" against governing intent) — the validator asks "is this enough to work from?". Reporter class, not fixer. TEXT presented, awaiting his word: "20. Inputs are validated before the consuming node is instantiated. A validator node answers one question about a package: can the next node complete its task using these inputs? It answers yes or no with reasons and never produces the artifact. So a rejected package costs a validator rather than a burned producer, and the producing node stays fresh when a corrected package arrives. The question stays single; a validator that judges quality has become a second reviewer." SENT to reboot-test-10 at his invitation: the validator's effect on their three contract tiers (contract-user-check bundles two questions — "is anything the callers need missing" is validator work needing no user; "is this the right boundary" is his judgement — and splitting it serves decision 19's minimise-blocking rule), plus the open question of whether a validator firing consumes a restart.pass number. THEY REPLIED: all four adopted into their walk item 2.4; a validator firing consumes no pass number (it refuses to start one) and the loop stays bounded because a rejection consumes the PRODUCER's next pass number; contract-user-check split as proposed. NOT TREATED AS A RULING HERE: they report the user approved the validator in their session, which is hearsay in this walk — decision 20's text still needs his word. This seat pushed back on one of their two additions: they wrote that a validator's approval "counts as no evidence" (rejection being direct evidence); the asymmetry is right but the magnitude overshoots — an approval is evidence bounded by the criteria applied, and under a zero-evidence reading there is no incentive ever to add a criterion, which contradicts the user's own "eventually with specific criteria" growth path; proposed instead that an approval carries the weight of the criteria it applied and names them, so a later failure is diagnosable and the criteria set earns its next member. They adopted that wording and amended their already-ruled item, telling the user what changed and why.

DECISION 4 REPLACED, not merely trimmed — the user 2026-09-04, from his work with reboot-test: "agents should be told to complain if they can not complete there tasks, or maybe something slightly broader, but not told to first review their inputs (including tasks) to validate them, because agents love to blame their problems on what comes before them, and we have a concern that if they are asked to look for problems before they start, even if they don't block on inputs, they will not try as hard to complete there tasks - and then block unnessarily (at times) on outputs." Decision 4 as written ("Every node examines its inputs before producing output") primes exactly that failure. Same evidence asymmetry as the validator-approval argument, pointing the other way: "I could not complete this" is grounded (the agent tried and hit a wall); "these inputs look defective" is a forecast, and a self-serving one. TEXT presented, awaiting his word: "4. A node attempts its task and reports what it could not do. A producing node is not asked to audit its inputs before starting. It is asked to complete the work, and to report plainly either that it could not, naming what defeated it, or that the work uncovered something upstream the graph needs to know. Whether the inputs are sufficient is the validator's question, asked before this node exists. A node primed to look for upstream faults finds them, tries less hard, and blocks on work it could have finished." The second reporting case is the "slightly broader" he reached for: discovery DURING the work is grounded, an audit before it is not. COST NAMED to him: decisions 4 and 20 are now coupled — producers no longer screen inputs, so an edge without a validator catches a defective package only when the work fails; the validator cannot be skipped while this rule stands. CONSEQUENTIAL: decision 6's closing sentence must say a defect DISCOVERED WHILE WORKING, not one found by inspection, or it reintroduces the audit.

VALIDATOR GENERALISED TO A CLASS by the user 2026-09-04: "all nodes are told to complain if they are having problems with creating solid outputs, but only validators (of which we have several forms cold-reader (for MDs), code-reviewer (for code) and now validators (for other stuff/nodes I guess) are told not to output anything besides concerns about the inputs (to the next node) and report them to the calling node - I think." This seat: the class already exists in the document (§ 5 / line 173 — "A successful producing node returns a candidate artifact, provenance, evidence, and a recommended transition. A non-producing node returns evidence and a route"); his contribution is the roster. REPORT TO THE CALLER, not to the input's producer — better than what this walk had, because it keeps routing knowledge out of the node and matches decision 7 (how nodes connect is not the node's business); the state machine routes. KEPT DISTINCT inside the class, this seat's one caveat: the three forms share an output shape but not a question (cold reader = does this read correctly to a fresh reader; code reviewer = is this correct against its design; validator = can the next node work from this at all); collapsing the questions is how a validator becomes a second reviewer at doubled cost. DECISION 6 REVISED AGAIN, presented, awaiting his word: "6. Fixing belongs to writers and arbitrators; non-producing nodes only report. A writer fixes nits in its own output, and no writer sets out to produce one. A non-producing node, whether a cold reader for documents, a code reviewer for code, or a validator for whether inputs suffice, emits findings and no artifact, and returns them to the node that called it, which routes them. An arbitrator, the node that resolves a collision such as a merge conflict or a test failure where either the test or the code may be wrong, may fix rather than only report, and its fix is recorded, never silent." Its old closing sentence on defects found in inputs is DROPPED, because decision 4 now owns that. TIGHTENED after the user called the wording "crappy ambiguous language, but conceptually good": "6. Only writers and arbitrators change artifacts. A writer fixes nits in its own output. An arbitrator resolves a collision, such as a merge conflict or a failing test where either the test or the code may be wrong, and may fix rather than only report; its fix is recorded, never silent. A non-producing node, whether cold reader, code reviewer or validator, returns findings and no artifact to whoever called it." ("Non-producing node" is the document's own term, § 5 / line 173, not a new coinage.) The whole set gets a language pass before the pull request and then the cold-read grid, which is item 1's ruling and the instrument for exactly this complaint.

CROSS-SEAT SYNC 2026-09-04, at the user's instruction ("can you check in again with reboo-test you are both workig o different aspects of the same problem"). They sent their full ruled state. NO SUBSTANTIVE CONTRADICTION found. Independent convergence worth recording: their "a node does not inspect its inputs; if you cannot complete your task because your inputs or instructions are defective, report those defects" is the substance of decision 4, ruled separately in two windows within the hour, neither seat feeding the other. ONE COLLISION, vocabulary: the user BANNED the word "artifact" in their session today (too broad and ambiguous; name things specifically — design, contract, test plan, code, tests; where a general word is unavoidable, "a node's output" or "the package an edge carries"). The standing decisions use it in at least five places (decision 3 "governing artifacts", decision 5 "a changed artifact invalidates the old review", decision 15 "durable artifacts are written for an independent reader", and the two drafted here). NOT adopted on their report — hearsay in this walk; PUT TO HIM as a wording pass over the eighteen, meaning unchanged. TWO OF THEIR RULINGS PROPOSED here as objective-level decisions, awaiting his word: "Prose always precedes code" (design and contract are prose, code comes from them; the test plan is prose, tests come from it) and "An arbitrator is a node with two input connections" (structural, sharper than decision 6's example list). Also relayed to them: where the sections they cite land under the altitude split (§§ 5, 7, 8 detail to the notes with a high-level paragraph above in the objective; §§ 19-21 whole in the objective), and the consequence — departing from a notes section is allowed if the design says why, contradicting the objective is not; cite sections, not the file path, which goes away. Question put to them: a validator whose criteria grow may need the governing prose alongside the package it judges, which under their two-inputs-means-arbitrator rule would misclassify it, so decide now whether the machine assembles that package before the validator sees it.

CONFLICT BETWEEN TWO OF THE USER'S OWN RULINGS, surfaced 2026-09-04 and put to him. In THIS session he said arbitrators "will be able to fix, not just report" (hedged, "I think"). In the reboot-test session he ruled and approved the opposite: the arbitrator "does not write the merged output and does not edit either side. It rules, and a fresh agent at the destination does the work." THEIR SYNTHESIS, which this seat endorsed over its own position: a merge collision's ruling IS the merged content, so let the arbitrator write it; a code-versus-tests collision's ruling only names the wrong side, so a producer writes the fix. THE PRINCIPLE UNDER IT, stated by this seat and sent to them: "An arbitrator writes only when its ruling and the output are the same thing." It also preserves their reason (the judge must not author what it judged), because in a merge collision the arbitrator authors a third thing, not either side it judged. RULED 2026-09-04 ("Y - and coordinate yhour upodate with reboot test"), after he asked to talk it over and argued the arbitrator has the right context because it read both sides. THE SETTLEMENT, superseding BOTH prior rulings: an arbitrator rules AND fixes, bounded by one line — "An arbitrator fixes what its ruling already determines, and hands off work its ruling merely implies." Worked cases he and this seat agreed: merge conflict, the ruling IS the merged text, so it writes; a test asserting what the contract does not promise, the ruling names the correct assertion, so it fixes; code that fails a test because the code is wrong, the ruling names the side but not the implementation, so a coder writes it. WHY THE OLD OBJECTION FELL: "a judge must not author what it judged" buys nothing here, because every output goes to an independent reviewer anyway, so an arbitrator's fix is reviewed like any other; forbidding it only discards the reading that produced the ruling. The real limit is SCALE, not authority — an arbitrator is instantiated to answer a question with a package assembled for judging, not equipped for production work of unknown size. HIS DESIGNER POINT, accepted with a sharpening: designer-to-validator is linear, so a validator rejection is not a conflict and needs no arbitrator; and a designer holding context they never wrote down is a DEFECT SIGNAL, not a resource — if the design cannot be worked from without that memory it is incomplete, which is what the validator catches; reaching into the designer's transcript would undo the minimum-hidden-context design and must be refused when someone later proposes it as an optimisation. ALSO IDENTIFIED: the pipeline already contains an arbitrator under another name — the Integrate node takes accepted code and tests from separate work items and reconciles them, two input connections, two producers, a conflict to resolve. The reboot-test seat accepted the principle over its own synthesis and is DELIBERATELY NOT asking him — it holds the question as pending in its minutes rather than editing its item, on the grounds that decision 19's gate rule forbids queueing him the same question twice, so THIS walk carries it and that seat applies whatever he rules. Same for their routing field (node reports an outcome, machine maps outcome to destination): they leave their item as ruled until he says otherwise, and asked this seat to take it only if it arises naturally here. ARBITRATOR CLAUSE GAINS ONE CLAUSE from their composite-node question: two input connections counted IN THE GRAPH BETWEEN NODES, not inside a composite one — otherwise the cold read's internal merge becomes an arbitrator by definition; this seat added that a merge which PRESENTS disagreement (each cell's wording preserved, with counts, per issue #166) is aggregation, while arbitration exists to produce one answer where there were two. SHARPENING from them on candidate (b): in this lane the already-ruled-text rule protects a pull request, but in the machine it protects the PASS COUNTER — without it the counter climbs on rounds that were never about the code and the escalation packet handed to the user is evidence about his own prior rulings being re-raised.

QUESTION PUT TO HIM, blocking their item 2.3: decision 19 gates him on the design and the test design and says nothing about the CONTRACT; their machine sends him one contract question (is this the right boundary for the component). Is the contract inside "design" for decision 19, or a third thing he gates? AWAITING HIS WORD.

TWO MORE CANDIDATE DECISIONS from their ruled list, proposed here: (a) a reviewer may reject the code or the prose parent it was built from (their consequence: the round-two "run is the round" guard applies to code findings only, since tests descend from the design and are no oracle for a defect in it); (b) a finding touching text the user has already ruled is quoted with the ruling and not reportable again — this already exists lane-locally in CLAUDE.md's reviewer rule, so adopting it would make it general. Their package/files vocabulary routes to the vocabulary walk (task #103). Their decision-4 equivalent was corrected on this seat's prompt: their contract writer no longer makes a stop-or-continue call on the design before writing; the pre-screen is a validator on the edge into it.

He supplied NOTES for the objective page, saved verbatim at docs/drafts/nedschorus-objective-wiki-page-user-notes-2026-09-03.md (four trusted node types — humans, agents, code, CPC; modular parallel nodes on a simple graph of simple state machines; minimum hidden context, state visible in versioned MD; no standard compaction; human oversight magnified; per-node point of view; upstream and downstream flow; arbitrator nodes merging inputs).

ITEM 6 CLOSED 2026-09-04 → revised and accepted ("approved"). THE RULING, the standing decisions as they enter the OBJECTIVE page: the twelve that restate rulings already in force stand as written; decision 4 REPLACED (a node attempts its task and reports what it could not do; no input audit); decision 6 REWRITTEN (only writers and arbitrators change things; a writer fixes nits in its own output; an arbitrator fixes what its ruling already determines and hands off work its ruling merely implies, its fix recorded, never silent; a non-producing node — cold reader, code reviewer, validator — returns findings and nothing else to whoever called it); decision 7 REWRITTEN (the state machine is logically persistent, not process-immortal; how nodes connect is an implementation choice, irrelevant to the state machine and the nodes; no controller mediates node traffic); decision 3 STRENGTHENED (nothing crosses a pass boundary except files); decision 13 LOSES the word "deterministic" (the user: the word is ambiguous; its real discriminator is judgment and unenumerated inputs versus enumerable behavior); NEW 18 (an agent the user is not directing reaches him through the liaison or by filing a `draft`-labelled issue); NEW 19 (the human reviews all final prose, after its cold read and before it lands; a node in that path, not an approver at its end; in the pipeline the designs, the contracts and the test plans, outside it the documents that govern the project; not code changes, not traffic between nodes — the "may approve without reading" clause was CUT on his instruction as extraneous to the state machine); NEW 20 (because he is the only participant with memory across passes he is a gate rather than an ordinary node; the restart cap caps how often the machine spends his attention; states keep his wait short — one decision at a time, plainly stated, independent work continuing meanwhile, never a question another node could settle); NEW, the validator (inputs are validated before the consuming node is instantiated; one question; an approval carries the weight of the criteria applied and names them, a rejection blocks on its own); NEW, prose always precedes code; NEW, an arbitrator is a node with two input connections, counted in the graph between nodes, not inside a composite one; NEW, a reviewer may reject the code or the prose parent it was built from; NEW, a finding touching text the user has already ruled is quoted with the ruling and not reportable again. The word "artifact" is STRUCK throughout (his ban, ruled in the reboot-test session, confirmed here). Naming: design and test plan, NOT code-design/test-code-design — he partially reversed that rename ("this process is for all designs... use designs unless we in a state or tag that is pure code"); code design remains available inside a purely-code state; the nouns are provisional pending the vocabulary walk (task #103). CORRECTION HE MADE to this seat: AI-run tests are not "non-deterministic" as a class — a test giving different verdicts on the same inputs is an UNRELIABLE test, which code tests also are through races and unpinned randomness, so it is not a new route and not an AI property; his framing, accepted: a node either did what it was asked or it did not, checkable for prompts and code alike. Consequential numbering: the decisions are renumbered cleanly in the split pull request (task #98).

OPEN AFTER ITEM 6, raised by the user at its close and being discussed before item 7: the naming of what a producing node BUILDS. His words: "I want to discuss test design and test code. I think we need to broaden our thinking (and perhaps for the code and test code path to be explicit that these nodes might build code, prompts or CPC. This is very powerful for both code and test paths because we may be able to come up with simple prompts that do smart things... code isn't a terrible word to cover both code and prompts, I dont' know a better one, instructions doesn't cut it."

RULED 2026-09-04 ("y"): the general term for what a producing node builds is IMPLEMENTATION, with "code" kept where the thing is code — the same shape he ruled for designs. The argument against "code" as the umbrella came from his own notes: he names four trusted node types (humans, agents, code, CPC), so code and CPC are distinct there, and using code as the umbrella makes the word mean one thing at the node-type level and a wider thing at the pipeline level. Implementation is the standard SDLC word for what realizes a design, carries no assumption about medium, and gives the parallels design→implementation and test plan→test implementation. DECISION 13 GAINS one sentence: a producing node may build code, a prompt, or a combination, and which it chose is part of its output, so a reviewer sees the choice rather than infers it. Grounding offered and accepted: the pattern is already in this repository — the cold-read cells are prompts doing what code could not (judging whether a passage reads as intended), and the codex review cell is CPC (code that launches a prompt and interprets its report).

## Item 7 — build step 1 filed as an issue

processed 2026-09-04 → accepted ("y - can you check in again with reboot-test to keep them in sync"). THE RULING: build step 1 from the notes' § 18 is filed as a GitHub issue with a pair document under docs/issues/, through the ghi-write skill; the reboot-test seat's state-machine design document lands into it. This is the promotion path he called dangerous at item 2, done deliberately and by his ruling. WHAT CHANGED BETWEEN THE ITEM BEING WRITTEN AND RULED: that seat is writing the state-machine design NOW (states, transitions, file names, numbering, each role's package), which is most of step 1, so the promotion was already under way and what was missing was the issue behind it — without one a design document reaches main with nothing anchoring it, the problem already open as task #70 about another design. ISSUE SCOPE must also name the step-1 pieces their document may not cover: the work item and node execution schemas, provenance and attestation fields, the outcome dimensions, and the decision packet, adapting the gatekeeper's existing response shape rather than inventing one. OWNER: his call; filed unowned. CAPTURED: task #106. Also ruled at this item, on the naming chore: task #105, scoped by measurement (9 pipeline-role lines in the architecture document; the 357 occurrences in scripts/ are about real code and must not change).

## Item 8 — the capture list

processed 2026-09-04 → revised and accepted ("y - that walk is old, so maybe obsolete - beware. Also is reboot test up on this? maybe you need to start monitoring their jsonl?"). THE CAPTURE LIST AS RULED: item 1's cold read on the objective and notes pages then a triage walk; item 2's split pull request from this seat as ned-review-merge (objective page into docs/wiki/queue opening with his verbatim notes, notes page with its front matter, old file deleted, twenty citations repointed, one link line in CLAUDE.md); item 3's liaison, controller-as-code, rename and reduced seat model in that pull request plus the liaison's own issue; item 4's skill line under a .walk-approved marker; item 5's draft-label sentence, the two retirements and the vocabulary walk; item 6's amended standing decisions and the scoped code-to-implementation rename; item 7's build step 1 issue with pair document. Six tasks from the discussion rather than the items: objective seat brief (#99), communication SOP (#100), stale seat briefs (#101), liaison issue (#102), vocabulary walk (#103), diagnose-failure two halves (#104); plus #105 rename, #106 build step 1. TWO LATE ADDITIONS ruled into this item, both from the reboot-test cold read (236 findings, rewrite verdict): (a) THE MACHINE, the busiest actor in their design and undefined — answered from decisions he had already ruled (13 lists permissions, state transitions, counters, validation, repeatable transforms and promotion as code's job, which is the machine's job list; 7 says the state machine is logically persistent), so decision 7 gains "The design-to-main state machine is logically persistent, not process-immortal. It is a program: it assembles each node's package, routes on the destination a node returns, counts passes and delivers escalations."; (b) THE TERMINAL STATE — their machine reaching no terminal state that touches main is CORRECT under decision 14 (one gate to main), so its terminal state is handing the package to the gate; answered by this seat as the merge lane without troubling him. HIS NAMING, which produced (a): "I think we are building the design-to-main-workflow the nodes are part of the design-to-main-state-machine." The workflow is the process, the state machine is the program that runs it, nodes are its states. THE SEAM DISCOVERED by checking that name for collision: docs/drafts/pr-main-process-design.md (commit 65a6670, 2026-08-22, on the unmerged merge-lane branch only) already designs the OTHER half — what happens once a pull request exists. Two seats designed two halves of one workflow; they meet at decision 14's gate; the seam is stated in the objective so each design knows where to stop.

WALK CLOSED 2026-09-04, all eight items ruled.
